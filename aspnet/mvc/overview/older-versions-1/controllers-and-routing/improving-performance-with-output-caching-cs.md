---
uid: mvc/overview/older-versions-1/controllers-and-routing/improving-performance-with-output-caching-cs
title: Повышение производительности с помощью кэшированияC#вывода () | Документация Майкрософт
author: microsoft
description: В этом руководстве вы узнаете, как можно значительно повысить производительность веб-приложений ASP.NET MVC, используя преимущества кэширования выходных данных. Вы...
ms.author: riande
ms.date: 01/27/2009
ms.assetid: 521c9117-81cd-4d8d-9d96-0256dc7bf50f
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/improving-performance-with-output-caching-cs
msc.type: authoredcontent
ms.openlocfilehash: 548c5bea2e9cf26e0574e72d2c0ea204dbd90f9c
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/06/2020
ms.locfileid: "78486672"
---
# <a name="improving-performance-with-output-caching-c"></a><span data-ttu-id="ad680-104">Повышение производительности благодаря кэшированию вывода (C#)</span><span class="sxs-lookup"><span data-stu-id="ad680-104">Improving Performance with Output Caching (C#)</span></span>

<span data-ttu-id="ad680-105">по [Майкрософт](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="ad680-105">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="ad680-106">В этом руководстве вы узнаете, как можно значительно повысить производительность веб-приложений ASP.NET MVC, используя преимущества кэширования выходных данных.</span><span class="sxs-lookup"><span data-stu-id="ad680-106">In this tutorial, you learn how you can dramatically improve the performance of your ASP.NET MVC web applications by taking advantage of output caching.</span></span> <span data-ttu-id="ad680-107">Вы узнаете, как кэшировать результат, возвращенный действием контроллера, чтобы одно и то же содержимое не создавалось каждый раз при вызове действия новым пользователем.</span><span class="sxs-lookup"><span data-stu-id="ad680-107">You learn how to cache the result returned from a controller action so that the same content does not need to be created each and every time a new user invokes the action.</span></span>

<span data-ttu-id="ad680-108">Цель этого руководства — объяснить, как можно значительно повысить производительность приложения ASP.NET MVC, используя преимущества кэша вывода.</span><span class="sxs-lookup"><span data-stu-id="ad680-108">The goal of this tutorial is to explain how you can dramatically improve the performance of an ASP.NET MVC application by taking advantage of the output cache.</span></span> <span data-ttu-id="ad680-109">Кэш вывода позволяет кэшировать содержимое, возвращенное действием контроллера.</span><span class="sxs-lookup"><span data-stu-id="ad680-109">The output cache enables you to cache the content returned by a controller action.</span></span> <span data-ttu-id="ad680-110">Таким образом, не нужно создавать одно и то же содержимое каждый раз, когда вызывается одно и то же действие контроллера.</span><span class="sxs-lookup"><span data-stu-id="ad680-110">That way, the same content does not need to be generated each and every time the same controller action is invoked.</span></span>

<span data-ttu-id="ad680-111">Представьте, например, что приложение ASP.NET MVC отображает список записей базы данных в представлении с именем index.</span><span class="sxs-lookup"><span data-stu-id="ad680-111">Imagine, for example, that your ASP.NET MVC application displays a list of database records in a view named Index.</span></span> <span data-ttu-id="ad680-112">Как правило, каждый раз, когда пользователь вызывает действие контроллера, возвращающее представление индекса, набор записей базы данных необходимо извлечь из базы данных, выполнив запрос к базе данных.</span><span class="sxs-lookup"><span data-stu-id="ad680-112">Normally, each and every time that a user invokes the controller action that returns the Index view, the set of database records must be retrieved from the database by executing a database query.</span></span>

<span data-ttu-id="ad680-113">С другой стороны, если воспользоваться кэшем вывода, то можно избежать выполнения запроса к базе данных при каждом вызове одного и того же действия контроллера.</span><span class="sxs-lookup"><span data-stu-id="ad680-113">If, on the other hand, you take advantage of the output cache then you can avoid executing a database query every time any user invokes the same controller action.</span></span> <span data-ttu-id="ad680-114">Представление можно извлечь из кэша, а не повторно создавать из действия контроллера.</span><span class="sxs-lookup"><span data-stu-id="ad680-114">The view can be retrieved from the cache instead of being regenerated from the controller action.</span></span> <span data-ttu-id="ad680-115">Кэширование позволяет избежать выполнения избыточной работы на сервере.</span><span class="sxs-lookup"><span data-stu-id="ad680-115">Caching enables you to avoid performing redundant work on the server.</span></span>

## <a name="enabling-output-caching"></a><span data-ttu-id="ad680-116">Включение кэширования вывода</span><span class="sxs-lookup"><span data-stu-id="ad680-116">Enabling Output Caching</span></span>

<span data-ttu-id="ad680-117">Чтобы включить кэширование вывода, добавьте атрибут [OutputCache] к отдельному действию контроллера или ко всему классу контроллера.</span><span class="sxs-lookup"><span data-stu-id="ad680-117">You enable output caching by adding an [OutputCache] attribute to either an individual controller action or an entire controller class.</span></span> <span data-ttu-id="ad680-118">Например, контроллер в листинге 1 предоставляет действие с именем index ().</span><span class="sxs-lookup"><span data-stu-id="ad680-118">For example, the controller in Listing 1 exposes an action named Index().</span></span> <span data-ttu-id="ad680-119">Выходные данные действия Index () кэшируются в течение 10 секунд.</span><span class="sxs-lookup"><span data-stu-id="ad680-119">The output of the Index() action is cached for 10 seconds.</span></span>

<span data-ttu-id="ad680-120">**Листинг 1 — Controllers\HomeController.cs**</span><span class="sxs-lookup"><span data-stu-id="ad680-120">**Listing 1 – Controllers\HomeController.cs**</span></span>

[!code-csharp[Main](improving-performance-with-output-caching-cs/samples/sample1.cs)]

<span data-ttu-id="ad680-121">В бета-версиях ASP.NET MVC не работает кэширование выходных данных для URL-адреса, такого как [http://www.MySite.com/](http://www.mysite.com/).</span><span class="sxs-lookup"><span data-stu-id="ad680-121">In the Beta versions of ASP.NET MVC, output caching does not work for a URL like [http://www.MySite.com/](http://www.mysite.com/).</span></span> <span data-ttu-id="ad680-122">Вместо этого необходимо ввести URL-адрес, например [http://www.MySite.com/Home/Index](http://www.mysite.com/Home/Index).</span><span class="sxs-lookup"><span data-stu-id="ad680-122">Instead, you must enter a URL like [http://www.MySite.com/Home/Index](http://www.mysite.com/Home/Index).</span></span> 

<span data-ttu-id="ad680-123">В листинге 1 выходные данные действия Index () кэшируются в течение 10 секунд.</span><span class="sxs-lookup"><span data-stu-id="ad680-123">In Listing 1, the output of the Index() action is cached for 10 seconds.</span></span> <span data-ttu-id="ad680-124">При желании можно указать более длительную продолжительность кэширования.</span><span class="sxs-lookup"><span data-stu-id="ad680-124">If you prefer, you can specify a much longer cache duration.</span></span> <span data-ttu-id="ad680-125">Например, если требуется кэшировать выходные данные действия контроллера в течение одного дня, можно указать длительность кэширования в 86400 секунд (60 секунд \* 60 минут \* 24 часа).</span><span class="sxs-lookup"><span data-stu-id="ad680-125">For example, if you want to cache the output of a controller action for one day then you can specify a cache duration of 86400 seconds (60 seconds \* 60 minutes \* 24 hours).</span></span>

<span data-ttu-id="ad680-126">Нет никакой гарантии, что содержимое будет кэшироваться на указанный период времени.</span><span class="sxs-lookup"><span data-stu-id="ad680-126">There is no guarantee that content will be cached for the amount of time that you specify.</span></span> <span data-ttu-id="ad680-127">Когда ресурсы памяти становятся ненизкими, кэш начинает автоматическое исключение содержимого.</span><span class="sxs-lookup"><span data-stu-id="ad680-127">When memory resources become low, the cache starts evicting content automatically.</span></span>

<span data-ttu-id="ad680-128">Контроллер Home в листинге 1 возвращает представление индекса в листинге 2.</span><span class="sxs-lookup"><span data-stu-id="ad680-128">The Home controller in Listing 1 returns the Index view in Listing 2.</span></span> <span data-ttu-id="ad680-129">Нет ничего особенного в этом представлении.</span><span class="sxs-lookup"><span data-stu-id="ad680-129">There is nothing special about this view.</span></span> <span data-ttu-id="ad680-130">В представлении индекса просто отображается текущее время (см. рис. 1).</span><span class="sxs-lookup"><span data-stu-id="ad680-130">The Index view simply displays the current time (see Figure 1).</span></span>

<span data-ttu-id="ad680-131">**Листинг 2 — Виевс\хоме\индекс.аспкс**</span><span class="sxs-lookup"><span data-stu-id="ad680-131">**Listing 2 – Views\Home\Index.aspx**</span></span>

[!code-aspx[Main](improving-performance-with-output-caching-cs/samples/sample2.aspx)]

<span data-ttu-id="ad680-132">**Рис. 1. представление кэшированного индекса**</span><span class="sxs-lookup"><span data-stu-id="ad680-132">**Figure 1 – Cached Index view**</span></span>

![clip_image002](improving-performance-with-output-caching-cs/_static/image1.jpg)

<span data-ttu-id="ad680-134">Если вы вызываете действие index () несколько раз, введя URL-адрес/Хоме/индекс в адресной строке браузера и нажимая кнопку Обновить/перезагрузить в браузере, то время, отображаемое представлением индекса, не изменится на 10 секунд.</span><span class="sxs-lookup"><span data-stu-id="ad680-134">If you invoke the Index() action multiple times by entering the URL /Home/Index in the address bar of your browser and hitting the Refresh/Reload button in your browser repeatedly, then the time displayed by the Index view won't change for 10 seconds.</span></span> <span data-ttu-id="ad680-135">Это же время отображается, так как представление кэшируется.</span><span class="sxs-lookup"><span data-stu-id="ad680-135">The same time is displayed because the view is cached.</span></span>

<span data-ttu-id="ad680-136">Важно понимать, что одно и то же представление кэшируется для всех, кто посещает ваше приложение.</span><span class="sxs-lookup"><span data-stu-id="ad680-136">It is important to understand that the same view is cached for everyone who visits your application.</span></span> <span data-ttu-id="ad680-137">Любой, кто вызывает действие index (), получает ту же кэшированную версию представления индекса.</span><span class="sxs-lookup"><span data-stu-id="ad680-137">Anyone who invokes the Index() action will get the same cached version of the Index view.</span></span> <span data-ttu-id="ad680-138">Это означает, что объем работы, которую должен выполнить веб-сервер для обслуживания представления индекса, значительно уменьшился.</span><span class="sxs-lookup"><span data-stu-id="ad680-138">This means that the amount of work that the web server must perform to serve the Index view is dramatically reduced.</span></span>

<span data-ttu-id="ad680-139">Представление в листинге 2 делает что-то очень простое.</span><span class="sxs-lookup"><span data-stu-id="ad680-139">The view in Listing 2 happens to be doing something really simple.</span></span> <span data-ttu-id="ad680-140">В представлении просто отображается текущее время.</span><span class="sxs-lookup"><span data-stu-id="ad680-140">The view just displays the current time.</span></span> <span data-ttu-id="ad680-141">Тем не менее можно легко кэшировать представление, отображающее набор записей базы данных.</span><span class="sxs-lookup"><span data-stu-id="ad680-141">However, you could just as easily cache a view that displays a set of database records.</span></span> <span data-ttu-id="ad680-142">В этом случае набор записей базы данных не требуется извлекать из базы данных каждый раз, когда вызывается действие контроллера, возвращающее представление.</span><span class="sxs-lookup"><span data-stu-id="ad680-142">In that case, the set of database records would not need to be retrieved from the database each and every time the controller action that returns the view is invoked.</span></span> <span data-ttu-id="ad680-143">Кэширование может уменьшить объем работы, которую должен выполнять как веб-сервер, так и сервер базы данных.</span><span class="sxs-lookup"><span data-stu-id="ad680-143">Caching can reduce the amount of work that both your web server and database server must perform.</span></span>

<span data-ttu-id="ad680-144">Не используйте директиву Page &lt;% @ OutputCache%&gt; в представлении MVC.</span><span class="sxs-lookup"><span data-stu-id="ad680-144">Don't use the page &lt;%@ OutputCache %&gt; directive in an MVC view.</span></span> <span data-ttu-id="ad680-145">Эта директива суперсовременныеся из мира веб-форм и не должна использоваться в приложении ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="ad680-145">This directive is bleeding over from the Web Forms world and should not be used in an ASP.NET MVC application.</span></span>

## <a name="where-content-is-cached"></a><span data-ttu-id="ad680-146">Где кэшируется содержимое</span><span class="sxs-lookup"><span data-stu-id="ad680-146">Where Content is Cached</span></span>

<span data-ttu-id="ad680-147">По умолчанию при использовании атрибута [OutputCache] содержимое кэшируется в трех местах: веб-сервере, прокси-серверах и веб-браузере.</span><span class="sxs-lookup"><span data-stu-id="ad680-147">By default, when you use the [OutputCache] attribute, content is cached in three locations: the web server, any proxy servers, and the web browser.</span></span> <span data-ttu-id="ad680-148">Вы можете точно контролировать место кэширования содержимого, изменяя свойство Location атрибута [OutputCache].</span><span class="sxs-lookup"><span data-stu-id="ad680-148">You can control exactly where content is cached by modifying the Location property of the [OutputCache] attribute.</span></span>

<span data-ttu-id="ad680-149">Можно задать для свойства Location одно из следующих значений:</span><span class="sxs-lookup"><span data-stu-id="ad680-149">You can set the Location property to any one of the following values:</span></span>

> <span data-ttu-id="ad680-150">· Всеми</span><span class="sxs-lookup"><span data-stu-id="ad680-150">· Any</span></span>
> 
> <span data-ttu-id="ad680-151">· Компьютера</span><span class="sxs-lookup"><span data-stu-id="ad680-151">· Client</span></span>
> 
> <span data-ttu-id="ad680-152">· Подчинен</span><span class="sxs-lookup"><span data-stu-id="ad680-152">· Downstream</span></span>
> 
> <span data-ttu-id="ad680-153">· Сервером</span><span class="sxs-lookup"><span data-stu-id="ad680-153">· Server</span></span>
> 
> <span data-ttu-id="ad680-154">· None</span><span class="sxs-lookup"><span data-stu-id="ad680-154">· None</span></span>
> 
> <span data-ttu-id="ad680-155">· серверандклиент</span><span class="sxs-lookup"><span data-stu-id="ad680-155">· ServerAndClient</span></span>

<span data-ttu-id="ad680-156">По умолчанию свойство Location имеет значение Any.</span><span class="sxs-lookup"><span data-stu-id="ad680-156">By default, the Location property has the value Any.</span></span> <span data-ttu-id="ad680-157">Однако существуют ситуации, в которых может потребоваться кэширование только в браузере или только на сервере.</span><span class="sxs-lookup"><span data-stu-id="ad680-157">However, there are situations in which you might want to cache only on the browser or only on the server.</span></span> <span data-ttu-id="ad680-158">Например, при кэшировании данных, персонализированных для каждого пользователя, не следует кэшировать данные на сервере.</span><span class="sxs-lookup"><span data-stu-id="ad680-158">For example, if you are caching information that is personalized for each user then you should not cache the information on the server.</span></span> <span data-ttu-id="ad680-159">При отображении разных сведений для разных пользователей следует кэшировать данные только на клиенте.</span><span class="sxs-lookup"><span data-stu-id="ad680-159">If you are displaying different information to different users then you should cache the information only on the client.</span></span>

<span data-ttu-id="ad680-160">Например, контроллер в листинге 3 предоставляет действие с именем Name (), которое возвращает имя текущего пользователя.</span><span class="sxs-lookup"><span data-stu-id="ad680-160">For example, the controller in Listing 3 exposes an action named GetName() that returns the current user name.</span></span> <span data-ttu-id="ad680-161">Если гнездо входит в систему на веб-сайте и вызывает действие GetString (), то действие возвращает строку «Hi-гнездо».</span><span class="sxs-lookup"><span data-stu-id="ad680-161">If Jack logs into the website and invokes the GetName() action then the action returns the string "Hi Jack".</span></span> <span data-ttu-id="ad680-162">Если, в дальнейшем, Jill входит в систему на веб-сайте и вызывает действие GetString (), то она также получит строку «Hi-гнездо».</span><span class="sxs-lookup"><span data-stu-id="ad680-162">If, subsequently, Jill logs into the website and invokes the GetName() action then she also will get the string "Hi Jack".</span></span> <span data-ttu-id="ad680-163">Строка кэшируется на веб-сервере для всех пользователей после того, как гнездо первоначально вызывает действие контроллера.</span><span class="sxs-lookup"><span data-stu-id="ad680-163">The string is cached on the web server for all users after Jack initially invokes the controller action.</span></span>

<span data-ttu-id="ad680-164">**Листинг 3 — Контроллерс\бадусерконтроллер.КС**</span><span class="sxs-lookup"><span data-stu-id="ad680-164">**Listing 3 – Controllers\BadUserController.cs**</span></span>

[!code-csharp[Main](improving-performance-with-output-caching-cs/samples/sample3.cs)]

<span data-ttu-id="ad680-165">Скорее всего, контроллер в листинге 3 не работает нужным образом.</span><span class="sxs-lookup"><span data-stu-id="ad680-165">Most likely, the controller in Listing 3 does not work the way that you want.</span></span> <span data-ttu-id="ad680-166">Вы не хотите отображать сообщение «Hi» для Jill.</span><span class="sxs-lookup"><span data-stu-id="ad680-166">You don't want to display the message "Hi Jack" to Jill.</span></span>

<span data-ttu-id="ad680-167">Никогда не следует кэшировать персонализированное содержимое в кэше сервера.</span><span class="sxs-lookup"><span data-stu-id="ad680-167">You should never cache personalized content in the server cache.</span></span> <span data-ttu-id="ad680-168">Однако для повышения производительности может потребоваться кэширование персонализированного содержимого в кэше браузера.</span><span class="sxs-lookup"><span data-stu-id="ad680-168">However, you might want to cache the personalized content in the browser cache to improve performance.</span></span> <span data-ttu-id="ad680-169">Если содержимое кэшируется в браузере и пользователь вызывает одно и то же действие контроллера несколько раз, то содержимое можно извлечь из кэша браузера, а не сервера.</span><span class="sxs-lookup"><span data-stu-id="ad680-169">If you cache content in the browser, and a user invokes the same controller action multiple times, then the content can be retrieved from the browser cache instead of the server.</span></span>

<span data-ttu-id="ad680-170">Измененный контроллер в листинге 4 кэширует выходные данные действия-имени ().</span><span class="sxs-lookup"><span data-stu-id="ad680-170">The modified controller in Listing 4 caches the output of the GetName() action.</span></span> <span data-ttu-id="ad680-171">Однако содержимое кэшируется только в браузере, а не на сервере.</span><span class="sxs-lookup"><span data-stu-id="ad680-171">However, the content is cached only on the browser and not on the server.</span></span> <span data-ttu-id="ad680-172">Таким образом, когда несколько пользователей вызывают метод Name (), каждый пользователь получает собственное имя пользователя, а не имя пользователя другого человека.</span><span class="sxs-lookup"><span data-stu-id="ad680-172">That way, when multiple users invoke the GetName() method, each person gets their own user name and not another person's user name.</span></span>

<span data-ttu-id="ad680-173">**Листинг 4 — Контроллерс\усерконтроллер.КС**</span><span class="sxs-lookup"><span data-stu-id="ad680-173">**Listing 4 – Controllers\UserController.cs**</span></span>

[!code-csharp[Main](improving-performance-with-output-caching-cs/samples/sample4.cs)]

<span data-ttu-id="ad680-174">Обратите внимание, что атрибут [OutputCache] в листинге 4 содержит свойство Location, установленное в значение Аутпуткачелокатион. Client.</span><span class="sxs-lookup"><span data-stu-id="ad680-174">Notice that the [OutputCache] attribute in Listing 4 includes a Location property set to the value OutputCacheLocation.Client.</span></span> <span data-ttu-id="ad680-175">Атрибут [OutputCache] также включает свойство «не в хранилище».</span><span class="sxs-lookup"><span data-stu-id="ad680-175">The [OutputCache] attribute also includes a NoStore property.</span></span> <span data-ttu-id="ad680-176">Свойство "хранимая служба" используется для информирования прокси-серверов и браузера о том, что они не хранят постоянную копию кэшированного содержимого.</span><span class="sxs-lookup"><span data-stu-id="ad680-176">The NoStore property is used to inform proxy servers and browser that they should not store a permanent copy of the cached content.</span></span>

## <a name="varying-the-output-cache"></a><span data-ttu-id="ad680-177">Изменение кэша вывода</span><span class="sxs-lookup"><span data-stu-id="ad680-177">Varying the Output Cache</span></span>

<span data-ttu-id="ad680-178">В некоторых ситуациях могут потребоваться разные кэшированные версии одного и того же содержимого.</span><span class="sxs-lookup"><span data-stu-id="ad680-178">In some situations, you might want different cached versions of the very same content.</span></span> <span data-ttu-id="ad680-179">Предположим, например, что вы создаете страницу «основной/подробности».</span><span class="sxs-lookup"><span data-stu-id="ad680-179">Imagine, for example, that you are creating a master/detail page.</span></span> <span data-ttu-id="ad680-180">На главной странице отображается список заголовков фильмов.</span><span class="sxs-lookup"><span data-stu-id="ad680-180">The master page displays a list of movie titles.</span></span> <span data-ttu-id="ad680-181">Если щелкнуть заголовок, то будут получены подробные сведения о выбранном фильме.</span><span class="sxs-lookup"><span data-stu-id="ad680-181">When you click a title, you get details for the selected movie.</span></span>

<span data-ttu-id="ad680-182">При кэшировании страницы сведений сведения о том же фильме будут отображаться независимо от того, какой фильм вы щелкнули.</span><span class="sxs-lookup"><span data-stu-id="ad680-182">If you cache the details page, then the details for the same movie will be displayed no matter which movie you click.</span></span> <span data-ttu-id="ad680-183">Первый фильм, выбранный первым пользователем, будет отображаться для всех последующих пользователей.</span><span class="sxs-lookup"><span data-stu-id="ad680-183">The first movie selected by the first user will be displayed to all future users.</span></span>

<span data-ttu-id="ad680-184">Эту проблему можно устранить, воспользовавшись свойством VaryByParam атрибута [OutputCache].</span><span class="sxs-lookup"><span data-stu-id="ad680-184">You can fix this problem by taking advantage of the VaryByParam property of the [OutputCache] attribute.</span></span> <span data-ttu-id="ad680-185">Это свойство позволяет создавать разные кэшированные версии одного и того же содержимого, если параметр формы или параметр строки запроса различаются.</span><span class="sxs-lookup"><span data-stu-id="ad680-185">This property enables you to create different cached versions of the very same content when a form parameter or query string parameter varies.</span></span>

<span data-ttu-id="ad680-186">Например, контроллер в листинге 5 предоставляет два действия с именами Master () и Details ().</span><span class="sxs-lookup"><span data-stu-id="ad680-186">For example, the controller in Listing 5 exposes two actions named Master() and Details().</span></span> <span data-ttu-id="ad680-187">Действие Master () возвращает список заголовков фильмов, а действие Details () возвращает сведения о выбранном фильме.</span><span class="sxs-lookup"><span data-stu-id="ad680-187">The Master() action returns a list of movie titles and the Details() action returns the details for the selected movie.</span></span>

<span data-ttu-id="ad680-188">**Листинг 5 — Контроллерс\мовиесконтроллер.КС**</span><span class="sxs-lookup"><span data-stu-id="ad680-188">**Listing 5 – Controllers\MoviesController.cs**</span></span>

[!code-csharp[Main](improving-performance-with-output-caching-cs/samples/sample5.cs)]

<span data-ttu-id="ad680-189">Действие Master () включает свойство VaryByParam со значением «None».</span><span class="sxs-lookup"><span data-stu-id="ad680-189">The Master() action includes a VaryByParam property with the value "none".</span></span> <span data-ttu-id="ad680-190">При вызове действия Master () возвращается та же кэшированная версия главного представления.</span><span class="sxs-lookup"><span data-stu-id="ad680-190">When the Master() action is invoked, the same cached version of the Master view is returned.</span></span> <span data-ttu-id="ad680-191">Все параметры формы или параметры строки запроса игнорируются (см. рис. 2).</span><span class="sxs-lookup"><span data-stu-id="ad680-191">Any form parameters or query string parameters are ignored (see Figure 2).</span></span>

<span data-ttu-id="ad680-192">**Рис. 2. представление/Мовиес/мастер**</span><span class="sxs-lookup"><span data-stu-id="ad680-192">**Figure 2 – The /Movies/Master view**</span></span>

![clip_image004](improving-performance-with-output-caching-cs/_static/image2.jpg)

<span data-ttu-id="ad680-194">**Рис. 3 — представление/Мовиес/детаилс**</span><span class="sxs-lookup"><span data-stu-id="ad680-194">**Figure 3 – The /Movies/Details view**</span></span>

![clip_image006](improving-performance-with-output-caching-cs/_static/image3.jpg)

<span data-ttu-id="ad680-196">Действие Details () включает свойство VaryByParam со значением ID.</span><span class="sxs-lookup"><span data-stu-id="ad680-196">The Details() action includes a VaryByParam property with the value "Id".</span></span> <span data-ttu-id="ad680-197">Если в действие контроллера передаются различные значения параметра ID, создаются разные кэшированные версии представления сведений.</span><span class="sxs-lookup"><span data-stu-id="ad680-197">When different values of the Id parameter are passed to the controller action, different cached versions of the Details view are generated.</span></span>

<span data-ttu-id="ad680-198">Важно понимать, что использование свойства VaryByParam приводит к большему кэшированию и не меньше.</span><span class="sxs-lookup"><span data-stu-id="ad680-198">It is important to understand that using the VaryByParam property results in more caching and not less.</span></span> <span data-ttu-id="ad680-199">Для каждой другой версии параметра ID создается другая кэшированная версия представления Details.</span><span class="sxs-lookup"><span data-stu-id="ad680-199">A different cached version of the Details view is created for each different version of the Id parameter.</span></span>

<span data-ttu-id="ad680-200">Для свойства VaryByParam можно задать следующие значения:</span><span class="sxs-lookup"><span data-stu-id="ad680-200">You can set the VaryByParam property to the following values:</span></span>

> <span data-ttu-id="ad680-201">\* = создать другую кэшированную версию всякий раз, когда параметр формы или строки запроса изменяется.</span><span class="sxs-lookup"><span data-stu-id="ad680-201">\* = Create a different cached version whenever a form or query string parameter varies.</span></span>
> 
> <span data-ttu-id="ad680-202">None = никогда не создавать разные кэшированные версии</span><span class="sxs-lookup"><span data-stu-id="ad680-202">none = Never create different cached versions</span></span>
> 
> <span data-ttu-id="ad680-203">Список параметров точки с запятой: Создание разных кэшированных версий каждый раз, когда любая из параметров формы или строки запроса изменяется</span><span class="sxs-lookup"><span data-stu-id="ad680-203">Semicolon list of parameters = Create different cached versions whenever any of the form or query string parameters in the list varies</span></span>

## <a name="creating-a-cache-profile"></a><span data-ttu-id="ad680-204">Создание профиля кэша</span><span class="sxs-lookup"><span data-stu-id="ad680-204">Creating a Cache Profile</span></span>

<span data-ttu-id="ad680-205">В качестве альтернативы настройке свойств кэша вывода путем изменения свойств атрибута [OutputCache] можно создать профиль кэша в файле веб-конфигурации (Web. config).</span><span class="sxs-lookup"><span data-stu-id="ad680-205">As an alternative to configuring output cache properties by modifying properties of the [OutputCache] attribute, you can create a cache profile in the web configuration (web.config) file.</span></span> <span data-ttu-id="ad680-206">Создание профиля кэша в файле веб-конфигурации обеспечивает ряд важных преимуществ.</span><span class="sxs-lookup"><span data-stu-id="ad680-206">Creating a cache profile in the web configuration file offers a couple of important advantages.</span></span>

<span data-ttu-id="ad680-207">Прежде всего, настроив кэширование вывода в файле веб-конфигурации, можно контролировать, как действия контроллера кэшируют содержимое в одном централизованном расположении.</span><span class="sxs-lookup"><span data-stu-id="ad680-207">First, by configuring output caching in the web configuration file, you can control how controller actions cache content in one central location.</span></span> <span data-ttu-id="ad680-208">Можно создать один профиль кэша и применить профиль к нескольким контроллерам или действиям контроллера.</span><span class="sxs-lookup"><span data-stu-id="ad680-208">You can create one cache profile and apply the profile to several controllers or controller actions.</span></span>

<span data-ttu-id="ad680-209">Во-вторых, можно изменить конфигурационный файл, не выполняя повторную компиляцию приложения.</span><span class="sxs-lookup"><span data-stu-id="ad680-209">Second, you can modify the web configuration file without recompiling your application.</span></span> <span data-ttu-id="ad680-210">Если необходимо отключить кэширование для приложения, которое уже развернуто в рабочей среде, можно просто изменить профили кэша, определенные в файле веб-конфигурации.</span><span class="sxs-lookup"><span data-stu-id="ad680-210">If you need to disable caching for an application that has already been deployed to production, then you can simply modify the cache profiles defined in the web configuration file.</span></span> <span data-ttu-id="ad680-211">Любые изменения в файле веб-конфигурации будут автоматически обнаружены и применены.</span><span class="sxs-lookup"><span data-stu-id="ad680-211">Any changes to the web configuration file will be detected automatically and applied.</span></span>

<span data-ttu-id="ad680-212">Например, раздел &lt;Caching&gt; Web Configuration в листинге 6 определяет профиль кэша с именем Cache1Hour.</span><span class="sxs-lookup"><span data-stu-id="ad680-212">For example, the &lt;caching&gt; web configuration section in Listing 6 defines a cache profile named Cache1Hour.</span></span> <span data-ttu-id="ad680-213">Раздел &lt;Caching&gt; должен находиться в разделе &lt;System. Web&gt; файла веб-конфигурации.</span><span class="sxs-lookup"><span data-stu-id="ad680-213">The &lt;caching&gt; section must appear within the &lt;system.web&gt; section of a web configuration file.</span></span>

<span data-ttu-id="ad680-214">**Листинг 6. раздел кэширования для Web. config**</span><span class="sxs-lookup"><span data-stu-id="ad680-214">**Listing 6 – Caching section for web.config**</span></span>

[!code-xml[Main](improving-performance-with-output-caching-cs/samples/sample6.xml)]

<span data-ttu-id="ad680-215">Контроллер в листинге 7 показывает, как можно применить профиль Cache1Hour к действию контроллера с помощью атрибута [OutputCache].</span><span class="sxs-lookup"><span data-stu-id="ad680-215">The controller in Listing 7 illustrates how you can apply the Cache1Hour profile to a controller action with the [OutputCache] attribute.</span></span>

<span data-ttu-id="ad680-216">**Листинг 7 — Контроллерс\профилеконтроллер.КС**</span><span class="sxs-lookup"><span data-stu-id="ad680-216">**Listing 7 – Controllers\ProfileController.cs**</span></span>

[!code-csharp[Main](improving-performance-with-output-caching-cs/samples/sample7.cs)]

<span data-ttu-id="ad680-217">Если вызвать действие index (), предоставляемое контроллером в листинге 7, то то же самое время будет возвращено в течение 1 часа.</span><span class="sxs-lookup"><span data-stu-id="ad680-217">If you invoke the Index() action exposed by the controller in Listing 7 then the same time will be returned for 1 hour.</span></span>

## <a name="summary"></a><span data-ttu-id="ad680-218">Сводка</span><span class="sxs-lookup"><span data-stu-id="ad680-218">Summary</span></span>

<span data-ttu-id="ad680-219">Кэширование выходных данных предоставляет очень простой способ значительно повысить производительность приложений ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="ad680-219">Output caching provides you with a very easy method of dramatically improving the performance of your ASP.NET MVC applications.</span></span> <span data-ttu-id="ad680-220">В этом руководстве вы узнали, как использовать атрибут [OutputCache] для кэширования выходных данных действий контроллера.</span><span class="sxs-lookup"><span data-stu-id="ad680-220">In this tutorial, you learned how to use the [OutputCache] attribute to cache the output of controller actions.</span></span> <span data-ttu-id="ad680-221">Вы также узнали, как изменять свойства атрибута [OutputCache], такие как свойства Duration и VaryByParam, чтобы изменить способ кэширования содержимого.</span><span class="sxs-lookup"><span data-stu-id="ad680-221">You also learned how to modify properties of the [OutputCache] attribute such as the Duration and VaryByParam properties to modify how content gets cached.</span></span> <span data-ttu-id="ad680-222">Наконец, вы узнали, как определить профили кэша в файле веб-конфигурации.</span><span class="sxs-lookup"><span data-stu-id="ad680-222">Finally, you learned how to define cache profiles in the web configuration file.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="ad680-223">[Назад](understanding-action-filters-cs.md)
> [Вперед](adding-dynamic-content-to-a-cached-page-cs.md)</span><span class="sxs-lookup"><span data-stu-id="ad680-223">[Previous](understanding-action-filters-cs.md)
[Next](adding-dynamic-content-to-a-cached-page-cs.md)</span></span>
