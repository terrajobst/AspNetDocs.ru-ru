---
uid: mvc/overview/older-versions-1/contact-manager/iteration-7-add-ajax-functionality-vb
title: 'Итерации #7 – Добавление функций Ajax (VB) | Документация Майкрософт'
author: microsoft
description: В седьмой итерации мы повысить скорость реагирования и производительность приложения, добавляя поддержку Ajax.
ms.author: riande
ms.date: 02/20/2009
ms.assetid: f640e063-150e-453d-8cfc-7e54a6ce0f1e
msc.legacyurl: /mvc/overview/older-versions-1/contact-manager/iteration-7-add-ajax-functionality-vb
msc.type: authoredcontent
ms.openlocfilehash: cee2b6e7c7517a1e03ae26d5233fc438857a030c
ms.sourcegitcommit: 51b01b6ff8edde57d8243e4da28c9f1e7f1962b2
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 05/06/2019
ms.locfileid: "65123881"
---
# <a name="iteration-7--add-ajax-functionality-vb"></a><span data-ttu-id="6a8de-103">Итерации #7 – Добавление функций Ajax (VB)</span><span class="sxs-lookup"><span data-stu-id="6a8de-103">Iteration #7 – Add Ajax functionality (VB)</span></span>

<span data-ttu-id="6a8de-104">по [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="6a8de-104">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="6a8de-105">Скачать код</span><span class="sxs-lookup"><span data-stu-id="6a8de-105">Download Code</span></span>](iteration-7-add-ajax-functionality-vb/_static/contactmanager_7_vb1.zip)

> <span data-ttu-id="6a8de-106">В седьмой итерации мы повысить скорость реагирования и производительность приложения, добавляя поддержку Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-106">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="building-a-contact-management-aspnet-mvc-application-vb"></a><span data-ttu-id="6a8de-107">Создание приложения управления контактами ASP.NET MVC (VB)</span><span class="sxs-lookup"><span data-stu-id="6a8de-107">Building a Contact Management ASP.NET MVC Application (VB)</span></span>

<span data-ttu-id="6a8de-108">В этой серии руководств мы создаем всего приложения управления контактами от начала до конца.</span><span class="sxs-lookup"><span data-stu-id="6a8de-108">In this series of tutorials, we build an entire Contact Management application from start to finish.</span></span> <span data-ttu-id="6a8de-109">Приложение диспетчера контактов позволяет хранить контактные данные - имена, номера телефонов и адреса электронной почты — список людей.</span><span class="sxs-lookup"><span data-stu-id="6a8de-109">The Contact Manager application enables you to store contact information - names, phone numbers and email addresses - for a list of people.</span></span>

<span data-ttu-id="6a8de-110">Мы создаем приложение через несколько итераций.</span><span class="sxs-lookup"><span data-stu-id="6a8de-110">We build the application over multiple iterations.</span></span> <span data-ttu-id="6a8de-111">С каждой итерацией мы постепенно улучшить приложение.</span><span class="sxs-lookup"><span data-stu-id="6a8de-111">With each iteration, we gradually improve the application.</span></span> <span data-ttu-id="6a8de-112">Этот подход с несколькими итерации предназначена для того, чтобы можно было понять причину для каждого изменения.</span><span class="sxs-lookup"><span data-stu-id="6a8de-112">The goal of this multiple iteration approach is to enable you to understand the reason for each change.</span></span>

- <span data-ttu-id="6a8de-113">Итерация #1 - Создание приложения.</span><span class="sxs-lookup"><span data-stu-id="6a8de-113">Iteration #1 - Create the application.</span></span> <span data-ttu-id="6a8de-114">В первой итерации мы создадим Contact Manager простейшим способом невозможно.</span><span class="sxs-lookup"><span data-stu-id="6a8de-114">In the first iteration, we create the Contact Manager in the simplest way possible.</span></span> <span data-ttu-id="6a8de-115">Мы добавили поддержку для основных операций базы данных: Создание, чтение, обновление и удаление (CRUD).</span><span class="sxs-lookup"><span data-stu-id="6a8de-115">We add support for basic database operations: Create, Read, Update, and Delete (CRUD).</span></span>

- <span data-ttu-id="6a8de-116">#2 - итерация приложения красиво выглядеть.</span><span class="sxs-lookup"><span data-stu-id="6a8de-116">Iteration #2 - Make the application look nice.</span></span> <span data-ttu-id="6a8de-117">В этой итерации мы улучшить внешний вид приложения, изменив значение по умолчанию master страница представления ASP.NET MVC и каскадные таблицы стилей.</span><span class="sxs-lookup"><span data-stu-id="6a8de-117">In this iteration, we improve the appearance of the application by modifying the default ASP.NET MVC view master page and cascading style sheet.</span></span>

- <span data-ttu-id="6a8de-118">Итерации #3 - Добавление проверки форм.</span><span class="sxs-lookup"><span data-stu-id="6a8de-118">Iteration #3 - Add form validation.</span></span> <span data-ttu-id="6a8de-119">В третьей итерации мы добавим проверки базовой форме.</span><span class="sxs-lookup"><span data-stu-id="6a8de-119">In the third iteration, we add basic form validation.</span></span> <span data-ttu-id="6a8de-120">Мы запретить Отправка формы без завершения обязательные поля.</span><span class="sxs-lookup"><span data-stu-id="6a8de-120">We prevent people from submitting a form without completing required form fields.</span></span> <span data-ttu-id="6a8de-121">Кроме того, мы проверяем, адреса электронной почты и номера телефонов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-121">We also validate email addresses and phone numbers.</span></span>

- <span data-ttu-id="6a8de-122">Итерация #4 - Создание слабых связей в приложении.</span><span class="sxs-lookup"><span data-stu-id="6a8de-122">Iteration #4 - Make the application loosely coupled.</span></span> <span data-ttu-id="6a8de-123">В этой четвертой итерации мы воспользоваться преимуществами нескольких шаблонов дизайна программного обеспечения, чтобы упростить обслуживании и изменении приложения диспетчера контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-123">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="6a8de-124">Например мы выполнили рефакторинг наше приложение, чтобы использовать шаблон репозитория и шаблон внедрения зависимостей.</span><span class="sxs-lookup"><span data-stu-id="6a8de-124">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

- <span data-ttu-id="6a8de-125">Итерации #5 - Создание модульных тестов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-125">Iteration #5 - Create unit tests.</span></span> <span data-ttu-id="6a8de-126">В пятой итерации мы вам наше приложение проще обслуживании и изменении путем добавления модульных тестов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-126">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="6a8de-127">Мы макетирование наших классов модели данных и создания модульных тестов для контроллеров и логику проверки.</span><span class="sxs-lookup"><span data-stu-id="6a8de-127">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>

- <span data-ttu-id="6a8de-128">Итерация #6 - использование управляемой тестами разработки.</span><span class="sxs-lookup"><span data-stu-id="6a8de-128">Iteration #6 - Use test-driven development.</span></span> <span data-ttu-id="6a8de-129">В этой шестой итерации мы добавляем новые функциональные возможности в наше приложение, сначала написание модульных тестов и написании кода в отношении модульные тесты.</span><span class="sxs-lookup"><span data-stu-id="6a8de-129">In this sixth iteration, we add new functionality to our application by writing unit tests first and writing code against the unit tests.</span></span> <span data-ttu-id="6a8de-130">В этой итерации мы добавляем групп контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-130">In this iteration, we add contact groups.</span></span>

- <span data-ttu-id="6a8de-131">Итерации #7 - Добавление функций Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-131">Iteration #7 - Add Ajax functionality.</span></span> <span data-ttu-id="6a8de-132">В седьмой итерации мы повысить скорость реагирования и производительность приложения, добавляя поддержку Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-132">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="this-iteration"></a><span data-ttu-id="6a8de-133">Эта итерация</span><span class="sxs-lookup"><span data-stu-id="6a8de-133">This Iteration</span></span>

<span data-ttu-id="6a8de-134">В этой итерации приложения диспетчера контактов мы выполнили рефакторинг наше приложение, чтобы использовать Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-134">In this iteration of the Contact Manager application, we refactor our application to make use of Ajax.</span></span> <span data-ttu-id="6a8de-135">Используя преимущества Ajax, мы ускорения нашего приложения.</span><span class="sxs-lookup"><span data-stu-id="6a8de-135">By taking advantage of Ajax, we make our application more responsive.</span></span> <span data-ttu-id="6a8de-136">Можно избежать визуализации всей страницы, когда нам потребуется обновить только определенные области страницы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-136">We can avoid rendering an entire page when we need to update only a certain region in a page.</span></span>

<span data-ttu-id="6a8de-137">Мы выполните рефакторинг нашего представления индекса, таким образом, чтобы мы кое t необходимость повторного отображения всей страницы, каждый раз, когда пользователь выберет новую группу контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-137">We'll refactor our Index view so that we don t need to redisplay the entire page whenever someone selects a new contact group.</span></span> <span data-ttu-id="6a8de-138">Вместо этого при нажатии группе контактов, мы просто обновить список контактов и оставить только остальной части страницы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-138">Instead, when someone clicks a contact group, we'll just update the list of contacts and leave the rest of the page alone.</span></span>

<span data-ttu-id="6a8de-139">Также мы изменим наш delete связать works способ.</span><span class="sxs-lookup"><span data-stu-id="6a8de-139">We'll also change the way our delete link works.</span></span> <span data-ttu-id="6a8de-140">Вместо отображения на страницу подтверждения в отдельном, мы будем отображать диалоговое окно подтверждения JavaScript.</span><span class="sxs-lookup"><span data-stu-id="6a8de-140">Instead of displaying a separate confirmation page, we'll display a JavaScript confirmation dialog.</span></span> <span data-ttu-id="6a8de-141">Если вы подтвердите, что Вы действительно хотите удалить контакт, операция HTTP DELETE выполняется на сервере, чтобы удалить запись из базы данных.</span><span class="sxs-lookup"><span data-stu-id="6a8de-141">If you confirm that you want to delete a contact, an HTTP DELETE operation is performed against the server to delete the contact record from the database.</span></span>

<span data-ttu-id="6a8de-142">Кроме того мы будет использовать jQuery для добавления эффектов анимации для нашего представления индекса.</span><span class="sxs-lookup"><span data-stu-id="6a8de-142">Furthermore, we will take advantage of jQuery to add animation effects to our Index view.</span></span> <span data-ttu-id="6a8de-143">Мы будем отображать анимации, когда новый список контактов извлекается с сервера.</span><span class="sxs-lookup"><span data-stu-id="6a8de-143">We'll display an animation when the new list of contacts is being fetched from the server.</span></span>

<span data-ttu-id="6a8de-144">Наконец мы рассмотрим преимущества платформы AJAX для ASP.NET поддержка управление историей браузера.</span><span class="sxs-lookup"><span data-stu-id="6a8de-144">Finally, we'll take advantage of the ASP.NET AJAX framework support for managing browser history.</span></span> <span data-ttu-id="6a8de-145">Мы создадим точек всякий раз, когда мы выполняем вызова Ajax для обновления списка контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-145">We'll create history points whenever we perform an Ajax call to update the contact list.</span></span> <span data-ttu-id="6a8de-146">Таким образом, браузер назад и вперед кнопки будет работать.</span><span class="sxs-lookup"><span data-stu-id="6a8de-146">That way, the browser backward and forward buttons will work.</span></span>

## <a name="why-use-ajax"></a><span data-ttu-id="6a8de-147">Зачем использовать Ajax?</span><span class="sxs-lookup"><span data-stu-id="6a8de-147">Why use Ajax?</span></span>

<span data-ttu-id="6a8de-148">Использование Ajax имеет множество преимуществ.</span><span class="sxs-lookup"><span data-stu-id="6a8de-148">Using Ajax has many benefits.</span></span> <span data-ttu-id="6a8de-149">Во-первых Добавление функциональности Ajax на приложения позволяет обеспечить лучшие впечатления.</span><span class="sxs-lookup"><span data-stu-id="6a8de-149">First, adding Ajax functionality to an application results in a better user experience.</span></span> <span data-ttu-id="6a8de-150">В обычном веб-приложения всей страницы должен учитываться на сервере каждый раз, пользователь выполняет действие.</span><span class="sxs-lookup"><span data-stu-id="6a8de-150">In a normal web application, the entire page must be posted back to the server each and every time a user performs an action.</span></span> <span data-ttu-id="6a8de-151">Каждый раз, когда выполняется действие блокировки браузера и пользователю необходимо дождаться всей страницы извлечь и впоследствии.</span><span class="sxs-lookup"><span data-stu-id="6a8de-151">Whenever you perform an action, the browser locks and the user must wait until the entire page is fetched and redisplayed.</span></span>

<span data-ttu-id="6a8de-152">Это было бы интерфейс неприемлемыми в случае настольного приложения.</span><span class="sxs-lookup"><span data-stu-id="6a8de-152">This would be an unacceptable experience in the case of a desktop application.</span></span> <span data-ttu-id="6a8de-153">Но в большинстве случаев мы жили с этой неприятной в случае веб-приложения, так как мы не знали, что мы можем сделать лучше.</span><span class="sxs-lookup"><span data-stu-id="6a8de-153">But, traditionally, we lived with this bad user experience in the case of a web application because we did not know that we could do any better.</span></span> <span data-ttu-id="6a8de-154">Мы подумали, что было ограничение веб-приложений, когда на самом деле он был только что ограничение наших imaginations.</span><span class="sxs-lookup"><span data-stu-id="6a8de-154">We thought it was a limitation of web applications when, in actuality, it was just a limitation of our imaginations.</span></span>

<span data-ttu-id="6a8de-155">В приложении Ajax нигде не t нужно привести к остановке для обновления страницы взаимодействие с пользователем.</span><span class="sxs-lookup"><span data-stu-id="6a8de-155">In an Ajax application, you don t need to bring the user experience to a halt just to update a page.</span></span> <span data-ttu-id="6a8de-156">Вместо этого можно выполнить асинхронный запрос в фоновом режиме для обновления страницы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-156">Instead, you can perform an asynchronous request in the background to update the page.</span></span> <span data-ttu-id="6a8de-157">Нигде не t force пользователю, подождите, пока обновляется часть страницы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-157">You don t force the user to wait while part of the page gets updated.</span></span>

<span data-ttu-id="6a8de-158">Используя преимущества Ajax, можно повысить производительность приложения.</span><span class="sxs-lookup"><span data-stu-id="6a8de-158">By taking advantage of Ajax, you also can improve the performance of your application.</span></span> <span data-ttu-id="6a8de-159">Рассмотрим, как приложение Contact Manager прямо сейчас работает без функций Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-159">Consider how the Contact Manager application works right now without Ajax functionality.</span></span> <span data-ttu-id="6a8de-160">Если щелкнуть группу контактов, необходимо повторно всего индекса представления.</span><span class="sxs-lookup"><span data-stu-id="6a8de-160">When you click a contact group, the entire Index view must be redisplayed.</span></span> <span data-ttu-id="6a8de-161">Список контактов и список групп контактов должна быть получена с сервера базы данных.</span><span class="sxs-lookup"><span data-stu-id="6a8de-161">The list of contacts and list of contact groups must be retrieved from the database server.</span></span> <span data-ttu-id="6a8de-162">Все эти данные должны быть переданы по каналу связи с веб-сервера веб-браузер.</span><span class="sxs-lookup"><span data-stu-id="6a8de-162">All of this data must be passed across the wire from web server to web browser.</span></span>

<span data-ttu-id="6a8de-163">После мы добавлять функциональность Ajax на наше приложение, тем не менее, можно избежать повторное отображение всей страницы, когда пользователь щелкает группе контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-163">After we add Ajax functionality to our application, however, we can avoid redisplaying the entire page when a user clicks a contact group.</span></span> <span data-ttu-id="6a8de-164">Мы больше не придется взять групп, контактов из базы данных.</span><span class="sxs-lookup"><span data-stu-id="6a8de-164">We no longer need to grab the contact groups from the database.</span></span> <span data-ttu-id="6a8de-165">Кроме того, мы кое необходимость t для передачи всего представления индекса по каналу связи.</span><span class="sxs-lookup"><span data-stu-id="6a8de-165">We also don t need to push the entire Index view across the wire.</span></span> <span data-ttu-id="6a8de-166">Используя преимущества Ajax, нам уменьшить объем работы, которую необходимо выполнять наш сервер базы данных и мы сократить объем сетевого трафика, требующегося наше приложение.</span><span class="sxs-lookup"><span data-stu-id="6a8de-166">By taking advantage of Ajax, we reduce the amount of work that our database server must perform and we reduce the amount of network traffic required by our application.</span></span>

## <a name="don-t-be-afraid-of-ajax"></a><span data-ttu-id="6a8de-167">Дон t быть боится Ajax</span><span class="sxs-lookup"><span data-stu-id="6a8de-167">Don t be Afraid of Ajax</span></span>

<span data-ttu-id="6a8de-168">Некоторые разработчики не с помощью Ajax, так как они беспокоиться о браузерах нижнего уровня.</span><span class="sxs-lookup"><span data-stu-id="6a8de-168">Some developers avoid using Ajax because they worry about downlevel browsers.</span></span> <span data-ttu-id="6a8de-169">Они хотели бы убедиться, что веб-приложениям по-прежнему будет работать при обращении к в браузере, не поддерживает JavaScript.</span><span class="sxs-lookup"><span data-stu-id="6a8de-169">They want to make sure that their web applications will still work when accessed by a browser that does not support JavaScript.</span></span> <span data-ttu-id="6a8de-170">Поскольку Ajax зависит от JavaScript, некоторые разработчики не используют Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-170">Because Ajax depends on JavaScript, some developers avoid using Ajax.</span></span>

<span data-ttu-id="6a8de-171">Тем не менее если вы с осторожностью относитесь реализация Ajax можно создавать приложения, которые работают с верхнего и нижнего.</span><span class="sxs-lookup"><span data-stu-id="6a8de-171">However, if you are careful about how you implement Ajax then you can build applications that work with both uplevel and downlevel browsers.</span></span> <span data-ttu-id="6a8de-172">Наше приложение диспетчера контактов будет работать с обозревателями, поддерживающими JavaScript и браузеры, — нет.</span><span class="sxs-lookup"><span data-stu-id="6a8de-172">Our Contact Manager application will work with browsers that support JavaScript and browsers that do not.</span></span>

<span data-ttu-id="6a8de-173">Если вы используете приложение диспетчера контактов с помощью обозревателя, который поддерживает JavaScript вы получите удобства работы пользователя.</span><span class="sxs-lookup"><span data-stu-id="6a8de-173">If you use the Contact Manager application with a browser that supports JavaScript then you will have a better user experience.</span></span> <span data-ttu-id="6a8de-174">Например если щелкнуть группу контактов, будет обновляться только в регионе со страницей, отображающей контакты.</span><span class="sxs-lookup"><span data-stu-id="6a8de-174">For example, when you click a contact group, only the region of the page that displays contacts will be updated.</span></span>

<span data-ttu-id="6a8de-175">Если, с другой стороны, вы используете приложение диспетчера контактов с помощью браузера, который не поддерживает JavaScript (или с отключает JavaScript) будет иметь немного менее предпочтителен взаимодействие с пользователем.</span><span class="sxs-lookup"><span data-stu-id="6a8de-175">If, on the other hand, you use the Contact Manager application with a browser that does not support JavaScript (or that has JavaScript disabled) then you will have a slightly less desirable user experience.</span></span> <span data-ttu-id="6a8de-176">Например если щелкнуть группу контактов, всего индекса представления должен быть отправлен обратно браузера для отображения соответствующий список контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-176">For example, when you click a contact group, the entire Index view must be posted back to the browser in order to display the matching list of contacts.</span></span>

## <a name="adding-the-required-javascript-files"></a><span data-ttu-id="6a8de-177">Добавление файлов требуемого кода JavaScript</span><span class="sxs-lookup"><span data-stu-id="6a8de-177">Adding the Required JavaScript Files</span></span>

<span data-ttu-id="6a8de-178">Нам потребуется использовать три файла JavaScript для добавления функциональных возможностей Ajax к нашему приложению.</span><span class="sxs-lookup"><span data-stu-id="6a8de-178">We'll need to use three JavaScript files to add Ajax functionality to our application.</span></span> <span data-ttu-id="6a8de-179">Все три эти файлы включены в папке Scripts нового приложения ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="6a8de-179">All three of these files are included in the Scripts folder of a new ASP.NET MVC application.</span></span>

<span data-ttu-id="6a8de-180">Если вы планируете использовать Ajax на нескольких страницах приложения затем смысл включить необходимые файлы JavaScript в главную страницу приложения s представления.</span><span class="sxs-lookup"><span data-stu-id="6a8de-180">If you plan to use Ajax in multiple pages in your application then it makes sense to include the required JavaScript files in your application s view master page.</span></span> <span data-ttu-id="6a8de-181">Таким образом, файлы JavaScript будут включены во всех страниц в приложении автоматически.</span><span class="sxs-lookup"><span data-stu-id="6a8de-181">That way, the JavaScript files will be included in all of the pages in your application automatically.</span></span>

<span data-ttu-id="6a8de-182">Добавьте следующий код JavaScript включает внутри &lt;head&gt; тега, на главной странице представления:</span><span class="sxs-lookup"><span data-stu-id="6a8de-182">Add the following JavaScript includes inside the &lt;head&gt; tag of your view master page:</span></span>

[!code-html[Main](iteration-7-add-ajax-functionality-vb/samples/sample1.html)]

## <a name="refactoring-the-index-view-to-use-ajax"></a><span data-ttu-id="6a8de-183">Рефакторинг в представление Index, использование Ajax</span><span class="sxs-lookup"><span data-stu-id="6a8de-183">Refactoring the Index View to use Ajax</span></span>

<span data-ttu-id="6a8de-184">Позвольте s начать с изменения нашего представления индекса, таким образом, только если щелкнуть группу, контактные обновляет область представления, отображающий контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-184">Let s start by modifying our Index view so that clicking a contact group only updates the region of the view that displays contacts.</span></span> <span data-ttu-id="6a8de-185">Красным прямоугольником на рис. 1 содержит области, нам нужно обновить.</span><span class="sxs-lookup"><span data-stu-id="6a8de-185">The red box in Figure 1 contains the region that we want to update.</span></span>

<span data-ttu-id="6a8de-186">[![Обновление только контакты](iteration-7-add-ajax-functionality-vb/_static/image1.jpg)](iteration-7-add-ajax-functionality-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="6a8de-186">[![Updating only contacts](iteration-7-add-ajax-functionality-vb/_static/image1.jpg)](iteration-7-add-ajax-functionality-vb/_static/image1.png)</span></span>

<span data-ttu-id="6a8de-187">**Рис 01**: Обновление только контакты ([Просмотр полноразмерного изображения](iteration-7-add-ajax-functionality-vb/_static/image2.png))</span><span class="sxs-lookup"><span data-stu-id="6a8de-187">**Figure 01**: Updating only contacts([Click to view full-size image](iteration-7-add-ajax-functionality-vb/_static/image2.png))</span></span>

<span data-ttu-id="6a8de-188">Первым делом для отделения части представления, нам нужно обновить асинхронно в отдельном разделяемом (пользовательский элемент управления представления).</span><span class="sxs-lookup"><span data-stu-id="6a8de-188">The first step is to separate the part of the view that we want to update asynchronously into a separate partial (view user control).</span></span> <span data-ttu-id="6a8de-189">В части представления индекса, который выводит таблицу контактов была перемещена в разделяемом в листинге 1.</span><span class="sxs-lookup"><span data-stu-id="6a8de-189">The section of the Index view that displays the table of contacts has been moved into the partial in Listing 1.</span></span>

<span data-ttu-id="6a8de-190">**В листинге 1 - Views\Contact\ContactList.ascx**</span><span class="sxs-lookup"><span data-stu-id="6a8de-190">**Listing 1 - Views\Contact\ContactList.ascx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample2.aspx)]

<span data-ttu-id="6a8de-191">Обратите внимание, что частично в листинге 1 другой модели, чем в представление Index.</span><span class="sxs-lookup"><span data-stu-id="6a8de-191">Notice that the partial in Listing 1 has a different model than the Index view.</span></span> <span data-ttu-id="6a8de-192">*Inherits* атрибут в &lt;% @ Page %&gt; директива указывает, что разделяемого наследует от ViewUserControl&lt;группы&gt; класса.</span><span class="sxs-lookup"><span data-stu-id="6a8de-192">The *Inherits* attribute in the &lt;%@ Page %&gt; directive specifies that the partial inherits from the ViewUserControl&lt;Group&gt; class.</span></span>

<span data-ttu-id="6a8de-193">Обновленное представление индекса содержится в листинге 2.</span><span class="sxs-lookup"><span data-stu-id="6a8de-193">The updated Index view is contained in Listing 2.</span></span>

<span data-ttu-id="6a8de-194">**В листинге 2 - Views\Contact\Index.aspx**</span><span class="sxs-lookup"><span data-stu-id="6a8de-194">**Listing 2 - Views\Contact\Index.aspx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample3.aspx)]

<span data-ttu-id="6a8de-195">Существуют две вещи, которые вы должны заметить о обновленное представление в листинге 2.</span><span class="sxs-lookup"><span data-stu-id="6a8de-195">There are two things that you should notice about the updated view in Listing 2.</span></span> <span data-ttu-id="6a8de-196">Во-первых Обратите внимание на то, что все содержимое перемещено в разделяемом заменяется вызов Html.RenderPartial().</span><span class="sxs-lookup"><span data-stu-id="6a8de-196">First, notice that all of the content moved into the partial is replaced with a call to Html.RenderPartial().</span></span> <span data-ttu-id="6a8de-197">Метод Html.RenderPartial() вызывается в том случае, когда представление Index сначала запрашивается для отображения начального набора контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-197">The Html.RenderPartial() method is called when the Index view is first requested in order to display the initial set of contacts.</span></span>

<span data-ttu-id="6a8de-198">Во-вторых Обратите внимание на то, что Html.ActionLink(), используемый для отображения групп контактов были заменены Ajax.ActionLink().</span><span class="sxs-lookup"><span data-stu-id="6a8de-198">Second, notice that the Html.ActionLink() used to display contact groups has been replaced with an Ajax.ActionLink().</span></span> <span data-ttu-id="6a8de-199">Ajax.ActionLink() вызывается со следующими параметрами:</span><span class="sxs-lookup"><span data-stu-id="6a8de-199">The Ajax.ActionLink() is called with the following parameters:</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample4.aspx)]

<span data-ttu-id="6a8de-200">Первый параметр представляет текст, отображаемый для ссылки, второй параметр представляет значения маршрута, а третий параметр представляет параметры Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-200">The first parameter represents the text to display for the link, the second parameter represents the route values, and the third parameter represents the Ajax options.</span></span> <span data-ttu-id="6a8de-201">В этом случае мы используем параметр UpdateTargetId Ajax указывал на HTML-код &lt;div&gt; тег, который необходимо обновить после завершения запроса Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-201">In this case, we use the UpdateTargetId Ajax option to point to the HTML &lt;div&gt; tag that we want to update after the Ajax request completes.</span></span> <span data-ttu-id="6a8de-202">Нам нужно обновить &lt;div&gt; тег с новый список контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-202">We want to update the &lt;div&gt; tag with the new list of contacts.</span></span>

<span data-ttu-id="6a8de-203">Обновленный метод Index(), обратитесь в службу контроллера содержится в листинге 3.</span><span class="sxs-lookup"><span data-stu-id="6a8de-203">The updated Index() method of the Contact controller is contained in Listing 3.</span></span>

<span data-ttu-id="6a8de-204">**Листинг 3 - Controllers\ContactController.vb (метод Index)**</span><span class="sxs-lookup"><span data-stu-id="6a8de-204">**Listing 3 - Controllers\ContactController.vb (Index method)**</span></span>

[!code-vb[Main](iteration-7-add-ajax-functionality-vb/samples/sample5.vb)]

<span data-ttu-id="6a8de-205">Обновление действия Index() условно возвращает одно из следующих действий.</span><span class="sxs-lookup"><span data-stu-id="6a8de-205">The updated Index() action conditionally returns one of two things.</span></span> <span data-ttu-id="6a8de-206">Если действие Index() вызывается с помощью запроса Ajax контроллер возвращает частичный.</span><span class="sxs-lookup"><span data-stu-id="6a8de-206">If the Index() action is invoked by an Ajax request then the controller returns a partial.</span></span> <span data-ttu-id="6a8de-207">В противном случае действие Index() возвращает всю область.</span><span class="sxs-lookup"><span data-stu-id="6a8de-207">Otherwise, the Index() action returns an entire view.</span></span>

<span data-ttu-id="6a8de-208">Обратите внимание на то, что действие Index() не нужно возвращать столько данных, при вызове с помощью запроса Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-208">Notice that the Index() action does not need to return as much data when invoked by an Ajax request.</span></span> <span data-ttu-id="6a8de-209">В контексте обычным запросом действие индекса возвращает список всех групп, контактов и выбранной группе контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-209">In the context of a normal request, the Index action returns a list of all of the contact groups and the selected contact group.</span></span> <span data-ttu-id="6a8de-210">В контексте Ajax-запросом действие Index() возвращает только выбранной группы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-210">In the context of an Ajax request, the Index() action returns only the selected group.</span></span> <span data-ttu-id="6a8de-211">AJAX означает меньше работы на сервере базы данных.</span><span class="sxs-lookup"><span data-stu-id="6a8de-211">Ajax means less work on your database server.</span></span>

<span data-ttu-id="6a8de-212">В случае верхнего и нижнего работает нашего измененный индекс представления.</span><span class="sxs-lookup"><span data-stu-id="6a8de-212">Our modified Index view works in the case of both uplevel and downlevel browsers.</span></span> <span data-ttu-id="6a8de-213">Если браузер поддерживает JavaScript щелкните группу контактов, обновляется только в регионе представление, содержащее список контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-213">If you click a contact group, and your browser supports JavaScript, then only the region of the view that contains the list of contacts is updated.</span></span> <span data-ttu-id="6a8de-214">Если, с другой стороны, ваш браузер не поддерживает JavaScript, все представление обновляется.</span><span class="sxs-lookup"><span data-stu-id="6a8de-214">If, on the other hand, your browser does not support JavaScript, then the entire view is updated.</span></span>

<span data-ttu-id="6a8de-215">Нашего обновленный индекс представления есть одна проблема.</span><span class="sxs-lookup"><span data-stu-id="6a8de-215">Our updated Index view has one problem.</span></span> <span data-ttu-id="6a8de-216">Если щелкнуть группу контактов, не выделяется выбранной группы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-216">When you click a contact group, the selected group is not highlighted.</span></span> <span data-ttu-id="6a8de-217">Поскольку список групп отображаются за пределами региона, который обновляется при выполнении запроса Ajax, подходящую группу получить гаснет.</span><span class="sxs-lookup"><span data-stu-id="6a8de-217">Because the list of groups is displayed outside of the region that is updated during an Ajax request, the right group does not get highlighted.</span></span> <span data-ttu-id="6a8de-218">Эта проблема будет устранена в следующем разделе.</span><span class="sxs-lookup"><span data-stu-id="6a8de-218">We'll fix this issue in the next section.</span></span>

## <a name="adding-jquery-animation-effects"></a><span data-ttu-id="6a8de-219">Добавление эффектов анимации jQuery</span><span class="sxs-lookup"><span data-stu-id="6a8de-219">Adding jQuery Animation Effects</span></span>

<span data-ttu-id="6a8de-220">Как правило при щелчке ссылки на веб-странице, можно использовать индикатор браузера для обнаружения ли браузер активно ненужные обновленное содержимое.</span><span class="sxs-lookup"><span data-stu-id="6a8de-220">Normally, when you click a link in a web page, you can use the browser progress bar to detect whether or not the browser is actively fetching the updated content.</span></span> <span data-ttu-id="6a8de-221">При выполнении запроса Ajax, с другой стороны, индикатор браузера не содержит никаких действий.</span><span class="sxs-lookup"><span data-stu-id="6a8de-221">When performing an Ajax request, on the other hand, the browser progress bar does not show any progress.</span></span> <span data-ttu-id="6a8de-222">Это может усложнить заботящихся пользователей.</span><span class="sxs-lookup"><span data-stu-id="6a8de-222">This can make users nervous.</span></span> <span data-ttu-id="6a8de-223">Как узнать, заморожена ли браузер?</span><span class="sxs-lookup"><span data-stu-id="6a8de-223">How do you know whether the browser has frozen?</span></span>

<span data-ttu-id="6a8de-224">Существует несколько способов, которые вы можете указать пользователю, что работа будет выполняться при выполнении запроса Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-224">There are several ways that you can indicate to a user that work is being performed while performing an Ajax request.</span></span> <span data-ttu-id="6a8de-225">Одним из подходов является отображение простой анимации.</span><span class="sxs-lookup"><span data-stu-id="6a8de-225">One approach is to display a simple animation.</span></span> <span data-ttu-id="6a8de-226">Например можно Исчезание область при начале Ajax-запросом, появление в регионе после выполнения запроса.</span><span class="sxs-lookup"><span data-stu-id="6a8de-226">For example, you can fade out a region when an Ajax request begins and fade in the region when the request completes.</span></span>

<span data-ttu-id="6a8de-227">Мы будем использовать библиотеку jQuery, которая входит в состав платформы Microsoft ASP.NET MVC для создания эффектов анимации.</span><span class="sxs-lookup"><span data-stu-id="6a8de-227">We'll use the jQuery library which is included with the Microsoft ASP.NET MVC framework, to create the animation effects.</span></span> <span data-ttu-id="6a8de-228">Обновленное представление индекса содержится в листинге 4.</span><span class="sxs-lookup"><span data-stu-id="6a8de-228">The updated Index view is contained in Listing 4.</span></span>

<span data-ttu-id="6a8de-229">**Листинг 4 - Views\Contact\Index.aspx**</span><span class="sxs-lookup"><span data-stu-id="6a8de-229">**Listing 4 - Views\Contact\Index.aspx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample6.aspx)]

<span data-ttu-id="6a8de-230">Обратите внимание на то, что обновленное представление индекса содержит три новые функции JavaScript.</span><span class="sxs-lookup"><span data-stu-id="6a8de-230">Notice that the updated Index view contains three new JavaScript functions.</span></span> <span data-ttu-id="6a8de-231">Первые две функции позволяет скрывать и исчезать в список контактов, если щелкнуть группу контактные jQuery.</span><span class="sxs-lookup"><span data-stu-id="6a8de-231">The first two functions use jQuery to fade out and fade in the list of contacts when you click a new contact group.</span></span> <span data-ttu-id="6a8de-232">Третья функция отображает сообщение об ошибке, когда результаты запроса Ajax ошибку (например, время ожидания сети истекло).</span><span class="sxs-lookup"><span data-stu-id="6a8de-232">The third function displays an error message when an Ajax request results in an error (for example, network timeout).</span></span>

<span data-ttu-id="6a8de-233">Первая функция также отвечает за выделение выбранной группы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-233">The first function also takes care of highlighting the selected group.</span></span> <span data-ttu-id="6a8de-234">Класс = выбранный атрибут добавляется к родительскому элементу (элемент LI) щелчке элемента.</span><span class="sxs-lookup"><span data-stu-id="6a8de-234">A class= selected attribute is added to the parent element (the LI element) of the element clicked.</span></span> <span data-ttu-id="6a8de-235">Опять же jQuery упрощает выбор правильного элемента и добавьте класс CSS.</span><span class="sxs-lookup"><span data-stu-id="6a8de-235">Again, jQuery makes it easy to select the right element and add the CSS class.</span></span>

<span data-ttu-id="6a8de-236">Эти сценарии привязаны к связи группами с помощью параметра Ajax.ActionLink() AjaxOptions.</span><span class="sxs-lookup"><span data-stu-id="6a8de-236">These scripts are tied to the group links with the help of the Ajax.ActionLink() AjaxOptions parameter.</span></span> <span data-ttu-id="6a8de-237">Обновленные вызова метода Ajax.ActionLink() выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="6a8de-237">The updated Ajax.ActionLink() method call looks like this:</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample7.aspx)]

## <a name="adding-browser-history-support"></a><span data-ttu-id="6a8de-238">Добавление поддержки журнал браузера</span><span class="sxs-lookup"><span data-stu-id="6a8de-238">Adding Browser History Support</span></span>

<span data-ttu-id="6a8de-239">Как правило если щелкнуть ссылку, чтобы обновить страницу, журнал обозревателя обновляется.</span><span class="sxs-lookup"><span data-stu-id="6a8de-239">Normally, when you click a link to update a page, your browser history is updated.</span></span> <span data-ttu-id="6a8de-240">Таким образом, можно щелкнуть "Назад" браузера для перемещения назад во времени к предыдущему состоянию страницы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-240">That way, you can click the browser Back button to move back in time to the previous state of the page.</span></span> <span data-ttu-id="6a8de-241">Например если вы выберите в группу друзей и нажмите кнопку в группу бизнеса, можно нажмите кнопку "Назад" браузера и перейти в состояние страницы, если был выбран в группу друзей.</span><span class="sxs-lookup"><span data-stu-id="6a8de-241">For example, if you click the Friends contact group and then click the Business contact group, you can click the browser Back button to navigate back to the state of the page when the Friends contact group was selected.</span></span>

<span data-ttu-id="6a8de-242">К сожалению выполнение запроса Ajax не обновляются истории браузера.</span><span class="sxs-lookup"><span data-stu-id="6a8de-242">Unfortunately, performing an Ajax request does not update browser history automatically.</span></span> <span data-ttu-id="6a8de-243">Если щелкнуть группу контактов и список совпадающих контактов возвращается с помощью Ajax-запросом, истории браузера не обновляется.</span><span class="sxs-lookup"><span data-stu-id="6a8de-243">If you click a contact group, and the list of matching contacts is retrieved with an Ajax request, then the browser history is not updated.</span></span> <span data-ttu-id="6a8de-244">Чтобы перейти к группе контактов, выбрав группу контактов нельзя использовать "Назад" браузера.</span><span class="sxs-lookup"><span data-stu-id="6a8de-244">You cannot use the browser Back button to navigate back to a contact group after selecting a new contact group.</span></span>

<span data-ttu-id="6a8de-245">Чтобы пользователи могли работать с браузером обратно кнопку после выполнения запросов Ajax, необходимо выполнять немного больше работы.</span><span class="sxs-lookup"><span data-stu-id="6a8de-245">If you want users to be able to use the browser Back button after performing Ajax requests then you need to perform a little more work.</span></span> <span data-ttu-id="6a8de-246">Необходимо воспользоваться преимуществами возможностей управления журнал браузера, встроенных в платформы AJAX для ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="6a8de-246">You need to take advantage of the browser history management functionality built in the ASP.NET AJAX Framework.</span></span>

<span data-ttu-id="6a8de-247">Содержимое журнала обозревателя AJAX для ASP.NET, необходимо выполнить три действия:</span><span class="sxs-lookup"><span data-stu-id="6a8de-247">ASP.NET AJAX browser history, you need to do three things:</span></span>

1. <span data-ttu-id="6a8de-248">Включите журнал браузера, задав свойство enableBrowserHistory значение true.</span><span class="sxs-lookup"><span data-stu-id="6a8de-248">Enable Browser History by setting the enableBrowserHistory property to true.</span></span>
2. <span data-ttu-id="6a8de-249">Сохраните журнал точки, при изменении состояния представления путем вызова метода addHistoryPoint().</span><span class="sxs-lookup"><span data-stu-id="6a8de-249">Save history points when the state of a view changes by calling the addHistoryPoint() method.</span></span>
3. <span data-ttu-id="6a8de-250">Восстановить состояние представления, когда возникает событие navigate.</span><span class="sxs-lookup"><span data-stu-id="6a8de-250">Reconstruct the state of the view when the navigate event is raised.</span></span>

<span data-ttu-id="6a8de-251">Обновленное представление индекса содержится в листинге 5.</span><span class="sxs-lookup"><span data-stu-id="6a8de-251">The updated Index view is contained in Listing 5.</span></span>

<span data-ttu-id="6a8de-252">**В листинге 5 - Views\Contact\Index.aspx**</span><span class="sxs-lookup"><span data-stu-id="6a8de-252">**Listing 5 - Views\Contact\Index.aspx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample8.aspx)]

<span data-ttu-id="6a8de-253">В листинге 5 в функции pageInit() включить журнал браузера.</span><span class="sxs-lookup"><span data-stu-id="6a8de-253">In Listing 5, Browser History is enabled in the pageInit() function.</span></span> <span data-ttu-id="6a8de-254">Функция pageInit() также используется для настройки обработчик событий для перехода.</span><span class="sxs-lookup"><span data-stu-id="6a8de-254">The pageInit() function is also used to set up the event handler for the navigate event.</span></span> <span data-ttu-id="6a8de-255">Событие navigate возникает всякий раз, когда браузер: вперед и кнопка "Назад", вызывают состояние страницы для изменения.</span><span class="sxs-lookup"><span data-stu-id="6a8de-255">The navigate event is raised whenever the browser Forward or Back button causes the state of the page to change.</span></span>

<span data-ttu-id="6a8de-256">Метод beginContactList() вызывается в том случае, если щелкнуть группу контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-256">The beginContactList() method is called when you click a contact group.</span></span> <span data-ttu-id="6a8de-257">Этот метод создает новую точку предыдущего состояния путем вызова метода addHistoryPoint().</span><span class="sxs-lookup"><span data-stu-id="6a8de-257">This method creates a new history point by calling the addHistoryPoint() method.</span></span> <span data-ttu-id="6a8de-258">Идентификатор группы контактные щелчке добавляется в журнал.</span><span class="sxs-lookup"><span data-stu-id="6a8de-258">The id of the contact group clicked is added to history.</span></span>

<span data-ttu-id="6a8de-259">Идентификатор группы извлекается из атрибута expando ссылку группе контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-259">The group id is retrieved from an expando attribute on the contact group link.</span></span> <span data-ttu-id="6a8de-260">Ссылка отрисовывается с следующий вызов в Ajax.ActionLink().</span><span class="sxs-lookup"><span data-stu-id="6a8de-260">The link is rendered with the following call to Ajax.ActionLink().</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample9.aspx)]

<span data-ttu-id="6a8de-261">Последний параметр, передаваемый Ajax.ActionLink() добавляет атрибут expando с именем groupid по ссылке (нижний регистр для совместимости XHTML).</span><span class="sxs-lookup"><span data-stu-id="6a8de-261">The last parameter passed to the Ajax.ActionLink() adds an expando attribute named groupid to the link (lowercase for XHTML compatibility).</span></span>

<span data-ttu-id="6a8de-262">Когда пользователь касается обратно в браузер или кнопка "вперед", возникает событие navigate и вызывается метод navigate().</span><span class="sxs-lookup"><span data-stu-id="6a8de-262">When a user hits the browser Back or Forward button, the navigate event is raised and the navigate() method is called.</span></span> <span data-ttu-id="6a8de-263">Этот метод обновляет контактов, отображаемой на странице, чтобы оно соответствовало состоянию страницы, соответствующий точке журнал браузера, передаваемый в метод navigate.</span><span class="sxs-lookup"><span data-stu-id="6a8de-263">This method updates the contacts displayed in the page to match the state of the page that corresponds to the browser history point passed to the navigate method.</span></span>

## <a name="performing-ajax-deletes"></a><span data-ttu-id="6a8de-264">Удаляет выполнения Ajax</span><span class="sxs-lookup"><span data-stu-id="6a8de-264">Performing Ajax Deletes</span></span>

<span data-ttu-id="6a8de-265">В настоящее время, чтобы удалить контакт, необходимо щелкнуть ссылку «Удалить» и нажмите кнопку «Удалить», отображаются на странице подтверждения удаления (см. рис. 2).</span><span class="sxs-lookup"><span data-stu-id="6a8de-265">Currently, in order to delete a contact, you need to click on the Delete link and then click the Delete button displayed in the delete confirmation page (see Figure 2).</span></span> <span data-ttu-id="6a8de-266">Это может показаться массу запросов страниц, чтобы сделать что-то простое, например удаление записи базы данных.</span><span class="sxs-lookup"><span data-stu-id="6a8de-266">This seems like a lot of page requests to do something simple like deleting a database record.</span></span>

<span data-ttu-id="6a8de-267">[![Страница подтверждения удаления](iteration-7-add-ajax-functionality-vb/_static/image2.jpg)](iteration-7-add-ajax-functionality-vb/_static/image3.png)</span><span class="sxs-lookup"><span data-stu-id="6a8de-267">[![The delete confirmation page](iteration-7-add-ajax-functionality-vb/_static/image2.jpg)](iteration-7-add-ajax-functionality-vb/_static/image3.png)</span></span>

<span data-ttu-id="6a8de-268">**Рис. 02**: Страница подтверждения удаления ([Просмотр полноразмерного изображения](iteration-7-add-ajax-functionality-vb/_static/image4.png))</span><span class="sxs-lookup"><span data-stu-id="6a8de-268">**Figure 02**: The delete confirmation page([Click to view full-size image](iteration-7-add-ajax-functionality-vb/_static/image4.png))</span></span>

<span data-ttu-id="6a8de-269">Это подмывает вас, чтобы пропустить страницу подтверждения удаления и удалить контакт непосредственно из окна просмотра индекса.</span><span class="sxs-lookup"><span data-stu-id="6a8de-269">It is tempting to skip the delete confirmation page and delete a contact directly from the Index view.</span></span> <span data-ttu-id="6a8de-270">Следует избегать искушению действовать, потому что при таком подходе приложению бреши в системе безопасности.</span><span class="sxs-lookup"><span data-stu-id="6a8de-270">You should avoid this temptation because taking this approach opens your application to security holes.</span></span> <span data-ttu-id="6a8de-271">Как правило Дон t требуется выполнить операцию HTTP GET, при вызове действие, которое изменяет состояние веб-приложения.</span><span class="sxs-lookup"><span data-stu-id="6a8de-271">In general, you don t want to perform an HTTP GET operation when invoking an action that modifies the state of your web application.</span></span> <span data-ttu-id="6a8de-272">При выполнении удаления, вы хотите выполнить запрос HTTP POST или лучше всего, операции HTTP DELETE.</span><span class="sxs-lookup"><span data-stu-id="6a8de-272">When performing a delete, you want to perform an HTTP POST, or better yet, an HTTP DELETE operation.</span></span>

<span data-ttu-id="6a8de-273">Удалить ссылка содержится в частичной ContactList.</span><span class="sxs-lookup"><span data-stu-id="6a8de-273">The Delete link is contained in the ContactList partial.</span></span> <span data-ttu-id="6a8de-274">Обновленная версия ContactList частичного содержится в листинге 6.</span><span class="sxs-lookup"><span data-stu-id="6a8de-274">An updated version of the ContactList partial is contained in Listing 6.</span></span>

<span data-ttu-id="6a8de-275">**В листинге 6 - Views\Contact\ContactList.ascx**</span><span class="sxs-lookup"><span data-stu-id="6a8de-275">**Listing 6 - Views\Contact\ContactList.ascx**</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample10.aspx)]

<span data-ttu-id="6a8de-276">Ссылку «Удалить» отображается на следующий вызов в метод Ajax.ImageActionLink():</span><span class="sxs-lookup"><span data-stu-id="6a8de-276">The Delete link is rendered with the following call to the Ajax.ImageActionLink() method:</span></span>

[!code-aspx[Main](iteration-7-add-ajax-functionality-vb/samples/sample11.aspx)]

> [!NOTE] 
> 
> <span data-ttu-id="6a8de-277">Ajax.ImageActionLink() не является стандартной частью платформы ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="6a8de-277">The Ajax.ImageActionLink() is not a standard part of the ASP.NET MVC framework.</span></span> <span data-ttu-id="6a8de-278">Ajax.ImageActionLink() является пользовательских вспомогательных методов, включенных в проект Contact Manager.</span><span class="sxs-lookup"><span data-stu-id="6a8de-278">The Ajax.ImageActionLink() is a custom helper methods included in the Contact Manager project.</span></span>

<span data-ttu-id="6a8de-279">Параметр AjaxOptions имеет два свойства.</span><span class="sxs-lookup"><span data-stu-id="6a8de-279">The AjaxOptions parameter has two properties.</span></span> <span data-ttu-id="6a8de-280">Во-первых свойство подтверждение используется для отображения всплывающее диалоговое окно подтверждения JavaScript.</span><span class="sxs-lookup"><span data-stu-id="6a8de-280">First, the Confirm property is used to display a popup JavaScript confirmation dialog.</span></span> <span data-ttu-id="6a8de-281">Во-вторых свойство HttpMethod используется для выполнения операции HTTP DELETE.</span><span class="sxs-lookup"><span data-stu-id="6a8de-281">Second, the HttpMethod property is used to perform an HTTP DELETE operation.</span></span>

<span data-ttu-id="6a8de-282">Листинг 7 содержит новое действие AjaxDelete(), которая была добавлена к контроллеру контакта.</span><span class="sxs-lookup"><span data-stu-id="6a8de-282">Listing 7 contains a new AjaxDelete() action that has been added to the Contact controller.</span></span>

<span data-ttu-id="6a8de-283">**Листинг 7 - Controllers\ContactController.vb (AjaxDelete)**</span><span class="sxs-lookup"><span data-stu-id="6a8de-283">**Listing 7 - Controllers\ContactController.vb (AjaxDelete)**</span></span>   

[!code-vb[Main](iteration-7-add-ajax-functionality-vb/samples/sample12.vb)]

<span data-ttu-id="6a8de-284">Действие AjaxDelete() дополнен атрибутом AcceptVerbs.</span><span class="sxs-lookup"><span data-stu-id="6a8de-284">The AjaxDelete() action is decorated with an AcceptVerbs attribute.</span></span> <span data-ttu-id="6a8de-285">Этот атрибут запрещает действие вызова за исключением любой операцией HTTP, отличный от операции HTTP DELETE.</span><span class="sxs-lookup"><span data-stu-id="6a8de-285">This attribute prevents the action from being invoked except by any HTTP operation other than an HTTP DELETE operation.</span></span> <span data-ttu-id="6a8de-286">В частности не удается вызвать это действие с HTTP GET.</span><span class="sxs-lookup"><span data-stu-id="6a8de-286">In particular, you cannot invoke this action with an HTTP GET.</span></span>

<span data-ttu-id="6a8de-287">После удаления записи в базе данных, необходимо отобразить обновленный список контактов, не содержащего удаленной записи.</span><span class="sxs-lookup"><span data-stu-id="6a8de-287">After you delete database record, you need to display the updated list of contacts that does not contain the deleted record.</span></span> <span data-ttu-id="6a8de-288">Метод AjaxDelete() возвращает ContactList частичное и обновленный список контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-288">The AjaxDelete() method returns the ContactList partial and the updated list of contacts.</span></span>

## <a name="summary"></a><span data-ttu-id="6a8de-289">Сводка</span><span class="sxs-lookup"><span data-stu-id="6a8de-289">Summary</span></span>

<span data-ttu-id="6a8de-290">В этой итерации мы добавили функций Ajax в наше приложение диспетчера контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-290">In this iteration, we added Ajax functionality to our Contact Manager application.</span></span> <span data-ttu-id="6a8de-291">Мы использовали Ajax для повышения скорости реагирования и производительность приложения.</span><span class="sxs-lookup"><span data-stu-id="6a8de-291">We used Ajax to improve the responsiveness and performance of our application.</span></span>

<span data-ttu-id="6a8de-292">Во-первых мы оптимизировали представление Index, таким образом, щелкнув группу контактов не приводит к обновлению всего представления.</span><span class="sxs-lookup"><span data-stu-id="6a8de-292">First, we refactored the Index view so that clicking a contact group does not update the entire view.</span></span> <span data-ttu-id="6a8de-293">Вместо этого щелкнув группу контактов только обновляет список контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-293">Instead, clicking a contact group only updates the list of contacts.</span></span>

<span data-ttu-id="6a8de-294">Затем мы использовали эффекты анимации jQuery Исчезание и появление список контактов.</span><span class="sxs-lookup"><span data-stu-id="6a8de-294">Next, we used jQuery animation effects to fade out and fade in the list of contacts.</span></span> <span data-ttu-id="6a8de-295">Добавление анимации в приложении Ajax можно использовать для предоставления пользователям приложения эквивалентным индикатор хода выполнения браузера.</span><span class="sxs-lookup"><span data-stu-id="6a8de-295">Adding animation to an Ajax application can be used to provide users of the application with the equivalent of a browser progress bar.</span></span>

<span data-ttu-id="6a8de-296">Мы также добавили возможность журнал браузера к нашему приложению Ajax.</span><span class="sxs-lookup"><span data-stu-id="6a8de-296">We also added browser history support to our Ajax application.</span></span> <span data-ttu-id="6a8de-297">Мы включили пользователю, выбирая браузер обратно кнопки для изменения состояния представления индекса.</span><span class="sxs-lookup"><span data-stu-id="6a8de-297">We enabled users to click the browser Back and Forward buttons to change the state of the Index view.</span></span>

<span data-ttu-id="6a8de-298">Наконец мы создали удалить ссылку, которая поддерживает операции HTTP DELETE.</span><span class="sxs-lookup"><span data-stu-id="6a8de-298">Finally, we created a delete link that supports HTTP DELETE operations.</span></span> <span data-ttu-id="6a8de-299">Путем удаления Ajax, мы позволяют пользователям удалять записи базы данных без пользователя запрашивается страница подтверждения удаления дополнительных.</span><span class="sxs-lookup"><span data-stu-id="6a8de-299">By performing Ajax deletes, we enable users to delete database records without requiring the user to request an additional delete confirmation page.</span></span>

> [!div class="step-by-step"]
> [<span data-ttu-id="6a8de-300">Назад</span><span class="sxs-lookup"><span data-stu-id="6a8de-300">Previous</span></span>](iteration-6-use-test-driven-development-vb.md)
