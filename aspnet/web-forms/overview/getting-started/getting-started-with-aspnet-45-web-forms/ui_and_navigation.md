---
uid: web-forms/overview/getting-started/getting-started-with-aspnet-45-web-forms/ui_and_navigation
title: Пользовательский Интерфейс и навигация | Документация Майкрософт
author: Erikre
description: В этой серии руководств будет основы создания приложений веб-форм ASP.NET с помощью ASP.NET 4.5 и Microsoft Visual Studio Express 2013 для мы...
ms.author: riande
ms.date: 09/08/2014
ms.assetid: 5c76891d-e515-4885-b576-76bd2c494efe
msc.legacyurl: /web-forms/overview/getting-started/getting-started-with-aspnet-45-web-forms/ui_and_navigation
msc.type: authoredcontent
ms.openlocfilehash: 06816f3555a806d41caf562b910a4ec4427b13bd
ms.sourcegitcommit: 51b01b6ff8edde57d8243e4da28c9f1e7f1962b2
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 05/06/2019
ms.locfileid: "65134225"
---
# <a name="ui-and-navigation"></a>Пользовательский интерфейс и навигация

по [Erik Reitan](https://github.com/Erikre)

[Скачайте пример проекта Wingtip Toys (C#)](http://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) или [скачайте электронную книгу (PDF)](http://download.microsoft.com/download/0/F/B/0FBFAA46-2BFD-478F-8E56-7BF3C672DF9D/Getting%20Started%20with%20ASP.NET%204.5%20Web%20Forms%20and%20Visual%20Studio%202013.pdf)

> В этой серии руководств будет основы создания приложений веб-форм ASP.NET с помощью ASP.NET 4.5 и Microsoft Visual Studio Express 2013 для Web. Visual Studio 2013 [проект с исходным кодом C#](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) доступен на следующей странице этой серии руководств.

В этом учебнике предстоит изменить пользовательский Интерфейс веб-приложения по умолчанию для поддержки возможностей приложения внешнего хранилища Wingtip Toys. Кроме того вы добавите простые и навигации с привязкой к данным. Этот учебник основан на предыдущем учебном курсе «Создание уровня доступа к данным» и является частью серии руководств Wingtip Toys.

## <a name="what-youll-learn"></a>Вы узнаете, как:

- Как изменить пользовательский Интерфейс для поддержки возможностей приложения внешнего хранилища Wingtip Toys.
- Сведения о настройке элемент HTML5 для включения навигации по страницам.
- Как создать элемент управления, управляемых данными, для перехода к определенному продукту данных.
- Как отобразить данные из базы данных, созданных с помощью Entity Framework Code First.

Веб-формы ASP.NET позволяют создать динамическое содержимое для веб-приложения. Каждый веб-страницы ASP.NET создается так же, как статический веб-страницу (страница, не включает серверную обработку), но веб-страницу ASP.NET включает в себя дополнительные элементы, которые ASP.NET распознает и обрабатывает для создания HTML-кода при запуске страницы.

С помощью статической страницы HTML (*.htm* или *.html* файл), сервер выполняет `Web` запроса путем чтения файла и отправка его как-в браузер. В противоположность этому, когда кто-то запрашивает веб-страницу ASP.NET (*.aspx* файл), страница выполняется как программа на веб-сервере. Во время работы страницы, оно может выполнить любую задачу, требующую веб-сайт, включая вычисление значений, чтения или записи сведений о базе данных или вызов других программ. В качестве результата страница динамически создает разметку (например, элементы в формате HTML) и отправляет эти динамические выходные данные в браузер.

## <a name="modifying-the-ui"></a>Изменение пользовательского интерфейса

Вам будут и дальше в этой серии руководств, изменив *Default.aspx* страницы. Необходимо изменить пользовательский Интерфейс, который уже установлен с помощью шаблона по умолчанию, используемый для создания приложения. Тип изменения, которая будет выполняться являются типичными, при создании любого приложения веб-форм. Это будет сделать, изменив название, заменив часть содержимого и удаление содержимого ненужные по умолчанию.

1. Откройте или переключитесь на *Default.aspx* страницы.
2. Если страница отображается в **разработки** переход, просмотреть **источника** представления.
3. В верхней части страницы в `@Page` директив, изменение `Title` атрибут «Добро пожаловать», выделенные желтым ниже. 

    [!code-aspx[Main](ui_and_navigation/samples/sample1.aspx?highlight=1)]
4. Также на *Default.aspx* страницы, замените все значения по умолчанию содержимого содержится в `<asp:Content>` тег для отображения разметки как ниже. 

    [!code-aspx[Main](ui_and_navigation/samples/sample2.aspx)]
5. Сохранить *Default.aspx* страницу, выбрав **сохранить Default.aspx** из **файл** меню.

   Полученный в результате *Default.aspx* страница будет выглядеть следующим образом: 

[!code-aspx[Main](ui_and_navigation/samples/sample3.aspx)]

В примере, вы задали `Title` атрибут `@Page` директива. При отображении HTML в браузере, серверный код `<%: Page.Title %>` разрешается в содержимое, находящееся в `Title` атрибута.

Пример страницы включает в себя основные элементы, которые составляют веб-страницу ASP.NET. Страница содержит статический текст, возможно, в HTML-страницы, а также элементы, относящиеся к ASP.NET. Содержимое, находящееся в *Default.aspx* страницы будет интегрировано с содержимое главной страницы, который будет рассматриваться далее в этом руководстве.

### <a name="page-directive"></a>@Page Директива

Веб-формы ASP.NET обычно содержат директивы, которые позволяют пользователю указать страницу свойств и данные конфигурации для страницы. Директивы используются ASP.NET в качестве инструкции способ обработки страницы, но они не отображаются как часть разметки, который отправляется в браузер.

Наиболее часто используемые директива `@Page` директива, которая позволяет задавать множество параметров конфигурации для страницы, включая следующие:

1. Сервер, язык программирования для кода на странице, например C#.
2. Ли страница является страницей с использованием серверного кода непосредственно в страницу, называемый страницей в одном файле, или является ли страница с кодом в отдельный файл класса, который называется кода программной части.
3. Ли страница имеет связанную главную страницу и поэтому должны рассматриваться как страницы содержимого.
4. Параметры отладки и трассировки.

Если не включать `@Page` директиву страницы, или если эта директива не включает определенные настройки, параметр будет унаследован от *Web.config* файла конфигурации или из *Machine.config* файла конфигурации. *Machine.config* файл предоставляет дополнительные параметры конфигурации для всех приложений, работающих на компьютере.

> [!NOTE] 
> 
> *Machine.config* также предоставляет сведения обо всех параметрах конфигурации.

### <a name="web-server-controls"></a>Элемент управления

В большинстве приложений веб-форм ASP.NET добавляются элементы управления, которые позволяют пользователю взаимодействовать со страницей, таких как кнопки, текстовые поля, списки и т. д. Эти веб-сервера управления похожи на кнопки HTML и элементов ввода. Тем не менее они обрабатываются на сервере, что позволяет использовать код сервера для задания их свойств. Эти элементы управления также создают события, которые можно обработать в серверном коде.

Серверные элементы управления, используют специальный синтаксис, который распознается ASP.NET при запуске страницы. Имя тега для серверных элементов управления ASP.NET начинается с `asp:` префикс. Это позволяет распознавать и обрабатывать эти серверные элементы управления для приложений ASP.NET. Префикс может отличаться в том случае, если элемент управления не является частью платформы .NET Framework. В дополнение к `asp:` префикс, серверные элементы управления ASP.NET также содержать `runat="server"` атрибут и `ID` , можно использовать для ссылки на элемент управления в серверном коде.

При запуске страницы ASP.NET определяет серверных элементов управления и выполняет код, который связан с этими элементами управления. Многие элементы управления отображают HTML или другой разметки на странице, когда она появится в браузере.

### <a name="server-code"></a>Серверный код

Большинство приложений веб-форм ASP.NET включают код, выполняемый на сервере при обработке страницы. Как упоминалось выше, серверный код можно использовать для выполнения различных действий, таких как добавление данных к элементу управления ListView. ASP.NET поддерживает множество языков, которая работает на сервере, включая C#, Visual Basic, J# и другие.

ASP.NET поддерживает две модели написания кода сервера для веб-страницы. В модели одного файла, код для страницы находится в элементе скрипта, которого входит в открывающий тег `runat="server"` атрибута. Кроме того можно создать код для страницы в отдельный файл класса, который называется модели кода. Таким образом страницы веб-форм ASP.NET обычно содержит код не сервера. Вместо этого `@Page` директива включает сведения, которые связывают *.aspx* страницу с ее файлом с выделенным кодом.

`CodeBehind` Атрибуту, содержащемуся в `@Page` директива указывает имя отдельного файла класса и `Inherits` атрибут указывает имя класса в файл с выделенным кодом, который соответствует странице.

### <a name="updating-the-master-page"></a>Обновление главной страницы

В веб-форм ASP.NET главные страницы позволяют создавать устойчивый макет страниц в приложении. Одна главная страница определяет внешний вид и стандартное поведение, которое требуется для всех страниц (или группы страниц) в приложении. Затем можно создать отдельных страниц контента с содержимым, которое будет отображаться, как описано выше. Когда пользователи запрашивают страницы содержимого, ASP.NET объединяет их с главной страницей для получения выходных данных, который соединяет макет главной страницы с содержимым на странице содержимого.

Новому сайту необходимо один логотип для отображения на каждой странице. Чтобы добавить этот логотип, можно изменить HTML на главной странице.

1. В **обозревателе решений**найдите и откройте **Site.Master** страницы.
2. Если страница находится в **разработки** переход, просмотреть **источника** представления.
3. Обновите главную страницу с **изменения или добавления** разметки, выделены желтым цветом: 

    [!code-aspx[Main](ui_and_navigation/samples/sample4.aspx?highlight=9,49,76-81,87)]

Следующий код HTML будет отображаться изображение с именем *logo.jpg* из *образы* папку веб-приложения, который вы впоследствии добавите. При отображении страницы, использующей главной страницы в браузере отображается логотип. Если пользователь щелкает на логотип, пользователь будет перейти обратно к *Default.aspx* страницы. HTML-тег `<a>` служит оболочкой для серверного элемента управления изображения и позволяет изображение, включаемое как часть ссылки. `href` Атрибут для тега привязки определяет корневой "`~/`" как URL-адрес веб-сайта. По умолчанию *Default.aspx* страницы отображается в том случае, когда пользователь переходит в корень веб-сайта. **Изображение** `<asp:Image>` серверный элемент управления включает в себя добавление свойства, такие как `BorderStyle`, отображаются в виде HTML при отображении в браузере.

### <a name="master-pages"></a>Эталонные страницы

Главная страница представляет собой файл ASP.NET с расширением MASTER (например, *Site.Master*) с предопределенной компоновкой, который может содержать статический текст, HTML-элементов и серверных элементов управления. Главная страница идентифицируется по специальной `@Master` директива, которая заменяет `@Page` директива, которая используется для обычного *.aspx* страниц.

В дополнение к `@Master` директив, Главная страница также содержит все элементы верхнего уровня HTML для страницы, такие как `html`, `head`, и `form`. Например, на главной странице, добавленных выше, можно использовать HTML `table` для макета, `img` элемент для логотип компании, статический текст и серверных элементов управления для обработки распространенных членства для сайта. Любой код HTML и какие-либо элементы ASP.NET можно использовать как часть главной странице.

Помимо статический текст и элементы управления, которые будут отображаться на всех страницах, Главная страница также включает один или несколько **ContentPlaceHolder** элементов управления. Эти элементы управления местозаполнителя определить области, где можно заменить содержимое будет отображаться. В свою очередь, можно заменить содержимое определяется на страницах содержимого, такие как *Default.aspx*, с использованием **содержимого** серверного элемента управления.

#### <a name="adding-image-files"></a>Добавление файлов изображений

Изображение логотипа, указанный выше, а также изображения всех товаров, должны добавляться в веб-приложение, чтобы они были видны при проект отображается в браузере.

#### <a name="download-from-msdn-samples-site"></a>Скачайте с сайта с примерами MSDN:

[Приступая к работе с ASP.NET 4.5 и Visual Studio 2013 — Wingtip Toys](https://go.microsoft.com/fwlink/?LinkID=389434&clcid=0x409) (C#)

Загружаемый файл содержит ресурсы в *WingtipToys-активы* папки, которые используются для создания примера приложения.

1. Если вы еще не сделали, скачайте сжатый образцы файлов, с помощью приведенной выше ссылке на сайте образцов MSDN.
2. После скачивания откройте ZIP-файл и скопируйте содержимое в локальную папку на компьютере.
3. Найдите и откройте *WingtipToys-активы* папки.
4. С помощью перетаскивания, копирование *каталога* папки из своей локальной папки в корневой каталог проекта веб-приложения в **обозревателе решений** из Visual Studio. 

    ![Пользовательский Интерфейс и навигация — копирование файлов](ui_and_navigation/_static/image1.png)
5. Создайте новую папку с именем *образы* щелкните правой кнопкой мыши **WingtipToys** в проекте **обозревателе решений** и выбрав **добавить**  - &gt; **Новую папку**.
6. Копировать *logo.jpg* файла из *WingtipToys-активы* папку в **проводника** для *образы* папку веб-приложения в проекте **обозревателе решений** из Visual Studio.
7. Нажмите кнопку **Показать все файлы** параметр в верхней части **обозревателе решений** для обновления списка файлов в том случае, если вы не видите новые файлы.  
  
    **Обозреватель решений** теперь отображаются файлы обновленного проекта. 

    ![Пользовательский Интерфейс и навигация - обозревателе решений](ui_and_navigation/_static/image2.png)

### <a name="adding-pages"></a>Добавление страниц

Перед добавлением навигации в веб-приложение, сначала добавим два новых страниц, вы перейдете. Далее в этой серии руководств как отображать продуктов и сведения о продукте на эти новые страницы.

1. В **обозревателе решений**, щелкните правой кнопкой мыши **WingtipToys**, нажмите кнопку **добавить**, а затем нажмите кнопку **новый элемент**.   
 Откроется диалоговое окно **Добавление нового элемента**.
2. Выберите **Visual C#**  - &gt; **Web** группы шаблонов в левой части. Выберите **веб-форма с главной страницы** из середины списка и назовите его *ProductList.aspx*. 

    ![Пользовательский Интерфейс и навигация - новый диалоговое окно добавления элемента](ui_and_navigation/_static/image3.png)
3. Выберите **Site.Master** для присоединения главной страницы к только что созданный *.aspx* страницы. 

    ![Пользовательский Интерфейс и навигация - Выбор главной страницы](ui_and_navigation/_static/image4.png)
4. Добавьте дополнительную страницу с именем *ProductDetails.aspx* , выполните эти же действия.

### <a name="updating-bootstrap"></a>Обновление начальной загрузки

Используйте шаблоны проектов Visual Studio 2013 [Bootstrap](http://getbootstrap.com/), макет и темы framework, созданные компанией Twitter. Bootstrap использует CSS3 обеспечивает гибкий Дизайн, это означает, что макеты могли динамически приспосабливаться к размеры окна другой браузер. Функции платформы начальной загрузки темы позволяет легко вызывать изменения в приложения внешний вид. По умолчанию шаблон веб-приложения ASP.NET в Visual Studio 2013 включает начальной загрузки как пакет NuGet.

В этом руководстве мы изменим внешнего вида и поведения приложения Wingtip Toys, заменив Bootstrap CSS-файл.

1. В **обозревателе решений**откройте *содержимого* папки.
2. Щелкните правой кнопкой мыши *bootstrap.css* файл и переименуйте его в *bootstrap original.css*.
3. Переименуйте *bootstrap.min.css* для *bootstrap original.min.css*.
4. В **обозревателе решений**, щелкните правой кнопкой мыши *содержимого* папку и выберите **открыть папку в проводнике**.  
   Отображается файл в проводнике. Это расположение будет сохранен Скачанный начальной загрузки CSS-файлах.
5. В окне браузера перейдите к [ https://bootswatch.com/3/ ](https://bootswatch.com/3/).
6. Прокрутите окно браузера, пока не увидите Cerulean темы. 

    ![Пользовательский Интерфейс и навигация - Cerulean темы](ui_and_navigation/_static/image5.png)
7. Скачайте оба *bootstrap.css* файл и *bootstrap.min.css* файл *содержимого* папки. Укажите путь к папке content, который отображается в **проводнике** ранее открытое окно.
8. В **Visual Studio** в верхней части **обозревателе решений**выберите **Показать все файлы** параметр, чтобы отобразить новые файлы в папке содержимого. 

    ![Пользовательский Интерфейс и навигация - обозревателе решений](ui_and_navigation/_static/image6.png)

   Вы увидите два новых файла CSS в **содержимого** папки, но Обратите внимание, что возле каждого имени файла отображается серым. Это означает, что файл имеет еще не были добавлены в проект.
9. Щелкните правой кнопкой мыши *bootstrap.css* и *bootstrap.min.css* файла и выберите **включить в проект**.   
   При запуске приложения Wingtip Toys далее в этом учебнике будет отображаться новый компонент пользовательского интерфейса.

> [!NOTE] 
> 
> Шаблон веб-приложение ASP.NET использует *Bundle.config* файл в корне проекта для сохранения пути файлов Bootstrap CSS.

### <a name="modifying-the-default-navigation"></a>Изменение навигации по умолчанию

Перемещение по умолчанию для каждой страницы в приложении можно изменить путем изменения элемента списка неупорядоченный навигации, которые в *Site.Master* страницы.

1. В **обозревателе решений**, найдите и откройте *Site.Master* страницы.
2. Добавьте дополнительные навигационная ссылка выделена желтым в неупорядоченный список, показано ниже:   

    [!code-html[Main](ui_and_navigation/samples/sample5.html?highlight=5)]

Как показано в приведенном выше коде HTML, вы изменили каждой позиции строки `<li>` содержащий тег привязки `<a>` со ссылкой `href` атрибута. Каждый `href` указывает на страницу веб-приложения. В браузере, когда пользователь щелкает на одном из этих ссылок (такие как **продуктов**), они будут перейдите на страницу, содержащихся в `href` (такие как **ProductList.aspx**). В конце этого руководства будет выполняться приложение.

> [!NOTE] 
> 
> Тильда (`~`) символ используется для указания, что `href` путь начинается с корневой папке проекта.

### <a name="adding-a-data-control-to-display-navigation-data"></a>Добавление данных элемента управления для отображения данных навигации

Далее добавим элемент управления для отображения всех категорий из базы данных. Каждая категория будет выступать в качестве ссылку *ProductList.aspx* страницы. Когда пользователь выбирает соответствующую ссылку в обозревателе, они перейдите к странице "Продукты" и см. в разделе только продукты, связанные с выбранной категорией.

Вы будете использовать **ListView** элемента управления, чтобы отобразить все категории, содержащиеся в базе данных. Чтобы добавить **ListView** управления на главную страницу:

1. В *Site.Master* странице, добавьте выделенный ниже `<div>` элемент **после** `<div>` элемента, содержащего `id="TitleContent"` , добавленный ранее:  

    [!code-aspx[Main](ui_and_navigation/samples/sample6.aspx?highlight=7-21)]

Этот код выводит все категории из базы данных. **ListView** элемент управления отображает имя каждой категории как текст ссылки и ссылка на *ProductList.aspx* страницы со значением строки запроса, содержащим `ID` категории. Задав `ItemType` свойство в **ListView** управления, выражение привязки данных `Item` в `ItemTemplate` узел и элемент управления становится строго типизированными. Можно выбрать сведения о `Item` с помощью IntelliSense, например, указать `CategoryName`. Этот код содержится внутри контейнера `<%#: %>` , отмечает выражение привязки данных. Путем добавления (:) и заканчивая `<%#` префикс, результат выражения привязки данных является HTML-кодировке. Если результатом является HTML-кодирование, приложение лучше защищено от межузловых сценариев внедрения (XSS) и атаки путем внедрения кода HTML.

> [!NOTE] 
> 
> **Совет**
> 
> При добавлении кода, введя во время разработки, вы можете быть уверены, что допустимый член объекта обнаруженное так, как строго типизированные элементы управления данные для отображения доступных элементов, в зависимости от IntelliSense. IntelliSense предоставляет варианты кода, соответствующие контексту, при вводе кода, такие как свойства, методы и объекты.

На следующем шаге вы реализуете `GetCategories` метод для извлечения данных.

### <a name="linking-the-data-control-to-the-database"></a>Связывание элемента управления данных к базе данных

Прежде чем можно отобразить данные в элемент управления данными, вам необходимо связать элемент управления данными к базе данных. Чтобы сделать ссылку, можно изменить код программной части *Site.Master.cs* файла.

1. В **обозревателе решений**, щелкните правой кнопкой мыши *Site.Master* странице и нажмите кнопку **Просмотр кода**. *Site.Master.cs* файл открыт в редакторе.
2. В начале *Site.Master.cs* добавьте две дополнительные пространства имен, чтобы все включаемые пространства имен будет выглядеть следующим образом:  

    [!code-csharp[Main](ui_and_navigation/samples/sample7.cs?highlight=8-9)]
3. Добавьте выделенный `GetCategories` метод после `Page_Load` обработчик событий следующим образом:  

    [!code-csharp[Main](ui_and_navigation/samples/sample8.cs?highlight=6-11)]

Приведенный выше код выполняется при загрузке любой страницы, использующей главной страницы в браузере. `ListView` Управления (именованные «categoryList»), который был добавлен ранее в этом учебнике используется привязка модели для выбора данных. В разметке `ListView` элемента управления значение элемента управления `SelectMethod` свойства `GetCategories` метод, показанный выше. `ListView` Контролирующие вызовы, `GetCategories` метод в соответствующее время существования страницы цикла и автоматически связывает возвращаемых данных. Дополнительные сведения о привязке данных в следующем учебном курсе.

### <a name="running-the-application-and-creating-the-database"></a>Запуск приложения и создание базы данных

Ранее в этой серии руководств вы создали класс инициализатора (с именем «ProductDatabaseInitializer») и указан этот класс в *global.asax.cs* файл. Платформа Entity Framework создает базу данных, когда приложение запускается первый раз, так как `Application_Start` метода, содержащегося в *global.asax.cs* файл будет вызывать класс инициализатора. Инициализатор класса будет использовать классы моделей (`Category` и `Product`), добавленные ранее в этой серии руководств, чтобы создать базу данных.

1. В **обозревателе решений**, щелкните правой кнопкой мыши *Default.aspx* и укажите **задать в качестве начальной страницы**.
2. В Visual Studio нажмите клавишу **F5**.   
 Займет немного времени, чтобы настроить все, что во время этого первого запуска.   
    ![Пользовательский Интерфейс и навигация - обозревателя Windows](ui_and_navigation/_static/image7.png)  
 При запуске приложения, приложение будет скомпилирован и базы данных с именем *wingtiptoys.mdf* будет создан в *приложения\_данных* папки. В браузере вы увидите категории меню переходов. Это меню был создан путем извлечения категорий из базы данных. В следующем руководстве вы реализуете навигации.
3. Закройте браузер, чтобы остановить выполняющееся приложение.

### <a name="reviewing-the-database"></a>Просмотр базы данных

Откройте *Web.config* файл и просмотрите раздел со строкой соединения. Можно заметить, что `AttachDbFilename` значение в строке подключения указывает на `DataDirectory` для проекта веб-приложения. Значение `|DataDirectory|` — это зарезервированное значение, представляющее *приложения\_данных* в папку проекта. Эта папка находится на котором размещена база данных, который был создан из классов сущностей.

[!code-xml[Main](ui_and_navigation/samples/sample9.xml)]

> [!NOTE] 
> 
> Если *приложения\_данных* папка не отображается или если она пуста, выберите **обновить** значок и затем **Показать все файлы** значок в верхней части **Обозревателе решений** окна. Развернув ширину **обозревателе решений** windows может потребоваться отображение всех доступных значков.

Теперь вы можете проверить данные, содержащиеся в *wingtiptoys.mdf* файл базы данных с помощью **обозревателя серверов** окна.

1. Разверните *приложения\_данных* папки. Если *приложения\_данных* папка не отображается, см. в примечании выше.
2. Если *wingtiptoys.mdf* файл базы данных не отображается, выберите **обновить** значок и затем **Показать все файлы** значок в верхней части **обозревателе решений**  окно.
3. Щелкните правой кнопкой мыши *wingtiptoys.mdf* файлу базы данных и выберите **откройте**.  
    **Обозреватель серверов** отображается. 

    ![Пользовательский Интерфейс и навигация - обозревателя серверов](ui_and_navigation/_static/image8.png)
4. Разверните *таблиц* папки.
5. Щелкните правой кнопкой мыши **продуктов**таблицы и выберите **Показать таблицу данных**.  
 **Продуктов** отображается таблица. 

    ![Пользовательский Интерфейс и навигация - таблица товаров](ui_and_navigation/_static/image9.png)
6. Это представление позволяет просмотреть и изменить данные в **продуктов** таблицы вручную.
7. Закрыть **продуктов** окно таблицы.
8. В **обозревателя серверов**, щелкните правой кнопкой мыши **продуктов** таблицы еще раз и выберите **Открыть определение таблицы**.  
 Данные разработки для **продуктов** отображается таблица. 

    ![Пользовательский Интерфейс и навигация - разработки продуктов](ui_and_navigation/_static/image10.png)
9. В **T-SQL** вкладку, вы увидите инструкцию SQL DDL, который использовался для создания таблицы. Можно также использовать пользовательский Интерфейс на **разработки** вкладку для изменения схемы.
10. В **обозревателя серверов**, щелкните правой кнопкой мыши **WingtipToys** базы данных и выберите **закрыть соединение**.   
 Путем отсоединения базы данных из Visual Studio, схему базы данных будут иметь возможность изменить позднее в этой серии руководств.
11. Вернитесь к **обозревателе решений**, выбрав **обозревателе решений** вкладку в нижней части **обозревателя серверов** окна.

## <a name="summary"></a>Сводка

В этом руководстве из серии вы добавили некоторые основной пользовательский Интерфейс, графики, страниц и переходов. Кроме того вы выполнили веб-приложения, в которой создана база данных от классов данных, добавленные в предыдущем учебном курсе. Можно также просмотреть содержимое *продуктов* таблицы в базе данных, непосредственно просматривая базы данных. В следующем учебном курсе как отображать элементы данных и в базе данных.

## <a name="additional-resources"></a>Дополнительные ресурсы

[Введение в программирование веб-страниц ASP.NET](https://msdn.microsoft.com/library/ms178125.aspx)   
[Обзор элементов управления веб-сервера ASP.NET](https://msdn.microsoft.com/library/zsyt68f1.aspx)   
[Учебник по CSS](http://www.w3schools.com/css/default.asp)

> [!div class="step-by-step"]
> [Назад](create_the_data_access_layer.md)
> [Вперед](display_data_items_and_details.md)
