---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: Знакомство с веб-страниц ASP.NET — обновление баз данных | Документация Майкрософт
author: Rick-Anderson
description: Этом руководстве показано, как обновить запись (изменение) существующей базы данных, при использовании веб-страниц ASP.NET (Razor). Предполагается, что вы выполнили ряд th...
ms.author: riande
ms.date: 01/02/2018
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: 206d7e209857aceb3eb92c2405bb73f7ff7dbaeb
ms.sourcegitcommit: 24b1f6decbb17bb22a45166e5fdb0845c65af498
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/01/2019
ms.locfileid: "57048111"
---
<a name="introducing-aspnet-web-pages---updating-database-data"></a>Знакомство с веб-страниц ASP.NET — обновление баз данных
====================
по [Tom FitzMacken](https://github.com/tfitzmac)

> Этом руководстве показано, как обновить запись (изменение) существующей базы данных, при использовании веб-страниц ASP.NET (Razor). Предполагается, вы выполнили рядов через [ввод данных с помощью форм с помощью ASP.NET Web Pages](entering-data.md).
> 
> Вы узнаете, как:
> 
> - Как выбрать отдельную запись в `WebGrid` вспомогательный.
> - Как выполнять чтение одной записи из базы данных.
> - Инструкции по предварительной загрузке формы со значениями из записи в базе данных.
> - Как обновить существующую запись в базу данных.
> - Как сохранить сведения на странице без его отображения.
> - Как использовать скрытое поле для хранения информации.
>   
> 
> Функции и технологии:
> 
> - `WebGrid` Вспомогательный.
> - SQL `Update` команды.
> - метод `Database.Execute` ;
> - Скрытые поля (`<input type="hidden">`).


## <a name="what-youll-build"></a>Что вы создадите

В предыдущем руководстве вы узнали, как добавить запись в базу данных. Здесь вы узнаете, как отобразить запись для редактирования. В *фильмы* обновим страницу, `WebGrid` вспомогательный, потому что он отображается как **изменить** ссылку рядом с каждой фильма:

![WebGrid отображения, включая ссылку «Изменить» для каждого фильма](updating-data/_static/image1.png)

При нажатии кнопки **изменить** ссылку, откроется на другую страницу, где сведения о фильме уже находится в форме:

![Страница «Изменение» фильма, показывающий фильма для редактирования](updating-data/_static/image2.png)

Можно изменить любое из значений. При отправке изменений кода на данной странице обновляет базу данных и вернуться к списку фильмов.

Эта часть процесса работы практически аналогичен *AddMovie.cshtml* страницу, созданную в предыдущем учебном курсе, поэтому большая часть этого руководства будут вам знакомы.

Существует несколько способов, которые можно реализовать способ редактирования отдельных фильма. Показанный подход был выбран, так как это легко реализовать и легкими для понимания.

## <a name="adding-an-edit-link-to-the-movie-listing"></a>Добавление ссылка на редактирование фильма

Чтобы начать работу, мы обновим *фильмы* странице таким образом, что каждый фильм, со списком также **изменить** ссылку.

Откройте *Movies.cshtml* файла.

В основной области страницы, измените `WebGrid` разметки, добавив столбец. Ниже приведен измененной разметкой.

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

Новый столбец является такой:

[!code-html[Main](updating-data/samples/sample2.html)]

Точки этого столбца — Показать ссылку (`<a>` элемент), текст которых говорит: «Изменить». Нам нужно будет создать ссылку, которая выглядит следующим образом, при запуске страницы с `id` значение отличается для каждого фильма:

[!code-css[Main](updating-data/samples/sample3.css)]

Эта ссылка будет вызывать страницу с именем *EditMovie*, и он будет передать строку запроса `?id=7` на эту страницу.

Синтаксис для нового столбца может выглядеть немного сложной, но только потому, что он объединяет несколько элементов. Каждый отдельный элемент прост. Если вы сосредоточиться на только что `<a>` элемент, вы увидите эту разметку:

[!code-html[Main](updating-data/samples/sample4.html)]

Некоторые общие сведения о работе сетки: Сетка отображает строки, по одной для каждой записи базы данных и отображает столбцы для каждого поля в записи в базе данных. Во время создания каждой строки сетки `item` объект содержит запись базы данных (элемент) для этой строки. Такой подход позволяет вам в коде для получения данных для этой строки. Это, показанный здесь: выражение `item.ID` получает значение идентификатора текущего элемента базы данных. Вы может получить какие-либо значения базы данных (title, Жанр или год) так же, как с помощью `item.Title`, `item.Genre`, или `item.Year`.

Выражение `"~/EditMovie?id=@item.ID` объединяет жестко частью целевой URL-адрес (`~/EditMovie?id=`) с этим идентификатором динамически производных. (Вы видели `~` оператор в предыдущем учебном курсе; это оператор ASP.NET, который представляет текущий корневой веб-сайта.)

Результатом является то, что эта часть разметки в столбце просто создает примерно следующую разметку во время выполнения:

[!code-xml[Main](updating-data/samples/sample5.xml)]

Естественно, фактическое значение `id` будет отличаться для каждой строки.

## <a name="creating-a-custom-display-for-a-grid-column"></a>Создание пользовательского представления для столбец сетки

Вернемся к столбце сетки. Три столбца необходимо первоначально было в сетке отображаются только значения данных (title, Жанр и год). Вы указали этого отображения, передав имя столбца базы данных &mdash; к примеру, `grid.Column("Title")`.

Этот новый **изменить** столбец ссылки отличается. Вместо указания имени столбца, передается `format` параметра. Этот параметр позволяет определить разметки, `WebGrid` вспомогательный объект будет отображать вместе с `item` значение для отображения данных в столбце зеленый или полужирным шрифтом, или в любом формате, что вы хотите. Например если требуется заголовок, который отображается полужирным шрифтом, можно создать столбец, как в следующем примере:

[!code-html[Main](updating-data/samples/sample6.html)]

(Различных `@` символы, отображаемые в `format` свойство пометить перехода между разметки и значение кода.)

Узнав о `format` свойство, стало проще понять, как новый **изменить** устроено столбец ссылки:

[!code-html[Main](updating-data/samples/sample7.html)]

Столбец состоит *только* разметки, который выполняет визуализацию ссылки, а также некоторые сведения о (идентификатор), извлекается из записи базы данных для строки.

> [!TIP]
> 
> **Именованные параметры и позиционные параметры для метода**
> 
> Много раз, когда вы именем метода и передается ему параметры, вы просто присоединились значения параметров, разделенных запятыми. Вот несколько примеров:
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> Мы забыли упомянуть проблема, когда впервые увидел этот код, но в каждом случае при передаче параметров в методы в определенном порядке &mdash; , порядок, в котором определены параметры в этом методе. Для `db.Execute` и `Validation.RequireFields`, при совместном порядок переданные значения, получит сообщение об ошибке при запуске страницы или по крайней мере непредвиденных результатов. Очевидно он должен знать порядок передачи параметров в. (В WebMatrix, IntelliSense может помочь вам узнать выясните имя, тип и порядок параметров.)
> 
> В качестве альтернативы Передача значений в порядке, можно использовать *именованные параметры*. (Передача параметров в порядке известен как с помощью *позиционные параметры*.) Для именованных параметров явно не включить имя параметра при передаче его значение. Вы использовали именованные параметры уже несколько раз в этих учебниках. Пример:
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> и
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> Именованные параметры удобны для нескольких ситуаций, особенно в том случае, если метод принимает много параметров. Одна —, когда требуется передать только один или два параметра, но не являются значения, которые требуется передать из первой позиции в списке параметров. Другая ситуация — Если вы хотите сделать код более понятным, передав параметры в порядке, который наиболее целесообразно для вас.
> 
> Очевидно, что для использования именованных параметров, необходимо знать имена параметров. WebMatrix IntelliSense можно *Показать* имена, но его нельзя заполнить в настоящее время их для вас.


## <a name="creating-the-edit-page"></a>Создание страницы "Edit"

Теперь вы можете создать *EditMovie* страницы. Когда пользователь нажимает кнопку **изменить** ссылку, они кончится на этой странице.

Создание страницы с именем *EditMovie.cshtml* и замените в файле, используя следующую разметку:

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

Это разметка и код аналогичен имеющихся *AddMovie* страницы. Имеется небольшое различие в текст для кнопки "Отправить". Как и в *AddMovie* странице, имеется `Html.ValidationSummary` вызов, который будет отображаться ошибок проверки, если таковые имеются. Сейчас мы уходите вызовы к `Validation.Message`, так как ошибки будут отображаться в сводку проверки. Как отмечалось в предыдущем учебном курсе, можно использовать в различных сочетаниях сводка проверки и сообщения об отдельных ошибках.

Снова Обратите внимание, `method` атрибут `<form>` элементу присваивается `post`. Как и в *AddMovie.cshtml* странице эта страница вносит изменения в базе данных. Таким образом, следует выполнить эту форму `POST` операции. (Дополнительные сведения о различиях между `GET` и `POST` операций, см. в разделе [GET, POST и HTTP Verb безопасности](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) боковой панели в этом руководстве на HTML-формы.)

Как было показано в предыдущем учебном курсе `value` атрибуты из текстовых полей устанавливаются с помощью кода Razor для предварительной загрузки их. На этот раз, вы используете переменные, такие как `title` и `genre` для выполнения этой задачи, а не `Request.Form["title"]`:

`<input type="text" name="title" value="@title" />`

Как ранее, эта разметка будет предварительно загрузить значения текстового поля со значениями фильма. Вы увидите в момент, почему это удобно для использования переменных в этот раз вместо использования `Request` объекта.

Имеется также `<input type="hidden">` элемент на этой странице. Этот элемент сохраняет идентификатор фильма без станет видимым на странице. Идентификатор изначально передается на страницу с помощью значения строки запроса (`?id=7` или аналогичным объектом в URL-адрес). Поместив значение идентификатора в скрытое поле, можно убедитесь, что он доступен при отправке формы, даже если отсутствует доступ к исходный URL-адрес страницы была вызвана со.

В отличие от *AddMovie* странице код *EditMovie* страница содержит две функции distinct. Первая функция, что при отображении страницы в первый раз (и *только* затем), код получает идентификатор фильма из строки запроса. Код затем использует идентификатор для чтения соответствующего фильма из базы данных и отображения (предварительно) его в текстовых полях.

Вторая функция — это, когда пользователь щелкает **отправить изменения** кнопки, код должен считывать значения из текстовых полей и проверить их работоспособность. Код также должен обновить элемент базы данных с новыми значениями. Этот метод аналогичен добавлению записи, как видно из *AddMovie*.

## <a name="adding-code-to-read-a-single-movie"></a>Добавление кода для чтения одного фильма

Для выполнения первой функции, добавьте следующий код в верхней части страницы:

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

Большая часть этого кода находится внутри блока, который начинается `if(!IsPost)`. `!` Оператор «not» означает означает выражение *Если этот запрос не такие данные post*, который является косвенным способом о том *Если этот запрос выполняется впервые, эта страница будет выполнено*. Как отмечалось ранее, этот код следует запускать *только* при первом запуске страницы. Если не заключить код в `if(!IsPost)`, он будет запускаться при каждом страницы вызывается, является ли первый раз или в ответ на нажатие кнопки щелкните.

Обратите внимание, что код содержит `else` заблокировать это время. Как уже говорилось, когда мы представили `if` блоки, иногда требуется выполнить альтернативный код, если вы тестируете условие не так. Это в данном случае. Если условие выполняется успешно (то есть, если идентификатор, переданный на страницу в норме), чтением строки из базы данных. Тем не менее, если условие не проходит, `else` блок выполняется, и код задает сообщение об ошибке.

## <a name="validating-a-value-passed-to-the-page"></a>Проверка значения, переданного на страницу

Код использует `Request.QueryString["id"]` для получения идентификатора, который передается на страницу. Код гарантирует, что значение действительно был передан для идентификатора. Если значение не было передано, код задает ошибку проверки.

Этот код показывает другой способ проверить информацию. В предыдущем руководстве, вы работали с `Validation` вспомогательный. Вы зарегистрировали поля для проверки и ASP.NET, автоматически была проверки и отображения ошибок с помощью `Html.ValidationMessage` и `Html.ValidationSummary`. Таким образом тем не менее, вы будете проверка не совсем вводимых пользователем данных. Вместо этого проверка значения, который был передан на страницу из другого источника. `Validation` Вспомогательный не сделает этого за вас.

Таким образом, можно проверить значение самостоятельно, тестируя его с `if(!Request.QueryString["ID"].IsEmpty()`). Если имеется проблема, ее можно отобразить с помощью `Html.ValidationSummary`, как это было сделано `Validation` вспомогательный. Чтобы сделать это, вызовите `Validation.AddFormError` и передать ему сообщение для отображения. `Validation.AddFormError` — это встроенный метод, который позволяет определять пользовательские сообщения, которые связаны с проверки системы, в которой вы уже знакомы. (Далее в этом руководстве мы поговорим о том, как сделать этот процесс проверки немного более надежным.)

Убедитесь, что есть идентификатор фильма, код считывает базы данных, ищете только элементы, отдельной базы данных. (Вы, вероятно, заметили общий шаблон для операций базы данных: открыть базу данных, определите инструкцию SQL и выполните инструкцию.) В данном случае SQL `Select` инструкция включает `WHERE ID = @0`. Так как этот идентификатор является уникальным, могут быть возвращены только одной записи.

Запрос выполняется с помощью `db.QuerySingle` (не `db.Query`, которая использовалась для фильма), и код помещает результат в `row` переменной. Имя `row` является произвольным; можно назвать переменные ничего вам нравится. Переменных, инициализированных в верхней затем заполняются сведений о фильмах, чтобы отображались эти значения в текстовых полях.

## <a name="testing-the-edit-page-so-far"></a>Тестирование страницы "Edit" (пока)

Если вы хотите протестировать страницу, запустите *фильмы* теперь и нажмите кнопку **изменить** ссылку рядом с любой фильм. Вы увидите *EditMovie* указан страницу с подробными сведениями для фильмов, который вы выбрали:

![Страница «Изменение» фильма, показывающий фильма для редактирования](updating-data/_static/image3.png)

Обратите внимание на то, что URL-адрес страницы включает в себя нечто вроде `?id=10` (или иному). Итак, тестирования **изменить** ссылок в *фильма* странице работы, что страницы считывает идентификатор из строки запроса, и что базы данных выполните запрос на получение одного фильма запись работает.

Вы можете изменить сведения о фильме, но ничего не происходит при нажатии кнопки **отправить изменения**.

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a>Добавление кода для обновления фильма с изменениями пользователя

В *EditMovie.cshtml* файл, чтобы реализовать вторую функцию (сохранения изменений), добавьте следующий код внутри закрывающую скобку `@` блока. (Если вы не уверены, где поместить код, можно взглянуть на [полный листинг кода для страницы изменить фильме](#Complete_Page_Listing_for_EditMovie) , расположенную в конце этого руководства.)

[!code-csharp[Main](updating-data/samples/sample12.cs)]

Опять же, это разметка и код аналогичен коду в *AddMovie*. Код находится в `if(IsPost)` block, так как этот код выполняется только в том случае, когда пользователь щелкает **отправить изменения** кнопку &mdash; таким образом, когда (и только тогда, когда) после отправки формы. В этом случае вы не используете такой тест `if(IsPost && Validation.IsValid())`— то есть, не объединенных оба теста с помощью и. На этой странице вы сначала определить, является отправка формы (`if(IsPost)`) и только затем зарегистрируйте поля для проверки. Затем можно проверить результаты проверки (`if(Validation.IsValid()`). Процесс выглядит немного отличается от таковой в *AddMovie.cshtml* страницы, но результат одинаков.

Получить значения из текстовых полей с помощью `Request.Form["title"]` и аналогичный код для других `<input>` элементов. Обратите внимание на то, что на этот раз код получает идентификатор фильма из скрытого поля (`<input type="hidden">`). При запуске страницы в первый раз код есть идентификатор из строки запроса. Получить значение из скрытого поля, чтобы убедиться в том, что вы получаете идентификатор фильма, который изначально был отображен, в случае, если строка запроса каким-либо образом была изменена с тех пор.

На самом деле важное отличие между *AddMovie* код и этот код является в этом коде, с помощью SQL `Update` инструкции, а не `Insert Into` инструкции. В следующем примере показано синтаксис SQL `Update` инструкции:

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

Можно указать какие-либо столбцы в любом порядке, и вы не обязательно обновлять каждый столбец во время `Update` операции. (Идентификатор, невозможно обновить, поскольку это фактически позволит сократить запись новой записи и недопустим для `Update` операции.)

> [!NOTE] 
> 
> **Важные** `Where` предложение с Идентификатором очень важно, потому что это, как базы данных знает, какие базы данных записей, вы хотите обновить. Если вы остановились `Where` предложение, обновит базу данных *каждые* записей в базе данных. В большинстве случаев это было бы аварии.


В коде значения, подлежащие обновлению передаются в инструкцию SQL с помощью заполнителей. Для повторения, что мы указали перед: по соображениям безопасности *только* использовать заполнители для передачи значений в инструкцию SQL.

После чего используется `db.Execute` для запуска `Update` инструкции, перенаправляется обратно на страницу, где можно просмотреть изменения.

> [!TIP] 
> 
> **Операторы разных SQL, различные методы**
> 
> Вы могли заметить, использовать немного разные методы для выполнения другой инструкции SQL. Для запуска `Select` запрос, которые возвращает несколько записей, использовать `Query` метод. Для запуска `Select` запрос, который вы знаете, будет возвращать только один элемент базы данных, использовать `QuerySingle` метод. Для выполнения команд, вносить изменения, но которые не возвращают элементы базы данных, используйте `Execute` метод.
> 
> У вас есть для разных методов, так как каждая из них возвращает разные результаты, как вы уже видели в разницу между `Query` и `QuerySingle`. ( `Execute` Метод фактически возвращает значение также &mdash; , количество строк базы данных, обработанных с помощью команды &mdash; , но вы игнорируется, пока.)
> 
> Само собой `Query` метод может вернуть только одну строку базы данных. Однако ASP.NET всегда обрабатывает результаты `Query` метода в виде коллекции. Даже если метод возвращает только одну строку, необходимо извлечь, одну строку из коллекции. Таким образом, в ситуациях, где вы *знать* вы получите только одну строку, это немного удобнее использовать `QuerySingle`.
> 
> Существует несколько других методов, выполняющих определенные типы операций базы данных. Вы найдете список методов базы данных в [краткий справочник по API веб-страниц ASP.NET](../../api-reference/asp-net-web-pages-api-reference.md#Data).


## <a name="making-validation-for-the-id-more-robust"></a>Что делает проверки для идентификатора более надежные

При первом запуске страницы, вы получите идентификатор фильма из строки запроса таким образом, вы можете получить этого фильма из базы данных. Вы проверили, что фактически было выполнять поиск, значение, которое делалось с помощью следующего кода:

[!code-csharp[Main](updating-data/samples/sample13.cs)]

Вы использовали этот код, чтобы убедиться в том, если пользователь получает *EditMovies* страницы без его предварительного выбора фильм в *фильмы* страницы, на странице будет отображаться понятное сообщение об ошибке. (В противном случае пользователи будут видеть ошибку, которая, вероятно, лишь бы путают их).

Тем не менее такая проверка не самый надежный вариант. Страницы также может вызываться с этими ошибками:

- Идентификатор не является числом. Например, страницы может быть вызван с помощью URL-адресу вида `http://localhost:nnnnn/EditMovie?id=abc`.
- Идентификатор должен быть числом, но он ссылается на фильм, не существует (например, `http://localhost:nnnnn/EditMovie?id=100934`).

Если вы хотите просмотреть ошибки, возникающие в результате эти URL-адреса, выполните *фильмы* страницы. Выберите фильм для редактирования и измените URL-адрес *EditMovie* странице на URL-адрес, который содержит буквенным, идентификатор или идентификатор фильма не существует.

Что необходимо сделать? Первый способ исправления заключается в убедитесь в том, что не только код передается на страницу, но что идентификатор является целое число. Изменить код для `!IsPost` тест, чтобы выглядеть как в следующем примере:

[!code-csharp[Main](updating-data/samples/sample14.cs)]

Вы добавили второе условие, которое `IsEmpty` теста, связанного с `&&` (логическое и):

[!code-csharp[Main](updating-data/samples/sample15.cs)]

Возможно, вы помните из [введение в программирование веб-страниц ASP.NET](../introducing-razor-syntax-c.md) учебник, в котором методы, такие как `AsBool` `AsInt` преобразование строки символов в другой тип данных. `IsInt` Метод (и другие, например `IsBool` и `IsDateTime`) похожи. Тем не менее, они проверяют только ли вы *можно* преобразовать строку, не выполняя преобразование. Так что здесь можно по сути, говорит *Если значение строки запроса может быть преобразовано в целое число...* .

Другие потенциальные проблемы ищет фильм, не существует. Код для получения фильм выглядит следующим образом:

[!code-csharp[Main](updating-data/samples/sample16.cs)]

Если передать `movieId` значение `QuerySingle` метод, который не соответствует фактический фильм, ничего не возвращается и операторы, следующие (например, `title=row.Title`) привести к ошибкам.

Снова будет легко исправить. Если `db.QuerySingle` метод не возвращает результатов, `row` переменной будет иметь значение null. Чтобы можно было вернуть ли `row` переменная имеет значение null, прежде чем пытаться получить значения из него. Следующий код добавляет `if` блок инструкций, которые получают значения из `row` объекта:

[!code-csharp[Main](updating-data/samples/sample17.cs)]

Эти две дополнительные проверочные тесты страницы становится более пуленепробиваема. Полный код для `!IsPost` ветви теперь выглядит следующим образом:

[!code-csharp[Main](updating-data/samples/sample18.cs)]

Мы будем Обратите внимание, еще раз что эту задачу рекомендуется использовать для `else` блока. Если не передать тесты, `else` блоки задать сообщения об ошибках.

## <a name="adding-a-link-to-return-to-the-movies-page"></a>Добавив ссылку, чтобы вернуться на страницу Movies

Является окончательной и полезные сведения для добавления ссылки к *фильмы* страницы. В обычный поток событий, пользователи начнут на *фильмы* и нажмите кнопку **изменить** ссылку. Это приводит к *EditMovie* страницу, где редактирование фильма и нажмите кнопку. После кода обрабатывает это изменение, он перенаправляет *фильмы* страницы.

Но:

- Пользователь может принять решение не вносите никаких изменений.
- Пользователь может стали на эту страницу без предварительного нажатия кнопки **изменить** ссылку в *фильмы* страницы.

В любом случае необходимо упростить процесс их, чтобы вернуться к основной список. Это легко исправить &mdash; добавьте следующую разметку после закрытия `</form>` тег в разметке:

[!code-html[Main](updating-data/samples/sample19.html)]

Эта разметка использует тот же синтаксис для `<a>` элемент, который вы видели в другом месте. URL-адрес включает в себя `~` означает «root веб-сайта».

## <a name="testing-the-movie-update-process"></a>Тестирование процесса обновления фильма

Теперь можно протестировать. Запустите *фильмы* и нажмите кнопку **изменить** рядом с фильм. Когда *EditMovie* появится страница, внести изменения в фильма и нажмите кнопку **отправить изменения**. Когда появится список фильмов, убедитесь, что изменения отображаются.

Чтобы убедиться в том, что проверка работает, нажмите кнопку **изменить** для другой фильма. При получении к *EditMovie* странице снимите **жанр** поле (или **год** или обоих полей) и пытается отправить изменения. Появится сообщение об ошибке, как и следовало ожидать.

![Страница «Изменение» фильма, отображение ошибок проверки](updating-data/_static/image4.png)

Нажмите кнопку **вернуться к фильма** ссылку, чтобы отменить внесенные изменения и вернуться к *фильмы* страницы.

## <a name="coming-up-next"></a>В ближайшее время

В следующем учебном курсе будет показано, как удалить запись фильма.

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a>Полный пример для страницы фильма (обновление с помощью ссылки на изменение)

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a>Полный пример страницы для страницы "Edit" фильма

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a>Дополнительные ресурсы

- [Введение в программирование веб-ASP.NET с помощью синтаксиса Razor](../../getting-started/introducing-razor-syntax-c.md)
- [Инструкции SQL UPDATE](http://www.w3schools.com/sql/sql_update.asp) на сайте W3Schools

> [!div class="step-by-step"]
> [Назад](entering-data.md)
> [Вперед](deleting-data.md)
