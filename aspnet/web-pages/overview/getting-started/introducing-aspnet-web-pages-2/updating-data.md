---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: Введение в веб-страницы ASP.NET обновление данных базы данных | Документация Майкрософт
author: Rick-Anderson
description: В этом руководстве показано, как обновить (изменить) существующую запись базы данных при использовании веб-страницы ASP.NET (Razor). Предполагается, что вы выполнили ряд...
ms.author: riande
ms.date: 01/02/2018
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: 8f8bcfb7d9d2416a2699776cadbdaae8e12415ba
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/06/2020
ms.locfileid: "78463494"
---
# <a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="38292-104">Введение в веб-страницы ASP.NET обновление данных базы данных</span><span class="sxs-lookup"><span data-stu-id="38292-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>

<span data-ttu-id="38292-105">от [Tom фитзмаккен](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="38292-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="38292-106">В этом руководстве показано, как обновить (изменить) существующую запись базы данных при использовании веб-страницы ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="38292-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="38292-107">Предполагается, что вы выполнили цикл по [вводу данных с помощью форм, Использующих веб-страницы ASP.NET](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="38292-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="38292-108">Из этого руководства вы узнаете, как выполнять такие задачи:</span><span class="sxs-lookup"><span data-stu-id="38292-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="38292-109">Выбор отдельной записи в вспомогательном приложении `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="38292-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="38292-110">Чтение одной записи из базы данных.</span><span class="sxs-lookup"><span data-stu-id="38292-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="38292-111">Предварительная загрузка формы со значениями из записи базы данных.</span><span class="sxs-lookup"><span data-stu-id="38292-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="38292-112">Обновление существующей записи в базе данных.</span><span class="sxs-lookup"><span data-stu-id="38292-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="38292-113">Хранение информации на странице без ее отображения.</span><span class="sxs-lookup"><span data-stu-id="38292-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="38292-114">Использование скрытого поля для хранения информации.</span><span class="sxs-lookup"><span data-stu-id="38292-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="38292-115">Обсуждаемые функции и технологии:</span><span class="sxs-lookup"><span data-stu-id="38292-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="38292-116">Вспомогательный метод `WebGrid`.</span><span class="sxs-lookup"><span data-stu-id="38292-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="38292-117">Команда SQL `Update`.</span><span class="sxs-lookup"><span data-stu-id="38292-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="38292-118">метод `Database.Execute` ;</span><span class="sxs-lookup"><span data-stu-id="38292-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="38292-119">Скрытые поля (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="38292-119">Hidden fields (`<input type="hidden">`).</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="38292-120">Что вы создадите</span><span class="sxs-lookup"><span data-stu-id="38292-120">What You'll Build</span></span>

<span data-ttu-id="38292-121">В предыдущем руководстве вы узнали, как добавить запись в базу данных.</span><span class="sxs-lookup"><span data-stu-id="38292-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="38292-122">Здесь вы узнаете, как отобразить запись для редактирования.</span><span class="sxs-lookup"><span data-stu-id="38292-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="38292-123">На странице *фильмов* вы обновите вспомогательную функцию `WebGrid`, чтобы она отображала ссылку **Edit (изменить** ) рядом с каждым фильмом:</span><span class="sxs-lookup"><span data-stu-id="38292-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![Отображение сетки, включая ссылку "Изменить" для каждого фильма](updating-data/_static/image1.png)

<span data-ttu-id="38292-125">Щелкнув ссылку **изменить** , вы переведете на другую страницу, где сведения о фильме уже находятся в форме:</span><span class="sxs-lookup"><span data-stu-id="38292-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Изменение страницы фильма с фильмом для изменения](updating-data/_static/image2.png)

<span data-ttu-id="38292-127">Можно изменить любое из значений.</span><span class="sxs-lookup"><span data-stu-id="38292-127">You can change any of the values.</span></span> <span data-ttu-id="38292-128">При отправке изменений код на странице обновляет базу данных и переводит вас назад в список фильмов.</span><span class="sxs-lookup"><span data-stu-id="38292-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="38292-129">Эта часть процесса работает почти так же, как страница *аддмовие. cshtml* , созданная в предыдущем учебном курсе, поэтому многие из этих учебников будут знакомы.</span><span class="sxs-lookup"><span data-stu-id="38292-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="38292-130">Существует несколько способов реализовать способ изменения отдельного фильма.</span><span class="sxs-lookup"><span data-stu-id="38292-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="38292-131">Показанный подход был выбран, так как его легко реализовать и легко понять.</span><span class="sxs-lookup"><span data-stu-id="38292-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="38292-132">Добавление ссылки для редактирования в список фильмов</span><span class="sxs-lookup"><span data-stu-id="38292-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="38292-133">Для начала обновите страницу *фильмов* , чтобы в каждом списке фильмов также была ссылка для **редактирования** .</span><span class="sxs-lookup"><span data-stu-id="38292-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="38292-134">Откройте файл *movies. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="38292-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="38292-135">В тексте страницы измените разметку `WebGrid`, добавив столбец.</span><span class="sxs-lookup"><span data-stu-id="38292-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="38292-136">Измененная разметка:</span><span class="sxs-lookup"><span data-stu-id="38292-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="38292-137">Новый столбец является следующим:</span><span class="sxs-lookup"><span data-stu-id="38292-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="38292-138">В этом столбце отображается ссылка (`<a>` элемент), текст которой говорит «Edit» (изменить).</span><span class="sxs-lookup"><span data-stu-id="38292-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="38292-139">После этого создается ссылка, которая выглядит следующим образом при выполнении страницы, при этом значение `id` для каждого фильма различается.</span><span class="sxs-lookup"><span data-stu-id="38292-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="38292-140">Эта ссылка вызывает страницу с именем *едитмовие*и передает строку запроса `?id=7` на эту страницу.</span><span class="sxs-lookup"><span data-stu-id="38292-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="38292-141">Синтаксис нового столбца может показаться немного сложным, но это только потому, что он объединяет несколько элементов.</span><span class="sxs-lookup"><span data-stu-id="38292-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="38292-142">Каждый отдельный элемент прост.</span><span class="sxs-lookup"><span data-stu-id="38292-142">Each individual element is straightforward.</span></span> <span data-ttu-id="38292-143">Если сосредоточиться только на элементе `<a>`, вы увидите следующую разметку:</span><span class="sxs-lookup"><span data-stu-id="38292-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="38292-144">Некоторые общие сведения о работе сетки: Сетка отображает строки, по одной для каждой записи базы данных и отображает столбцы для каждого поля в записи базы данных.</span><span class="sxs-lookup"><span data-stu-id="38292-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="38292-145">При создании каждой строки сетки объект `item` содержит запись базы данных (элемент) для этой строки.</span><span class="sxs-lookup"><span data-stu-id="38292-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="38292-146">Такое размещение дает возможность в коде получить данные для этой строки.</span><span class="sxs-lookup"><span data-stu-id="38292-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="38292-147">Вот что вы видите: выражение `item.ID` получает значение идентификатора текущего элемента базы данных.</span><span class="sxs-lookup"><span data-stu-id="38292-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="38292-148">Можно получить любое из значений базы данных (название, жанр или год) таким же образом, используя `item.Title`, `item.Genre`или `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="38292-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="38292-149">Выражение `"~/EditMovie?id=@item.ID` сочетает жестко закодированную часть целевого URL-адреса (`~/EditMovie?id=`) с этим динамически производным ИДЕНТИФИКАТОРом.</span><span class="sxs-lookup"><span data-stu-id="38292-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="38292-150">(Вы видели оператор `~` в предыдущем учебном курсе; это оператор ASP.NET, представляющий текущий корневой каталог веб-сайта.)</span><span class="sxs-lookup"><span data-stu-id="38292-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="38292-151">В результате эта часть разметки в столбце просто создает нечто вроде приведенной ниже разметки во время выполнения:</span><span class="sxs-lookup"><span data-stu-id="38292-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="38292-152">Естественно, фактическое значение `id` будет различным для каждой строки.</span><span class="sxs-lookup"><span data-stu-id="38292-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="38292-153">Создание пользовательского интерфейса для столбца сетки</span><span class="sxs-lookup"><span data-stu-id="38292-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="38292-154">Теперь вернемся к столбцу сетки.</span><span class="sxs-lookup"><span data-stu-id="38292-154">Now back to the grid column.</span></span> <span data-ttu-id="38292-155">Три столбца, которые изначально были в сетке, отображали только значения данных (заголовок, жанр и год).</span><span class="sxs-lookup"><span data-stu-id="38292-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="38292-156">Вы указали это отображение, передав имя столбца базы данных &mdash; например `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="38292-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="38292-157">Новый столбец **изменить** ссылку отличается.</span><span class="sxs-lookup"><span data-stu-id="38292-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="38292-158">Вместо указания имени столбца передается параметр `format`.</span><span class="sxs-lookup"><span data-stu-id="38292-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="38292-159">Этот параметр позволяет определить разметку, которую вспомогательный модуль `WebGrid` будет визуализировать вместе со значением `item`, чтобы отобразить данные столбца полужирным или зеленым цветом или в любом нужном формате.</span><span class="sxs-lookup"><span data-stu-id="38292-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="38292-160">Например, если нужно, чтобы заголовок отображался полужирным, можно создать столбец, подобный приведенному ниже.</span><span class="sxs-lookup"><span data-stu-id="38292-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="38292-161">(Различные символы `@`, отображаемые в свойстве `format`, помечают переход между разметкой и значением кода.)</span><span class="sxs-lookup"><span data-stu-id="38292-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="38292-162">Когда вы узнаете о свойстве `format`, проще понять, как помещается новый столбец ссылки **Edit** :</span><span class="sxs-lookup"><span data-stu-id="38292-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="38292-163">Столбец состоит *только* из разметки, в которой отображается ссылка, а также некоторые сведения (идентификатор), извлеченные из записи базы данных для строки.</span><span class="sxs-lookup"><span data-stu-id="38292-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP]
> 
> <span data-ttu-id="38292-164">**Именованные и позиционированные параметры для метода**</span><span class="sxs-lookup"><span data-stu-id="38292-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="38292-165">Многие случаи, когда вы вызывали метод и передали ему параметры, вы просто указали значения параметров, разделенные запятыми.</span><span class="sxs-lookup"><span data-stu-id="38292-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="38292-166">Вот несколько примеров.</span><span class="sxs-lookup"><span data-stu-id="38292-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="38292-167">Мы не упомянули проблемы, когда впервые познакомились с этим кодом, но в каждом случае вы передаете параметры в методы в определенном порядке &mdash; а именно в том порядке, в котором параметры определены в этом методе.</span><span class="sxs-lookup"><span data-stu-id="38292-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="38292-168">Для `db.Execute` и `Validation.RequireFields`, если вы простроили порядок передаваемых значений, при выполнении страницы или по крайней мере неопределенного результата выдается сообщение об ошибке.</span><span class="sxs-lookup"><span data-stu-id="38292-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="38292-169">Очевидно, что необходимо определить порядок передачи параметров в.</span><span class="sxs-lookup"><span data-stu-id="38292-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="38292-170">(В WebMatrix технология IntelliSense поможет вам понять, как можно узнать имя, тип и порядок параметров.)</span><span class="sxs-lookup"><span data-stu-id="38292-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="38292-171">В качестве альтернативы передаче значений по порядку можно использовать *именованные параметры*.</span><span class="sxs-lookup"><span data-stu-id="38292-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="38292-172">(Передача параметров в порядке называется использованием *параметров позиционирования*.) Для именованных параметров необходимо явно указать имя параметра при передаче его значения.</span><span class="sxs-lookup"><span data-stu-id="38292-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="38292-173">В этих учебниках уже несколько раз использовались именованные параметры.</span><span class="sxs-lookup"><span data-stu-id="38292-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="38292-174">Пример:</span><span class="sxs-lookup"><span data-stu-id="38292-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="38292-175">и</span><span class="sxs-lookup"><span data-stu-id="38292-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="38292-176">Именованные параметры удобны для нескольких ситуаций, особенно если метод принимает много параметров.</span><span class="sxs-lookup"><span data-stu-id="38292-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="38292-177">Одна из них — если требуется передать только один или два параметра, но значения, которые необходимо передать, не являются значениями в списке параметров.</span><span class="sxs-lookup"><span data-stu-id="38292-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="38292-178">Другая ситуация заключается в том, что вы хотите сделать код более удобочитаемым, передав параметры в том порядке, который наиболее удобен для вас.</span><span class="sxs-lookup"><span data-stu-id="38292-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="38292-179">Очевидно, что для использования именованных параметров необходимо знание имен параметров.</span><span class="sxs-lookup"><span data-stu-id="38292-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="38292-180">Технология WebMatrix IntelliSense может *Показывать* имена, но в настоящее время их невозможно заполнить.</span><span class="sxs-lookup"><span data-stu-id="38292-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>

## <a name="creating-the-edit-page"></a><span data-ttu-id="38292-181">Создание страницы редактирования</span><span class="sxs-lookup"><span data-stu-id="38292-181">Creating the Edit Page</span></span>

<span data-ttu-id="38292-182">Теперь можно создать страницу *едитмовие* .</span><span class="sxs-lookup"><span data-stu-id="38292-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="38292-183">Когда пользователь найдет ссылку " **изменить** ", они появятся на этой странице.</span><span class="sxs-lookup"><span data-stu-id="38292-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="38292-184">Создайте страницу с именем *едитмовие. cshtml* и замените содержимое файла следующей разметкой:</span><span class="sxs-lookup"><span data-stu-id="38292-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="38292-185">Эта разметка и код аналогичны тем, которые имеются на странице *аддмовие* .</span><span class="sxs-lookup"><span data-stu-id="38292-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="38292-186">Есть небольшая разница в тексте кнопки Submit (отправить).</span><span class="sxs-lookup"><span data-stu-id="38292-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="38292-187">Как и на странице *аддмовие* , имеется вызов `Html.ValidationSummary`, который будет отображать ошибки проверки, если таковые имеются.</span><span class="sxs-lookup"><span data-stu-id="38292-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="38292-188">На этот раз мы оставляем вызовы `Validation.Message`, так как ошибки будут отображаться в сводке проверки.</span><span class="sxs-lookup"><span data-stu-id="38292-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="38292-189">Как отмечалось в предыдущем руководстве, можно использовать сводку проверки и отдельные сообщения об ошибках в различных сочетаниях.</span><span class="sxs-lookup"><span data-stu-id="38292-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="38292-190">Обратите внимание, что атрибуту `method` элемента `<form>` присвоено значение `post`.</span><span class="sxs-lookup"><span data-stu-id="38292-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="38292-191">Как и на странице *аддмовие. cshtml* , эта страница вносит изменения в базу данных.</span><span class="sxs-lookup"><span data-stu-id="38292-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="38292-192">Поэтому эта форма должна выполнять `POST` операцию.</span><span class="sxs-lookup"><span data-stu-id="38292-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="38292-193">(Дополнительные сведения о различиях между операциями `GET` и `POST` см. в разделе [Получение, публикация и использование HTTP-команд](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) в учебнике по HTML-формам.)</span><span class="sxs-lookup"><span data-stu-id="38292-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="38292-194">Как было показано в предыдущем учебном курсе, `value` атрибуты текстовых полей задаются с помощью кода Razor, чтобы их можно было предварительно загрузить.</span><span class="sxs-lookup"><span data-stu-id="38292-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="38292-195">Но на этот раз вы используете такие переменные, как `title` и `genre` для этой задачи, а не `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="38292-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="38292-196">Как и ранее, эта разметка будет загружать значения текстовых полей со значениями фильмов.</span><span class="sxs-lookup"><span data-stu-id="38292-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="38292-197">Вы увидите, что в это время удобно использовать переменные, а не использовать объект `Request`.</span><span class="sxs-lookup"><span data-stu-id="38292-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="38292-198">На этой странице также имеется элемент `<input type="hidden">`.</span><span class="sxs-lookup"><span data-stu-id="38292-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="38292-199">Этот элемент сохраняет идентификатор фильма, не делая его видимым на странице.</span><span class="sxs-lookup"><span data-stu-id="38292-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="38292-200">Идентификатор первоначально передается на страницу с помощью значения строки запроса (`?id=7` или аналогичного URL-адресу).</span><span class="sxs-lookup"><span data-stu-id="38292-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="38292-201">Поместив значение идентификатора в скрытое поле, можно убедиться, что оно доступно при отправке формы, даже если у вас больше нет доступа к исходному URL-адресу, с помощью которого была вызвана страница.</span><span class="sxs-lookup"><span data-stu-id="38292-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="38292-202">В отличие от страницы *аддмовие* , код для страницы *едитмовие* имеет две отдельные функции.</span><span class="sxs-lookup"><span data-stu-id="38292-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="38292-203">Первая функция заключается в том, что когда страница отображается в первый раз (и *только* затем), код получает идентификатор фильма из строки запроса.</span><span class="sxs-lookup"><span data-stu-id="38292-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="38292-204">Затем код использует идентификатор для чтения соответствующего фильма из базы данных и его вывода (Предварительная загрузка) в текстовые поля.</span><span class="sxs-lookup"><span data-stu-id="38292-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="38292-205">Вторая функция заключается в том, что когда пользователь нажимает кнопку **отправить изменения** , код должен считывать значения текстовых полей и проверять их.</span><span class="sxs-lookup"><span data-stu-id="38292-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="38292-206">Код также должен обновить элемент базы данных новыми значениями.</span><span class="sxs-lookup"><span data-stu-id="38292-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="38292-207">Этот метод аналогичен добавлению записи, как показано в *аддмовие*.</span><span class="sxs-lookup"><span data-stu-id="38292-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="38292-208">Добавление кода для чтения одного фильма</span><span class="sxs-lookup"><span data-stu-id="38292-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="38292-209">Чтобы выполнить первую функцию, добавьте следующий код в верхнюю часть страницы:</span><span class="sxs-lookup"><span data-stu-id="38292-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="38292-210">Большая часть этого кода находится внутри блока, который начинается `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="38292-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="38292-211">Оператор `!` означает «not», поэтому выражение означает, что *этот запрос не является отправкой POST*, что является косвенным способом сказать, *является ли этот запрос первым при выполнении этой страницы*.</span><span class="sxs-lookup"><span data-stu-id="38292-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="38292-212">Как отмечалось ранее, этот код должен выполняться *только* при первом запуске страницы.</span><span class="sxs-lookup"><span data-stu-id="38292-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="38292-213">Если вы не заключаете код в `if(!IsPost)`, он будет выполняться при каждом вызове страницы, будь то первый раз или в ответ на нажатие кнопки.</span><span class="sxs-lookup"><span data-stu-id="38292-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="38292-214">Обратите внимание, что в этот раз код включает блок `else`.</span><span class="sxs-lookup"><span data-stu-id="38292-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="38292-215">Как мы сказали при вводе блоков `if`, иногда требуется выполнить другой код, если проверяемое условие не выполняется.</span><span class="sxs-lookup"><span data-stu-id="38292-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="38292-216">Это так.</span><span class="sxs-lookup"><span data-stu-id="38292-216">That's the case here.</span></span> <span data-ttu-id="38292-217">Если условие передается (то есть если идентификатор, переданный на страницу, имеет значение OK), то считывается строка из базы данных.</span><span class="sxs-lookup"><span data-stu-id="38292-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="38292-218">Однако если условие не проходит, выполняется блок `else`, и код устанавливает сообщение об ошибке.</span><span class="sxs-lookup"><span data-stu-id="38292-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="38292-219">Проверка значения, переданного на страницу</span><span class="sxs-lookup"><span data-stu-id="38292-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="38292-220">Код использует `Request.QueryString["id"]` для получения идентификатора, который передается на страницу.</span><span class="sxs-lookup"><span data-stu-id="38292-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="38292-221">Код гарантирует, что значение было фактически передано для идентификатора.</span><span class="sxs-lookup"><span data-stu-id="38292-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="38292-222">Если значение не было передано, код задает ошибку проверки.</span><span class="sxs-lookup"><span data-stu-id="38292-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="38292-223">Этот код показывает другой способ проверки информации.</span><span class="sxs-lookup"><span data-stu-id="38292-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="38292-224">В предыдущем руководстве вы работали с вспомогательным приложением `Validation`.</span><span class="sxs-lookup"><span data-stu-id="38292-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="38292-225">Вы зарегистрировали поля для проверки, и ASP.NET автоматически выполнил проверку и отображение ошибок с помощью `Html.ValidationMessage` и `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="38292-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="38292-226">Однако в этом случае вы не проверяете вводимые пользователем данные.</span><span class="sxs-lookup"><span data-stu-id="38292-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="38292-227">Вместо этого выполняется проверка значения, переданного на страницу из другого места.</span><span class="sxs-lookup"><span data-stu-id="38292-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="38292-228">Модуль поддержки `Validation` не делает этого.</span><span class="sxs-lookup"><span data-stu-id="38292-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="38292-229">Поэтому вы проверяете значение самостоятельно, проверяя его с помощью `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="38292-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="38292-230">Если возникла проблема, можно отобразить ошибку с помощью `Html.ValidationSummary`, как это было сделано при работе со вспомогательным приложением `Validation`.</span><span class="sxs-lookup"><span data-stu-id="38292-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="38292-231">Для этого вызовите `Validation.AddFormError` и передайте ему сообщение для вывода.</span><span class="sxs-lookup"><span data-stu-id="38292-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="38292-232">`Validation.AddFormError` — это встроенный метод, который позволяет определять пользовательские сообщения, которые связываются с системой проверки, с которой вы уже знакомы.</span><span class="sxs-lookup"><span data-stu-id="38292-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="38292-233">(Далее в этом учебнике мы поговорим о том, как сделать этот процесс проверки более надежным.)</span><span class="sxs-lookup"><span data-stu-id="38292-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="38292-234">Убедившись в наличии идентификатора для фильма, код считывает базу данных и ищет только один элемент базы данных.</span><span class="sxs-lookup"><span data-stu-id="38292-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="38292-235">(Возможно, вы заметили общий шаблон для операций с базой данных: Откройте базу данных, определите инструкцию SQL и выполните инструкцию.) На этот раз инструкция SQL `Select` включает `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="38292-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="38292-236">Поскольку идентификатор уникален, может возвращаться только одна запись.</span><span class="sxs-lookup"><span data-stu-id="38292-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="38292-237">Запрос выполняется с помощью `db.QuerySingle` (не `db.Query`, как используется для перечислений фильмов), и код помещает результат в переменную `row`.</span><span class="sxs-lookup"><span data-stu-id="38292-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="38292-238">Имя `row` является произвольным; Вы можете присвоить переменным любые значения.</span><span class="sxs-lookup"><span data-stu-id="38292-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="38292-239">Переменные, инициализированные в верхней части, затем заполняются сведениями о фильме, чтобы эти значения можно было отобразить в текстовых полях.</span><span class="sxs-lookup"><span data-stu-id="38292-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="38292-240">Тестирование страницы редактирования (до сих пор)</span><span class="sxs-lookup"><span data-stu-id="38292-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="38292-241">Если вы хотите протестировать страницу, запустите страницу *фильмов* сейчас и щелкните ссылку Edit ( **изменить** ) рядом с любым фильмом.</span><span class="sxs-lookup"><span data-stu-id="38292-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="38292-242">Вы увидите страницу *едитмовие* с данными, заполненными для выбранного фильма:</span><span class="sxs-lookup"><span data-stu-id="38292-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Изменение страницы фильма с фильмом для изменения](updating-data/_static/image3.png)

<span data-ttu-id="38292-244">Обратите внимание, что URL-адрес страницы содержит нечто вроде `?id=10` (или другое число).</span><span class="sxs-lookup"><span data-stu-id="38292-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="38292-245">Итак, вы проверили, что ссылки **Edit** на странице *фильмов* работают, что страница считывает идентификатор из строки запроса и работает запрос к базе данных для получения одной записи фильма.</span><span class="sxs-lookup"><span data-stu-id="38292-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="38292-246">Можно изменить сведения о фильме, но при нажатии кнопки **отправить изменения**ничего не происходит.</span><span class="sxs-lookup"><span data-stu-id="38292-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="38292-247">Добавление кода для обновления фильма с учетом изменений пользователя</span><span class="sxs-lookup"><span data-stu-id="38292-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="38292-248">Чтобы реализовать вторую функцию (сохранение изменений) в файле *едитмовие. cshtml* , добавьте следующий код непосредственно внутри закрывающей скобки блока `@`.</span><span class="sxs-lookup"><span data-stu-id="38292-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="38292-249">(Если вы точно не знаете, где разместить код, можно взглянуть на [полный листинг кода для страницы "изменение фильма"](#Complete_Page_Listing_for_EditMovie) , которая отображается в конце этого руководства.)</span><span class="sxs-lookup"><span data-stu-id="38292-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="38292-250">Опять же, эта разметка и код похожи на код в *аддмовие*.</span><span class="sxs-lookup"><span data-stu-id="38292-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="38292-251">Код находится в блоке `if(IsPost)`, так как этот код выполняется только в том случае, если пользователь нажимает кнопку **отправить изменения** &mdash; то есть (и только в момент, когда) была отправлена форма.</span><span class="sxs-lookup"><span data-stu-id="38292-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="38292-252">В этом случае вы не используете такой тест, как `if(IsPost && Validation.IsValid())`, то есть вы не объединяете оба теста с помощью и.</span><span class="sxs-lookup"><span data-stu-id="38292-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="38292-253">На этой странице сначала определите, есть ли отправка формы (`if(IsPost)`), и только затем зарегистрируйте поля для проверки.</span><span class="sxs-lookup"><span data-stu-id="38292-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="38292-254">Затем можно проверить результаты проверки (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="38292-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="38292-255">Последовательность немного отличается от последовательности на странице *аддмовие. cshtml* , но результат такой же.</span><span class="sxs-lookup"><span data-stu-id="38292-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="38292-256">Значения текстовых полей можно получить с помощью `Request.Form["title"]` и аналогичного кода для других элементов `<input>`.</span><span class="sxs-lookup"><span data-stu-id="38292-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="38292-257">Обратите внимание, что на этот раз код получает идентификатор фильма из скрытого поля (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="38292-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="38292-258">При первом запуске страницы код получил идентификатор из строки запроса.</span><span class="sxs-lookup"><span data-stu-id="38292-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="38292-259">Вы получаете значение из скрытого поля, чтобы убедиться, что вы получаете идентификатор фильма, который был первоначально отображен, в случае, когда строка запроса изменилась с этого момента.</span><span class="sxs-lookup"><span data-stu-id="38292-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="38292-260">Действительно важное различие между кодом *аддмовие* и этим кодом заключается в том, что в этом коде используется инструкция SQL `Update`, а не инструкция `Insert Into`.</span><span class="sxs-lookup"><span data-stu-id="38292-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="38292-261">В следующем примере показан синтаксис инструкции SQL `Update`.</span><span class="sxs-lookup"><span data-stu-id="38292-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="38292-262">Можно указать любые столбцы в любом порядке, и вам не обязательно обновлять каждый столбец во время операции `Update`.</span><span class="sxs-lookup"><span data-stu-id="38292-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="38292-263">(Вы не можете обновить сам идентификатор, так как он, в силу этого, сохранит запись как новую запись, и это недопустимо для операции `Update`.)</span><span class="sxs-lookup"><span data-stu-id="38292-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="38292-264">**Важно!** Предложение `Where` с ИДЕНТИФИКАТОРом очень важно, так как база данных знает, какую запись базы данных необходимо обновить.</span><span class="sxs-lookup"><span data-stu-id="38292-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="38292-265">Если оставить предложение `Where`, база данных будет обновлять *каждую* запись в базе данных.</span><span class="sxs-lookup"><span data-stu-id="38292-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="38292-266">В большинстве случаев это может привести к сбою.</span><span class="sxs-lookup"><span data-stu-id="38292-266">In most cases, that would be a disaster.</span></span>

<span data-ttu-id="38292-267">В коде обновляемые значения передаются в инструкцию SQL с помощью заполнителей.</span><span class="sxs-lookup"><span data-stu-id="38292-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="38292-268">Чтобы повторить то, что мы уже говорили: в целях безопасности используйте *только* заполнители для передачи значений в инструкцию SQL.</span><span class="sxs-lookup"><span data-stu-id="38292-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="38292-269">После того как код использует `db.Execute` для выполнения инструкции `Update`, он перенаправляется обратно на страницу листинга, где можно увидеть изменения.</span><span class="sxs-lookup"><span data-stu-id="38292-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="38292-270">**Разные инструкции SQL, различные методы**</span><span class="sxs-lookup"><span data-stu-id="38292-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="38292-271">Возможно, вы заметили, что для выполнения различных инструкций SQL используются несколько различных методов.</span><span class="sxs-lookup"><span data-stu-id="38292-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="38292-272">Для выполнения `Select` запроса, который потенциально возвращает несколько записей, используется метод `Query`.</span><span class="sxs-lookup"><span data-stu-id="38292-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="38292-273">Чтобы выполнить `Select` запрос, который будет возвращать только один элемент базы данных, используется метод `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="38292-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="38292-274">Для выполнения команд, которые делают изменения, но не возвращают элементы базы данных, используется метод `Execute`.</span><span class="sxs-lookup"><span data-stu-id="38292-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="38292-275">У вас должны быть разные методы, так как каждый из них возвращает разные результаты, как уже говорилось в различиях между `Query` и `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="38292-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="38292-276">(`Execute` метод фактически возвращает значение также &mdash; а именно, количество строк базы данных, затронутых командой &mdash; но проигнорированное до сих пор.)</span><span class="sxs-lookup"><span data-stu-id="38292-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="38292-277">Разумеется, метод `Query` может возвращать только одну строку базы данных.</span><span class="sxs-lookup"><span data-stu-id="38292-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="38292-278">Однако ASP.NET всегда обрабатывает результаты метода `Query` в виде коллекции.</span><span class="sxs-lookup"><span data-stu-id="38292-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="38292-279">Даже если метод возвращает только одну строку, необходимо извлечь эту единственную строку из коллекции.</span><span class="sxs-lookup"><span data-stu-id="38292-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="38292-280">Таким образом, если *известно* , что вы получаете только одну строку, более удобно использовать `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="38292-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="38292-281">Существует несколько других методов, выполняющих определенные типы операций с базами данных.</span><span class="sxs-lookup"><span data-stu-id="38292-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="38292-282">Список методов базы данных можно найти в [кратком справочнике по API веб-страницы ASP.NET](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="38292-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>

## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="38292-283">Обеспечение более надежной проверки идентификатора</span><span class="sxs-lookup"><span data-stu-id="38292-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="38292-284">При первом запуске страницы вы получаете идентификатор фильма из строки запроса, чтобы можно было получить этот фильм из базы данных.</span><span class="sxs-lookup"><span data-stu-id="38292-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="38292-285">Вы уверены, что на самом деле имелось значение для поиска, которое вы выполнили с помощью этого кода:</span><span class="sxs-lookup"><span data-stu-id="38292-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="38292-286">Этот код использовался, чтобы убедиться, что если пользователь получит на страницу *едитмовиес* , не выбирая фильм на странице *фильмов* , на странице будет отображаться понятное сообщение об ошибке.</span><span class="sxs-lookup"><span data-stu-id="38292-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="38292-287">(В противном случае пользователи увидят ошибку, которая, вероятно, просто запутать их.)</span><span class="sxs-lookup"><span data-stu-id="38292-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="38292-288">Однако эта проверка не очень надежна.</span><span class="sxs-lookup"><span data-stu-id="38292-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="38292-289">Эту страницу также можно вызвать с этими ошибками:</span><span class="sxs-lookup"><span data-stu-id="38292-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="38292-290">Идентификатор не является числом.</span><span class="sxs-lookup"><span data-stu-id="38292-290">The ID isn't a number.</span></span> <span data-ttu-id="38292-291">Например, страницу можно вызвать с URL-адресом, например `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="38292-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="38292-292">Идентификатор является числом, но ссылается на фильм, который не существует (например, `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="38292-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="38292-293">Если вы хотите просмотреть ошибки, возникающие в результате этих URL-адресов, запустите страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="38292-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="38292-294">Выберите фильм для редактирования, а затем измените URL-адрес страницы *едитмовие* на URL-адрес, содержащий буквенный идентификатор или идентификатор несуществующего фильма.</span><span class="sxs-lookup"><span data-stu-id="38292-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="38292-295">Так что же делать?</span><span class="sxs-lookup"><span data-stu-id="38292-295">So what should you do?</span></span> <span data-ttu-id="38292-296">Первое исправление — убедиться, что идентификатор не только передается на страницу, но идентификатор является целым числом.</span><span class="sxs-lookup"><span data-stu-id="38292-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="38292-297">Измените код для `!IsPost` теста, как показано в следующем примере:</span><span class="sxs-lookup"><span data-stu-id="38292-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="38292-298">Вы добавили второе условие в `IsEmpty` тест, связанное с `&&` (логическое и):</span><span class="sxs-lookup"><span data-stu-id="38292-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="38292-299">Вы можете не забыть ознакомиться со статьей [Введение в веб-страницы ASP.NET программирование](../introducing-razor-syntax-c.md) , в котором такие методы, как `AsBool` `AsInt`, преобразуют строку символов в другой тип данных.</span><span class="sxs-lookup"><span data-stu-id="38292-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="38292-300">Метод `IsInt` (и другие, такие как `IsBool` и `IsDateTime`) похожи.</span><span class="sxs-lookup"><span data-stu-id="38292-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="38292-301">Однако они проверяют только *возможность преобразования строки* без фактического выполнения преобразования.</span><span class="sxs-lookup"><span data-stu-id="38292-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="38292-302">Итак, здесь вы говорите, что *значение строки запроса можно преобразовать в целое число...* .</span><span class="sxs-lookup"><span data-stu-id="38292-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="38292-303">Другая потенциальная проблема заключается в поиске фильма, который не существует.</span><span class="sxs-lookup"><span data-stu-id="38292-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="38292-304">Код для получения фильма выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="38292-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="38292-305">Если в метод `QuerySingle` передается `movieId` значение, которое не соответствует реальному фильму, ничего не возвращается, а последующие инструкции (например, `title=row.Title`) приводят к ошибкам.</span><span class="sxs-lookup"><span data-stu-id="38292-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="38292-306">Опять же, это легко исправить.</span><span class="sxs-lookup"><span data-stu-id="38292-306">Again there's an easy fix.</span></span> <span data-ttu-id="38292-307">Если метод `db.QuerySingle` не возвращает результатов, `row` переменная будет иметь значение null.</span><span class="sxs-lookup"><span data-stu-id="38292-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="38292-308">Поэтому можно проверить, имеет ли переменная `row` значение null, прежде чем пытаться получить из нее значения.</span><span class="sxs-lookup"><span data-stu-id="38292-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="38292-309">Следующий код добавляет блок `if` вокруг инструкций, которые получают значения из объекта `row`:</span><span class="sxs-lookup"><span data-stu-id="38292-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="38292-310">С этими двумя дополнительными проверочными тестами страница станет более подробным.</span><span class="sxs-lookup"><span data-stu-id="38292-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="38292-311">Полный код ветви `!IsPost` теперь выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="38292-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="38292-312">Мы будем Заметьте, что эта задача хорошо используется для `else` блока.</span><span class="sxs-lookup"><span data-stu-id="38292-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="38292-313">Если тесты не пройдены, `else` блокирует задание сообщений об ошибках.</span><span class="sxs-lookup"><span data-stu-id="38292-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="38292-314">Добавление ссылки для возврата на страницу фильмов</span><span class="sxs-lookup"><span data-stu-id="38292-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="38292-315">Окончательная и полезная информация — Добавление ссылки обратно на страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="38292-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="38292-316">В обычном потоке событий пользователи начинают со страницы *фильмов* и щелкают ссылку Edit ( **изменить** ).</span><span class="sxs-lookup"><span data-stu-id="38292-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="38292-317">В результате они перемещаются на страницу *едитмовие* , где можно изменить фильм и нажать кнопку.</span><span class="sxs-lookup"><span data-stu-id="38292-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="38292-318">После того как код обрабатывает изменение, он перенаправляется обратно на страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="38292-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="38292-319">Но:</span><span class="sxs-lookup"><span data-stu-id="38292-319">However:</span></span>

- <span data-ttu-id="38292-320">Пользователь может решить не изменять что-либо.</span><span class="sxs-lookup"><span data-stu-id="38292-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="38292-321">Пользователь мог стать на этой странице без предварительного выбора ссылки Edit ( **изменить** ) на странице *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="38292-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="38292-322">В любом случае, вам нужно легко вернуться к основному листингу.</span><span class="sxs-lookup"><span data-stu-id="38292-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="38292-323">Это простое исправление &mdash; добавить следующую разметку сразу после закрывающего тега `</form>` в разметке:</span><span class="sxs-lookup"><span data-stu-id="38292-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="38292-324">Эта разметка использует тот же синтаксис для элемента `<a>`, который вы видели в других местах.</span><span class="sxs-lookup"><span data-stu-id="38292-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="38292-325">URL-адрес включает `~` в значение "корень веб-сайта".</span><span class="sxs-lookup"><span data-stu-id="38292-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="38292-326">Тестирование процесса обновления фильма</span><span class="sxs-lookup"><span data-stu-id="38292-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="38292-327">Теперь можно протестировать.</span><span class="sxs-lookup"><span data-stu-id="38292-327">Now you can test.</span></span> <span data-ttu-id="38292-328">Запустите страницу *фильмов* и нажмите кнопку **изменить** рядом с фильмом.</span><span class="sxs-lookup"><span data-stu-id="38292-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="38292-329">Когда появится страница *едитмовие* , внесите изменения в фильм и нажмите кнопку **отправить изменения**.</span><span class="sxs-lookup"><span data-stu-id="38292-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="38292-330">Когда появится список фильмов, убедитесь, что изменения отображаются.</span><span class="sxs-lookup"><span data-stu-id="38292-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="38292-331">Чтобы убедиться, что проверка работает, нажмите кнопку **изменить** для другого фильма.</span><span class="sxs-lookup"><span data-stu-id="38292-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="38292-332">При переходе на страницу *едитмовие* очистите поле **Жанр** (или поле **year** или оба) и попытайтесь отправить изменения.</span><span class="sxs-lookup"><span data-stu-id="38292-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="38292-333">Вы увидите сообщение об ошибке, как было бы ожидаемо:</span><span class="sxs-lookup"><span data-stu-id="38292-333">You'll see an error, as you'd expect:</span></span>

![Страница редактирования фильма с ошибками проверки](updating-data/_static/image4.png)

<span data-ttu-id="38292-335">Щелкните ссылку **вернуться к списку фильмов** , чтобы отменить изменения и вернуться на страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="38292-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="38292-336">Далее</span><span class="sxs-lookup"><span data-stu-id="38292-336">Coming Up Next</span></span>

<span data-ttu-id="38292-337">В следующем руководстве вы узнаете, как удалить запись фильма.</span><span class="sxs-lookup"><span data-stu-id="38292-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="38292-338">Полный список для страницы фильмов (обновляется ссылками для редактирования)</span><span class="sxs-lookup"><span data-stu-id="38292-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="38292-339">Полный список страниц для страницы "изменение фильма"</span><span class="sxs-lookup"><span data-stu-id="38292-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="38292-340">Дополнительные ресурсы</span><span class="sxs-lookup"><span data-stu-id="38292-340">Additional Resources</span></span>

- [<span data-ttu-id="38292-341">Введение в веб-программирование ASP.NET с помощью синтаксиса Razor</span><span class="sxs-lookup"><span data-stu-id="38292-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](../../getting-started/introducing-razor-syntax-c.md)
- <span data-ttu-id="38292-342">[Инструкция SQL UPDATE](http://www.w3schools.com/sql/sql_update.asp) на сайте W3Schools</span><span class="sxs-lookup"><span data-stu-id="38292-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="38292-343">[Назад](entering-data.md)
> [Вперед](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="38292-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
