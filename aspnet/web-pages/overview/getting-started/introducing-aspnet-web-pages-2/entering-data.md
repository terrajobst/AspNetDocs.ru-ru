---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
title: Введение веб-страницы ASP.NET ввод данных базы данных с помощью форм | Документация Майкрософт
author: Rick-Anderson
description: В этом учебнике показано, как создать форму ввода, а затем ввести данные, получаемые из формы, в таблицу базы данных при использовании веб-страницы ASP.NET (...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: d37c93fc-25fd-4e94-8671-0d437beef206
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
msc.type: authoredcontent
ms.openlocfilehash: b9354a7b97a7df9020a681f709e16a92650cfcf0
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/06/2020
ms.locfileid: "78506604"
---
# <a name="introducing-aspnet-web-pages---entering-database-data-by-using-forms"></a><span data-ttu-id="a99df-103">Введение веб-страницы ASP.NET ввод данных базы данных с помощью форм</span><span class="sxs-lookup"><span data-stu-id="a99df-103">Introducing ASP.NET Web Pages - Entering Database Data by Using Forms</span></span>

<span data-ttu-id="a99df-104">от [Tom фитзмаккен](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="a99df-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="a99df-105">В этом учебнике показано, как создать форму ввода, а затем ввести данные, получаемые из формы, в таблицу базы данных при использовании веб-страницы ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="a99df-105">This tutorial shows you how to create an entry form and then enter the data that you get from the form into a database table when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="a99df-106">Предполагается, что вы выполнили серию [из основ HTML-форм в веб-страницы ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251581).</span><span class="sxs-lookup"><span data-stu-id="a99df-106">It assumes you have completed the series through [Basics of HTML Forms in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581).</span></span>
> 
> <span data-ttu-id="a99df-107">Из этого руководства вы узнаете, как выполнять такие задачи:</span><span class="sxs-lookup"><span data-stu-id="a99df-107">What you'll learn:</span></span>
> 
> - <span data-ttu-id="a99df-108">Дополнительные сведения об обработке форм ввода.</span><span class="sxs-lookup"><span data-stu-id="a99df-108">More about how to process entry forms.</span></span>
> - <span data-ttu-id="a99df-109">Инструкции по добавлению (вставке) данных в базу данных.</span><span class="sxs-lookup"><span data-stu-id="a99df-109">How to add (insert) data in a database.</span></span>
> - <span data-ttu-id="a99df-110">Как убедиться, что пользователи указали требуемое значение в форме (проверка вводимых пользователем данных).</span><span class="sxs-lookup"><span data-stu-id="a99df-110">How to make sure that users have entered a required value in a form (how to validate user input).</span></span>
> - <span data-ttu-id="a99df-111">Как отобразить ошибки проверки.</span><span class="sxs-lookup"><span data-stu-id="a99df-111">How to display validation errors.</span></span>
> - <span data-ttu-id="a99df-112">Переход на другую страницу с текущей страницы.</span><span class="sxs-lookup"><span data-stu-id="a99df-112">How to jump to another page from the current page.</span></span>
>   
> 
> <span data-ttu-id="a99df-113">Обсуждаемые функции и технологии:</span><span class="sxs-lookup"><span data-stu-id="a99df-113">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="a99df-114">метод `Database.Execute` ;</span><span class="sxs-lookup"><span data-stu-id="a99df-114">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="a99df-115">Инструкция SQL `Insert Into`</span><span class="sxs-lookup"><span data-stu-id="a99df-115">The SQL `Insert Into` statement</span></span>
> - <span data-ttu-id="a99df-116">Вспомогательный метод `Validation`.</span><span class="sxs-lookup"><span data-stu-id="a99df-116">The `Validation` helper.</span></span>
> - <span data-ttu-id="a99df-117">метод `Response.Redirect` ;</span><span class="sxs-lookup"><span data-stu-id="a99df-117">The `Response.Redirect` method.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="a99df-118">Что вы создадите</span><span class="sxs-lookup"><span data-stu-id="a99df-118">What You'll Build</span></span>

<span data-ttu-id="a99df-119">В этом руководстве, в котором показано, как создать базу данных, вы указали данные базы данных путем изменения базы данных непосредственно в WebMatrix, работая в рабочей области **базы данных** .</span><span class="sxs-lookup"><span data-stu-id="a99df-119">In the tutorial earlier that showed you how to create a database, you entered database data by editing the database directly in WebMatrix, working in the **Database** workspace.</span></span> <span data-ttu-id="a99df-120">Однако в большинстве приложений это не самый практичный способ размещения данных в базе данных.</span><span class="sxs-lookup"><span data-stu-id="a99df-120">In most apps, that's not a practical way to put data into the database, though.</span></span> <span data-ttu-id="a99df-121">Итак, в этом учебнике вы создадите веб-интерфейс, который позволит пользователям вводить данные и сохранять их в базе данных.</span><span class="sxs-lookup"><span data-stu-id="a99df-121">So in this tutorial, you'll create a web-based interface that lets you or anyone enter data and save it to the database.</span></span>

<span data-ttu-id="a99df-122">Вы создадите страницу, на которой можно ввести новые фильмы.</span><span class="sxs-lookup"><span data-stu-id="a99df-122">You'll create a page where you can enter new movies.</span></span> <span data-ttu-id="a99df-123">На странице будет содержаться форма ввода, содержащая поля (текстовые поля), где можно ввести название фильма, жанр и год.</span><span class="sxs-lookup"><span data-stu-id="a99df-123">The page will contain an entry form that has fields (text boxes) where you can enter a movie title, genre, and year.</span></span> <span data-ttu-id="a99df-124">Страница будет выглядеть, как на следующей странице:</span><span class="sxs-lookup"><span data-stu-id="a99df-124">The page will look like this page:</span></span>

![Страница "Добавление фильма" в браузере](entering-data/_static/image1.png)

<span data-ttu-id="a99df-126">Текстовые поля будут `<input>` HTML-элементы, которые будут выглядеть, как в этой разметке:</span><span class="sxs-lookup"><span data-stu-id="a99df-126">The text boxes will be HTML `<input>` elements that will look like this markup:</span></span>

`<input type="text" name="genre" value="" />`

## <a name="creating-the-basic-entry-form"></a><span data-ttu-id="a99df-127">Создание базовой формы ввода</span><span class="sxs-lookup"><span data-stu-id="a99df-127">Creating the Basic Entry Form</span></span>

<span data-ttu-id="a99df-128">Создайте страницу с именем *аддмовие. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="a99df-128">Create a page named *AddMovie.cshtml*.</span></span>

<span data-ttu-id="a99df-129">Замените содержимое файла следующей разметкой.</span><span class="sxs-lookup"><span data-stu-id="a99df-129">Replace what's in the file with the following markup.</span></span> <span data-ttu-id="a99df-130">Перезаписать все; в ближайшее время вы добавите блок кода.</span><span class="sxs-lookup"><span data-stu-id="a99df-130">Overwrite everything; you'll add a code block at the top shortly.</span></span>

[!code-cshtml[Main](entering-data/samples/sample1.cshtml)]

<span data-ttu-id="a99df-131">В этом примере показан типичный код HTML для создания формы.</span><span class="sxs-lookup"><span data-stu-id="a99df-131">This example shows typical HTML for creating a form.</span></span> <span data-ttu-id="a99df-132">В нем используются элементы `<input>` для текстовых полей и для кнопки отправки.</span><span class="sxs-lookup"><span data-stu-id="a99df-132">It uses `<input>` elements for the text boxes and for the submit button.</span></span> <span data-ttu-id="a99df-133">Заголовки для текстовых полей создаются с помощью стандартных элементов `<label>`.</span><span class="sxs-lookup"><span data-stu-id="a99df-133">The captions for the text boxes are created by using standard `<label>` elements.</span></span> <span data-ttu-id="a99df-134">Элементы `<fieldset>` и `<legend>` помещают привлекательную рамку вокруг формы.</span><span class="sxs-lookup"><span data-stu-id="a99df-134">The `<fieldset>` and `<legend>` elements put a nice box around the form.</span></span>

<span data-ttu-id="a99df-135">Обратите внимание, что на этой странице элемент `<form>` использует `post` в качестве значения атрибута `method`.</span><span class="sxs-lookup"><span data-stu-id="a99df-135">Notice that in this page, the `<form>` element uses `post` as the value for the `method` attribute.</span></span> <span data-ttu-id="a99df-136">В предыдущем руководстве вы создали форму, в которой использовался метод `get`.</span><span class="sxs-lookup"><span data-stu-id="a99df-136">In the previous tutorial, you created a form that used the `get` method.</span></span> <span data-ttu-id="a99df-137">Это верно, поскольку хотя в форме отправлены значения на сервер, запрос не сделал никаких изменений.</span><span class="sxs-lookup"><span data-stu-id="a99df-137">That was correct, because although the form submitted values to the server, the request did not make any changes.</span></span> <span data-ttu-id="a99df-138">Все это делало выборку данных различными способами.</span><span class="sxs-lookup"><span data-stu-id="a99df-138">All it did was fetch data in different ways.</span></span> <span data-ttu-id="a99df-139">Однако на этой странице *будут* внесены изменения — вы добавите новые записи базы данных.</span><span class="sxs-lookup"><span data-stu-id="a99df-139">However, in this page you *will* make changes—you're going to add new database records.</span></span> <span data-ttu-id="a99df-140">Поэтому в этой форме должен использоваться метод `post`.</span><span class="sxs-lookup"><span data-stu-id="a99df-140">Therefore, this form should use the `post` method.</span></span> <span data-ttu-id="a99df-141">(Дополнительные сведения о различиях между операциями `GET` и `POST` см. в разделе[Получение, публикация и HTTP-команда безопасность глагола](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) в предыдущем руководстве.)</span><span class="sxs-lookup"><span data-stu-id="a99df-141">(For more about the difference between `GET` and `POST` operations, see the[GET, POST, and HTTP Verb Safety](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the previous tutorial.)</span></span>

<span data-ttu-id="a99df-142">Обратите внимание, что каждое текстовое поле содержит элемент `name` (`title`, `genre`, `year`).</span><span class="sxs-lookup"><span data-stu-id="a99df-142">Note that each text box has a `name` element (`title`, `genre`, `year`).</span></span> <span data-ttu-id="a99df-143">Как было показано в предыдущем учебном курсе, эти имена важны, так как они должны иметь такие имена, чтобы вы могли получить входные данные пользователя позже.</span><span class="sxs-lookup"><span data-stu-id="a99df-143">As you saw in the previous tutorial, these names are important because you must have those names so you can get the user's input later.</span></span> <span data-ttu-id="a99df-144">Можно использовать любые имена.</span><span class="sxs-lookup"><span data-stu-id="a99df-144">You can use any names.</span></span> <span data-ttu-id="a99df-145">Полезно использовать понятные имена, которые помогут вспомнить, с какими данными вы работаете.</span><span class="sxs-lookup"><span data-stu-id="a99df-145">It's helpful to use meaningful names that help you remember what data you're working with.</span></span>

<span data-ttu-id="a99df-146">Атрибут `value` каждого элемента `<input>` содержит несколько фрагментов кода Razor (например, `Request.Form["title"]`).</span><span class="sxs-lookup"><span data-stu-id="a99df-146">The `value` attribute of each `<input>` element contains a bit of Razor code (for example, `Request.Form["title"]`).</span></span> <span data-ttu-id="a99df-147">В предыдущем учебном курсе вы узнали, как сохранить значение, введенное в текстовое поле (если оно имеется) после отправки формы.</span><span class="sxs-lookup"><span data-stu-id="a99df-147">You learned a version of this trick in the previous tutorial to preserve the value entered into the text box (if any) after the form has been submitted.</span></span>

## <a name="getting-the-form-values"></a><span data-ttu-id="a99df-148">Получение значений формы</span><span class="sxs-lookup"><span data-stu-id="a99df-148">Getting the Form Values</span></span>

<span data-ttu-id="a99df-149">Затем добавьте код, обрабатывающий форму.</span><span class="sxs-lookup"><span data-stu-id="a99df-149">Next, you add code that processes the form.</span></span> <span data-ttu-id="a99df-150">В структуре вы выполните следующие действия.</span><span class="sxs-lookup"><span data-stu-id="a99df-150">In outline, you'll do the following:</span></span>

1. <span data-ttu-id="a99df-151">Проверьте, выполняется ли отправка страницы (отправлена).</span><span class="sxs-lookup"><span data-stu-id="a99df-151">Check whether the page is being posted (was submitted).</span></span> <span data-ttu-id="a99df-152">Необходимо, чтобы код выполнялся только при нажатии пользователем кнопки, а не при первом запуске страницы.</span><span class="sxs-lookup"><span data-stu-id="a99df-152">You want to your code to run only when users have clicked the button, not when the page first runs.</span></span>
2. <span data-ttu-id="a99df-153">Получение значений, вводимых пользователем в текстовые поля.</span><span class="sxs-lookup"><span data-stu-id="a99df-153">Get the values that the user entered into the text boxes.</span></span> <span data-ttu-id="a99df-154">В этом случае, поскольку форма использует команду `POST`, вы получаете значения формы из коллекции `Request.Form`.</span><span class="sxs-lookup"><span data-stu-id="a99df-154">In this case, because the form is using the `POST` verb, you get the form values from the `Request.Form` collection.</span></span>
3. <span data-ttu-id="a99df-155">Вставьте значения в качестве новой записи в таблицу базы данных *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="a99df-155">Insert the values as a new record in the *Movies* database table.</span></span>

<span data-ttu-id="a99df-156">В верхней части файла добавьте следующий код:</span><span class="sxs-lookup"><span data-stu-id="a99df-156">At the top of the file, add the following code:</span></span>

[!code-cshtml[Main](entering-data/samples/sample2.cshtml)]

<span data-ttu-id="a99df-157">Первые несколько строк создают переменные (`title`, `genre`и `year`) для хранения значений из текстовых полей.</span><span class="sxs-lookup"><span data-stu-id="a99df-157">The first few lines create variables (`title`, `genre`, and `year`) to hold the values from the text boxes.</span></span> <span data-ttu-id="a99df-158">Строка `if(IsPost)` гарантирует, что переменные задаются *только* при нажатии пользователем кнопки **Добавить фильм** , т. е. при публикации формы.</span><span class="sxs-lookup"><span data-stu-id="a99df-158">The line `if(IsPost)` makes sure that the variables are set *only* when users click the **Add Movie** button — that is, when the form has been posted.</span></span>

<span data-ttu-id="a99df-159">Как было показано в предыдущем учебном курсе, значение текстового поля можно получить с помощью выражения, например `Request.Form["name"]`, где *Name* — это имя элемента `<input>`.</span><span class="sxs-lookup"><span data-stu-id="a99df-159">As you saw in an earlier tutorial, you get the value of a text box by using an expression like `Request.Form["name"]`, where *name* is the name of the `<input>` element.</span></span>

<span data-ttu-id="a99df-160">Имена переменных (`title`, `genre`и `year`) являются произвольными.</span><span class="sxs-lookup"><span data-stu-id="a99df-160">The names of the variables (`title`, `genre`, and `year`) are arbitrary.</span></span> <span data-ttu-id="a99df-161">Как и имена, назначаемые элементам `<input>`, их можно вызывать любым из них.</span><span class="sxs-lookup"><span data-stu-id="a99df-161">Like the names that you assign to `<input>` elements, you can call them anything you like.</span></span> <span data-ttu-id="a99df-162">(Имена переменных не обязательно должны совпадать с атрибутами name элементов `<input>` в форме.) Но, как и в случае с элементами `<input>`, рекомендуется использовать имена переменных, отражающие данные, которые они содержат.</span><span class="sxs-lookup"><span data-stu-id="a99df-162">(The names of the variables don't have to match the name attributes of `<input>` elements on the form.) But as with the `<input>` elements, it's a good idea to use variable names that reflect the data that they contain.</span></span> <span data-ttu-id="a99df-163">При написании кода последовательные имена облегчают запоминание данных, с которыми вы работаете.</span><span class="sxs-lookup"><span data-stu-id="a99df-163">When you write code, consistent names make it easier for you to remember what data you're working with.</span></span>

## <a name="adding-data-to-the-database"></a><span data-ttu-id="a99df-164">Добавление данных в базу данных</span><span class="sxs-lookup"><span data-stu-id="a99df-164">Adding Data to the Database</span></span>

<span data-ttu-id="a99df-165">В только что добавленном блоке *кода в* закрывающей фигурной скобке (`}`) блока `if` (не только внутри блока кода) добавьте следующий код:</span><span class="sxs-lookup"><span data-stu-id="a99df-165">In the code block you just added, just *inside* the closing brace ( `}` ) of the `if` block (not just inside the code block), add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample3.cs)]

<span data-ttu-id="a99df-166">Этот пример похож на код, использованный в предыдущем руководстве для выборки и вывода данных.</span><span class="sxs-lookup"><span data-stu-id="a99df-166">This example is similar to the code you used in a previous tutorial to fetch and display data.</span></span> <span data-ttu-id="a99df-167">Строка, которая начинается с `db =`, открывает базу данных, как и ранее, а следующая строка определяет инструкцию SQL снова, как показано выше.</span><span class="sxs-lookup"><span data-stu-id="a99df-167">The line that starts with `db =` opens the database, like before, and the next line defines a SQL statement, again as you saw before.</span></span> <span data-ttu-id="a99df-168">Однако на этот раз он определяет инструкцию SQL `Insert Into`.</span><span class="sxs-lookup"><span data-stu-id="a99df-168">However, this time it defines a SQL `Insert Into` statement.</span></span> <span data-ttu-id="a99df-169">В следующем примере показан общий синтаксис оператора `Insert Into`:</span><span class="sxs-lookup"><span data-stu-id="a99df-169">The following example shows the general syntax of the `Insert Into` statement:</span></span>

`INSERT INTO table (column1, column2, column3, ...) VALUES (value1, value2, value3, ...)`

<span data-ttu-id="a99df-170">Иными словами, вы указываете таблицу для вставки, затем перечислите столбцы для вставки, а затем перечислите значения для вставки.</span><span class="sxs-lookup"><span data-stu-id="a99df-170">In other words, you specify the table to insert into, then list the columns to insert into, and then list the values to insert.</span></span> <span data-ttu-id="a99df-171">(Как отмечалось ранее, SQL не учитывает регистр, но некоторые люди заменяют ключевые слова, чтобы упростить чтение команды.)</span><span class="sxs-lookup"><span data-stu-id="a99df-171">(As noted before, SQL is not case sensitive but some people capitalize the keywords to make it easier to read the command.)</span></span>

<span data-ttu-id="a99df-172">Столбцы, в которые вы вставляете, уже указаны в команде — `(Title, Genre, Year)`.</span><span class="sxs-lookup"><span data-stu-id="a99df-172">The columns that you're inserting into are already listed in the command — `(Title, Genre, Year)`.</span></span> <span data-ttu-id="a99df-173">Интересной частью является получение значений из текстовых полей в `VALUES` части команды.</span><span class="sxs-lookup"><span data-stu-id="a99df-173">The interesting part is how you get the values from the text boxes into the `VALUES` part of the command.</span></span> <span data-ttu-id="a99df-174">Вместо фактических значений отображаются `@0`, `@1`и `@2`, которые являются заполнителями курса.</span><span class="sxs-lookup"><span data-stu-id="a99df-174">Instead of actual values, you see `@0`, `@1`, and `@2`, which are of course placeholders.</span></span> <span data-ttu-id="a99df-175">При выполнении команды (в строке `db.Execute`) передаются значения, полученные из текстовых полей.</span><span class="sxs-lookup"><span data-stu-id="a99df-175">When you run the command (on the `db.Execute` line), you pass the values that you got from the text boxes.</span></span>

<span data-ttu-id="a99df-176">**Важно!**</span><span class="sxs-lookup"><span data-stu-id="a99df-176">**Important!**</span></span> <span data-ttu-id="a99df-177">Помните, что единственным способом включения данных, вводимых пользователем в режиме «в сети» в инструкции SQL, является использование заполнителей, как показано здесь (`VALUES(@0, @1, @2)`).</span><span class="sxs-lookup"><span data-stu-id="a99df-177">Remember that the only way you should ever include data entered online by a user in a SQL statement is to use placeholders, as you see here (`VALUES(@0, @1, @2)`).</span></span> <span data-ttu-id="a99df-178">При объединении вводимых пользователем данных в инструкцию SQL вы открываете атаки путем внедрения кода SQL, как описано в разделе [основы форм в веб-страницы ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251581) (предыдущее руководство).</span><span class="sxs-lookup"><span data-stu-id="a99df-178">If you concatenate user input into a SQL statement, you open yourself to a SQL injection attack, as explained in [Form Basics in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581) (the previous tutorial).</span></span>

<span data-ttu-id="a99df-179">По-прежнему внутри блока `if` добавьте следующую строку после строки `db.Execute`:</span><span class="sxs-lookup"><span data-stu-id="a99df-179">Still inside the `if` block, add the following line after the `db.Execute` line:</span></span>

[!code-css[Main](entering-data/samples/sample4.css)]

<span data-ttu-id="a99df-180">После вставки нового фильма в базу данных эта строка перемещает вас (перенаправление) на страницу *фильмов* , чтобы можно было увидеть только что введенный фильм.</span><span class="sxs-lookup"><span data-stu-id="a99df-180">After the new movie has been inserted into the database, this line jumps you (redirects) to the *Movies* page so you can see the movie you just entered.</span></span> <span data-ttu-id="a99df-181">Оператор `~` означает "корень веб-сайта".</span><span class="sxs-lookup"><span data-stu-id="a99df-181">The `~` operator means "root of the website."</span></span> <span data-ttu-id="a99df-182">(Оператор `~` работает только на страницах ASP.NET, а не в HTML.)</span><span class="sxs-lookup"><span data-stu-id="a99df-182">(The `~` operator works only in ASP.NET pages, not in HTML generally.)</span></span>

<span data-ttu-id="a99df-183">Полный блок кода выглядит как в следующем примере:</span><span class="sxs-lookup"><span data-stu-id="a99df-183">The complete code block looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample5.cshtml)]

## <a name="testing-the-insert-command-so-far"></a><span data-ttu-id="a99df-184">Тестирование команды INSERT (до сих пор)</span><span class="sxs-lookup"><span data-stu-id="a99df-184">Testing the Insert Command (So Far)</span></span>

<span data-ttu-id="a99df-185">Вы еще не сделали этого, но теперь пора протестировать.</span><span class="sxs-lookup"><span data-stu-id="a99df-185">You're not done yet, but now is a good time to test.</span></span>

<span data-ttu-id="a99df-186">В древовидном представлении файлов в WebMatrix щелкните правой кнопкой мыши страницу *аддмовие. cshtml* и выберите пункт **запустить в браузере**.</span><span class="sxs-lookup"><span data-stu-id="a99df-186">In the tree view of files in WebMatrix, right-click the *AddMovie.cshtml* page and then click **Launch in browser**.</span></span>

![Страница "Добавление фильма" в браузере](entering-data/_static/image2.png)

<span data-ttu-id="a99df-188">(Если вы работаете с другой страницей в браузере, убедитесь, что URL-адрес имеет `http://localhost:nnnnn/AddMovie`), где *nnnnn* — это номер порта, который вы используете.)</span><span class="sxs-lookup"><span data-stu-id="a99df-188">(If you end up with a different page in the browser, make sure that the URL is `http://localhost:nnnnn/AddMovie`), where *nnnnn* is the port number that you're using.)</span></span>

<span data-ttu-id="a99df-189">Вы получаете страницу ошибки?</span><span class="sxs-lookup"><span data-stu-id="a99df-189">Did you get an error page?</span></span> <span data-ttu-id="a99df-190">Если да, внимательно прочтите его и убедитесь, что код выглядит точно так, как было указано ранее.</span><span class="sxs-lookup"><span data-stu-id="a99df-190">If so, read it carefully and make sure that the code looks exactly what was listed earlier.</span></span>

<span data-ttu-id="a99df-191">Введите фильм в форме &mdash; например, используйте "Кане", "драма" и "1941".</span><span class="sxs-lookup"><span data-stu-id="a99df-191">Enter a movie in the form &mdash; for example, use "Citizen Kane", "Drama", and "1941".</span></span> <span data-ttu-id="a99df-192">(Или любой другой.) Затем щелкните **Добавить фильм**.</span><span class="sxs-lookup"><span data-stu-id="a99df-192">(Or whatever.) Then click **Add Movie**.</span></span>

<span data-ttu-id="a99df-193">Если все работает правильно, вы будете перенаправлены на страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="a99df-193">If all goes well, you're redirected to the *Movies* page.</span></span> <span data-ttu-id="a99df-194">Убедитесь, что в списке указан новый фильм.</span><span class="sxs-lookup"><span data-stu-id="a99df-194">Make sure that your new movie is listed.</span></span>

![На странице фильмов показан недавно добавленный фильм](entering-data/_static/image3.png)

## <a name="validating-user-input"></a><span data-ttu-id="a99df-196">Проверка введенного пользователем</span><span class="sxs-lookup"><span data-stu-id="a99df-196">Validating User Input</span></span>

<span data-ttu-id="a99df-197">Вернитесь на страницу *аддмовие* или запустите ее снова.</span><span class="sxs-lookup"><span data-stu-id="a99df-197">Go back to the *AddMovie* page, or run it again.</span></span> <span data-ttu-id="a99df-198">Введите другой фильм, но на этот раз введите только заголовок &mdash; например, введите "Сингин' в дождя".</span><span class="sxs-lookup"><span data-stu-id="a99df-198">Enter another movie, but this time, enter only the title &mdash; for example, enter "Singin' in the Rain".</span></span> <span data-ttu-id="a99df-199">Затем щелкните **Добавить фильм**.</span><span class="sxs-lookup"><span data-stu-id="a99df-199">Then click **Add Movie**.</span></span>

<span data-ttu-id="a99df-200">Вы снова будете перенаправлены на страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="a99df-200">You're redirected to the *Movies* page again.</span></span> <span data-ttu-id="a99df-201">Новый фильм можно найти, но он является неполным.</span><span class="sxs-lookup"><span data-stu-id="a99df-201">You can find the new movie, but it's incomplete.</span></span>

![На странице фильмов отображается новый фильм, в котором отсутствуют некоторые значения](entering-data/_static/image4.png)

<span data-ttu-id="a99df-203">При создании таблицы *Movies* вы явно сказали, что ни одно из полей не может иметь значение null.</span><span class="sxs-lookup"><span data-stu-id="a99df-203">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="a99df-204">Здесь у вас есть форма для новых фильмов, и вы оставляете поля пустыми.</span><span class="sxs-lookup"><span data-stu-id="a99df-204">Here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="a99df-205">Это ошибка.</span><span class="sxs-lookup"><span data-stu-id="a99df-205">That's an error.</span></span>

<span data-ttu-id="a99df-206">В этом случае база данных не вызывает (или не *создает*) ошибку.</span><span class="sxs-lookup"><span data-stu-id="a99df-206">In this case, the database didn't actually raise (or *throw*) an error.</span></span> <span data-ttu-id="a99df-207">Вы не преддали жанр или год, поэтому код на странице *аддмовие* обрабатывает эти значения как так называемые *пустые строки*.</span><span class="sxs-lookup"><span data-stu-id="a99df-207">You didn't supply a genre or year, so the code in the *AddMovie* page treated those values as so-called *empty strings*.</span></span> <span data-ttu-id="a99df-208">При выполнении команды SQL `Insert Into` в полях жанр и year не было полезных данных, но они не были равны NULL.</span><span class="sxs-lookup"><span data-stu-id="a99df-208">When the SQL `Insert Into` command ran, the genre and year fields didn't have useful data in them, but they weren't null.</span></span>

<span data-ttu-id="a99df-209">Очевидно, что вы не хотите, чтобы пользователи вводили половину пустую информацию о фильме в базу данных.</span><span class="sxs-lookup"><span data-stu-id="a99df-209">Obviously, you don't want to let users enter half-empty movie information into the database.</span></span> <span data-ttu-id="a99df-210">Решением является проверка входных данных пользователя.</span><span class="sxs-lookup"><span data-stu-id="a99df-210">The solution is to validate the user's input.</span></span> <span data-ttu-id="a99df-211">Первоначально проверка позволит убедиться, что пользователь вводит значение для всех полей (то есть ни одно из них не содержит пустую строку).</span><span class="sxs-lookup"><span data-stu-id="a99df-211">Initially, the validation will simply make sure that the user has entered a value for all of the fields (that is, that none of them contains an empty string).</span></span>

> [!TIP]
> 
> <span data-ttu-id="a99df-212">**NULL и пустые строки**</span><span class="sxs-lookup"><span data-stu-id="a99df-212">**Null and Empty Strings**</span></span>
> 
> <span data-ttu-id="a99df-213">В программировании существует различие между разными понятиями "нет значения".</span><span class="sxs-lookup"><span data-stu-id="a99df-213">In programming, there's a distinction between different notions of "no value."</span></span> <span data-ttu-id="a99df-214">Как правило, значение равно *null* , если оно никогда не было задано или инициализировано каким-либо образом.</span><span class="sxs-lookup"><span data-stu-id="a99df-214">In general, a value is *null* if it has never been set or initialized in any way.</span></span> <span data-ttu-id="a99df-215">Напротив, переменная, которая принимает символьные данные (строки), может быть задана *пустой строкой*.</span><span class="sxs-lookup"><span data-stu-id="a99df-215">In contrast, a variable that expects character data (strings) can be set to an *empty string*.</span></span> <span data-ttu-id="a99df-216">В этом случае значение не равно null; просто явно задана строка символов, длина которых равна нулю.</span><span class="sxs-lookup"><span data-stu-id="a99df-216">In that case, the value is not null; it's just been explicitly set to a string of characters whose length is zero.</span></span> <span data-ttu-id="a99df-217">Эти две инструкции показывают разницу:</span><span class="sxs-lookup"><span data-stu-id="a99df-217">These two statements show the difference:</span></span>
> 
> [!code-csharp[Main](entering-data/samples/sample6.cs)]
> 
> <span data-ttu-id="a99df-218">Это немного сложнее, но важно отметить, что `null` представляет неопределенное состояние.</span><span class="sxs-lookup"><span data-stu-id="a99df-218">It's a little more complicated than that, but the important point is that `null` represents a sort of undetermined state.</span></span>
> 
> <span data-ttu-id="a99df-219">Теперь важно понимать, когда значение равно null, и когда это просто пустая строка.</span><span class="sxs-lookup"><span data-stu-id="a99df-219">Now and then it's important to understand exactly when a value is null and when it's just an empty string.</span></span> <span data-ttu-id="a99df-220">В коде для страницы *аддмовие* значения текстовых полей можно получить с помощью `Request.Form["title"]` и т. д.</span><span class="sxs-lookup"><span data-stu-id="a99df-220">In the code for the *AddMovie* page, you get the values of the text boxes by using `Request.Form["title"]` and so on.</span></span> <span data-ttu-id="a99df-221">При первом запуске страницы (перед нажатием кнопки) значение `Request.Form["title"]` равно null.</span><span class="sxs-lookup"><span data-stu-id="a99df-221">When the page first runs (before you click the button), the value of `Request.Form["title"]` is null.</span></span> <span data-ttu-id="a99df-222">Но при отправке формы `Request.Form["title"]` получает значение текстового поля `title`.</span><span class="sxs-lookup"><span data-stu-id="a99df-222">But when you submit the form, `Request.Form["title"]` gets the value of the `title` text box.</span></span> <span data-ttu-id="a99df-223">Это не очевидно, но пустое текстовое поле не равно null; в нем просто содержится пустая строка.</span><span class="sxs-lookup"><span data-stu-id="a99df-223">It's not obvious, but an empty text box is not null; it just has an empty string in it.</span></span> <span data-ttu-id="a99df-224">Таким образом, когда код выполняется в ответ на нажатие кнопки, `Request.Form["title"]` содержит в нем пустую строку.</span><span class="sxs-lookup"><span data-stu-id="a99df-224">So when the code runs in response to the button click, `Request.Form["title"]` has an empty string in it.</span></span>
> 
> <span data-ttu-id="a99df-225">Почему это различие важно?</span><span class="sxs-lookup"><span data-stu-id="a99df-225">Why is this distinction important?</span></span> <span data-ttu-id="a99df-226">При создании таблицы *Movies* вы явно сказали, что ни одно из полей не может иметь значение null.</span><span class="sxs-lookup"><span data-stu-id="a99df-226">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="a99df-227">Но здесь у вас есть форма для новых фильмов, и вы оставляете поля пустыми.</span><span class="sxs-lookup"><span data-stu-id="a99df-227">But here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="a99df-228">При попытке сохранения новых фильмов, которые не имеют значений для жанра или года, будет необходимо, чтобы база данных выглядела бы.</span><span class="sxs-lookup"><span data-stu-id="a99df-228">You would reasonably expect the database to complain when you tried to save new movies that didn't have values for genre or year.</span></span> <span data-ttu-id="a99df-229">Но это точка &mdash; даже если оставить эти текстовые поля пустыми, значения не будут равны NULL. они являются пустыми строками.</span><span class="sxs-lookup"><span data-stu-id="a99df-229">But that's the point &mdash; even if you leave those text boxes blank, the values aren't null; they're empty strings.</span></span> <span data-ttu-id="a99df-230">В результате вы можете сохранять новые фильмы в базе данных, если эти столбцы пусты &mdash; но не равны NULL!</span><span class="sxs-lookup"><span data-stu-id="a99df-230">As a result, you're able to save new movies to the database with these columns empty &mdash; but not null!</span></span> <span data-ttu-id="a99df-231">значения типа &mdash;.</span><span class="sxs-lookup"><span data-stu-id="a99df-231">&mdash; values.</span></span> <span data-ttu-id="a99df-232">Поэтому необходимо убедиться, что пользователи не отправляют пустую строку, что можно сделать, проверив входные данные пользователя.</span><span class="sxs-lookup"><span data-stu-id="a99df-232">Therefore, you have to make sure that users don't submit an empty string, which you can do by validating the user's input.</span></span>

### <a name="the-validation-helper"></a><span data-ttu-id="a99df-233">Вспомогательный метод проверки</span><span class="sxs-lookup"><span data-stu-id="a99df-233">The Validation Helper</span></span>

<span data-ttu-id="a99df-234">Веб-страницы ASP.NET включает вспомогательную функцию &mdash; `Validation` вспомогательного &mdash; приложения, которую можно использовать, чтобы убедиться, что пользователи вводят данные, соответствующие вашим требованиям.</span><span class="sxs-lookup"><span data-stu-id="a99df-234">ASP.NET Web Pages includes a helper &mdash; the `Validation` helper &mdash; that you can use to make sure that users enter data that meets your requirements.</span></span> <span data-ttu-id="a99df-235">`Validation` вспомогательное приложение — это один из встроенных средств поддержки веб-страницы ASP.NET, поэтому вам не нужно устанавливать его как пакет с помощью NuGet, так как вы установили вспомогательный модуль граватаров в предыдущем руководстве.</span><span class="sxs-lookup"><span data-stu-id="a99df-235">The `Validation` helper is one of the helpers that's built in to ASP.NET Web Pages, so you don't have to install it as a package by using NuGet, the way you installed the Gravatar helper in an earlier tutorial.</span></span>

<span data-ttu-id="a99df-236">Чтобы проверить входные данные пользователя, выполните следующие действия.</span><span class="sxs-lookup"><span data-stu-id="a99df-236">To validate the user's input, you'll do the following:</span></span>

- <span data-ttu-id="a99df-237">Используйте код, чтобы указать, что необходимо запрашивать значения в текстовых полях на странице.</span><span class="sxs-lookup"><span data-stu-id="a99df-237">Use code to specify that you want to require values in the text boxes on the page.</span></span>
- <span data-ttu-id="a99df-238">Добавьте тест в код, чтобы сведения о фильме добавлялись в базу данных только в том случае, если все правильно проверяется.</span><span class="sxs-lookup"><span data-stu-id="a99df-238">Put a test into the code so that the movie information is added to the database only if everything validates properly.</span></span>
- <span data-ttu-id="a99df-239">Добавьте код в разметку для вывода сообщений об ошибках.</span><span class="sxs-lookup"><span data-stu-id="a99df-239">Add code into the markup to display error messages.</span></span>

<span data-ttu-id="a99df-240">В блоке кода на странице *аддмовие* , расположенном в верхней части до объявления переменных, добавьте следующий код:</span><span class="sxs-lookup"><span data-stu-id="a99df-240">In the code block in the *AddMovie* page, right up at the top before the variable declarations, add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample7.cs)]

<span data-ttu-id="a99df-241">Вы вызываете `Validation.RequireField` один раз для каждого поля (`<input>` элемента), где требуется указать запись.</span><span class="sxs-lookup"><span data-stu-id="a99df-241">You call `Validation.RequireField` once for each field (`<input>` element) where you want to require an entry.</span></span> <span data-ttu-id="a99df-242">Можно также добавить пользовательское сообщение об ошибке для каждого вызова, как показано здесь.</span><span class="sxs-lookup"><span data-stu-id="a99df-242">You can also add a custom error message for each call, like you see here.</span></span> <span data-ttu-id="a99df-243">(Мы изменяем сообщения только для того, чтобы продемонстрировать, что вы можете вставить в него все, что угодно.)</span><span class="sxs-lookup"><span data-stu-id="a99df-243">(We varied the messages just to show that you can put anything you like there.)</span></span>

<span data-ttu-id="a99df-244">Если возникла проблема, необходимо предотвратить вставку новых сведений о фильме в базу данных.</span><span class="sxs-lookup"><span data-stu-id="a99df-244">If there's a problem, you want to prevent the new movie information from being inserted into the database.</span></span> <span data-ttu-id="a99df-245">В блоке `if(IsPost)` используйте `&&` (логическое и), чтобы добавить еще одно условие, которое проверяет `Validation.IsValid()`.</span><span class="sxs-lookup"><span data-stu-id="a99df-245">In the `if(IsPost)` block, use `&&` (logical AND) to add another condition that tests `Validation.IsValid()`.</span></span> <span data-ttu-id="a99df-246">Когда все будет готово, весь блок `if(IsPost)` будет выглядеть следующим образом:</span><span class="sxs-lookup"><span data-stu-id="a99df-246">When you're done, the whole `if(IsPost)` block looks like this code:</span></span>

[!code-csharp[Main](entering-data/samples/sample8.cs)]

<span data-ttu-id="a99df-247">Если при проверке любого из полей, зарегистрированных с помощью вспомогательного метода `Validation`, возникает ошибка проверки, метод `Validation.IsValid` возвращает значение false.</span><span class="sxs-lookup"><span data-stu-id="a99df-247">If there's a validation error with any of the fields that you registered by using the `Validation` helper, the `Validation.IsValid` method returns false.</span></span> <span data-ttu-id="a99df-248">В этом случае ни один из кода в этом блоке не будет выполняться, поэтому в базу данных не будут вставлены недопустимые записи фильмов.</span><span class="sxs-lookup"><span data-stu-id="a99df-248">And in that case, none of the code in that block will run, so no invalid movie entries will be inserted into the database.</span></span> <span data-ttu-id="a99df-249">И, конечно, вы не будете перенаправлены на страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="a99df-249">And of course you're not redirected to the *Movies* page.</span></span>

<span data-ttu-id="a99df-250">Полный блок кода, включая код проверки, теперь выглядит как в следующем примере:</span><span class="sxs-lookup"><span data-stu-id="a99df-250">The complete code block, including the validation code, now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample9.cshtml?highlight=10)]

## <a name="displaying-validation-errors"></a><span data-ttu-id="a99df-251">Отображение ошибок проверки</span><span class="sxs-lookup"><span data-stu-id="a99df-251">Displaying Validation Errors</span></span>

<span data-ttu-id="a99df-252">Последний шаг — отображение сообщений об ошибках.</span><span class="sxs-lookup"><span data-stu-id="a99df-252">The last step is to display any error messages.</span></span> <span data-ttu-id="a99df-253">Можно отображать отдельные сообщения для каждой ошибки проверки, а также отображать сводку или и то, и другое.</span><span class="sxs-lookup"><span data-stu-id="a99df-253">You can display individual messages for each validation error, or you can display a summary, or both.</span></span> <span data-ttu-id="a99df-254">В рамках этого руководства вы выполните оба действия, чтобы увидеть, как это работает.</span><span class="sxs-lookup"><span data-stu-id="a99df-254">For this tutorial, you'll do both so that you can see how it works.</span></span>

<span data-ttu-id="a99df-255">Рядом с каждым проверяемым элементом `<input>` вызовите метод `Html.ValidationMessage` и передайте ему имя проверяемого элемента `<input>`.</span><span class="sxs-lookup"><span data-stu-id="a99df-255">Next to each `<input>` element that you're validating, call the `Html.ValidationMessage` method and pass it the name of the `<input>` element you're validating.</span></span> <span data-ttu-id="a99df-256">Метод `Html.ValidationMessage` помещается непосредственно в то место, где должно отображаться сообщение об ошибке.</span><span class="sxs-lookup"><span data-stu-id="a99df-256">You put the `Html.ValidationMessage` method right where you want the error message to appear.</span></span> <span data-ttu-id="a99df-257">При выполнении страницы метод `Html.ValidationMessage` визуализирует элемент `<span>`, в котором произойдет ошибка проверки.</span><span class="sxs-lookup"><span data-stu-id="a99df-257">When the page runs, the `Html.ValidationMessage` method renders a `<span>` element where the validation error will go.</span></span> <span data-ttu-id="a99df-258">(Если ошибок нет, элемент `<span>` отображается, но в нем нет текста.)</span><span class="sxs-lookup"><span data-stu-id="a99df-258">(If there's no error, the `<span>` element is rendered, but there's no text in it.)</span></span>

<span data-ttu-id="a99df-259">Измените разметку на странице, чтобы она включала метод `Html.ValidationMessage` для каждого из трех элементов `<input>` на странице, как показано в следующем примере:</span><span class="sxs-lookup"><span data-stu-id="a99df-259">Change the markup in the page so that it includes an `Html.ValidationMessage` method for each of the three `<input>` elements on the page, like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample10.cshtml?highlight=3,8,13)]

<span data-ttu-id="a99df-260">Чтобы увидеть, как работает сводка, добавьте следующую разметку и код сразу после элемента `<h1>Add a Movie</h1>` на странице:</span><span class="sxs-lookup"><span data-stu-id="a99df-260">To see how the summary works, also add the following markup and code right after the `<h1>Add a Movie</h1>` element on the page:</span></span>

[!code-cshtml[Main](entering-data/samples/sample11.cshtml)]

<span data-ttu-id="a99df-261">По умолчанию метод `Html.ValidationSummary` отображает все сообщения проверки в списке (элемент `<ul>`, находящиеся внутри элемента `<div>`).</span><span class="sxs-lookup"><span data-stu-id="a99df-261">By default, the `Html.ValidationSummary` method displays all the validation messages in a list (a `<ul>` element that's inside a `<div>` element).</span></span> <span data-ttu-id="a99df-262">Как и в случае с методом `Html.ValidationMessage`, разметка для сводки проверки всегда отображается; Если ошибок нет, элементы списка не отображаются.</span><span class="sxs-lookup"><span data-stu-id="a99df-262">As with the `Html.ValidationMessage` method, the markup for the validation summary is always rendered; if there are no errors, no list items are rendered.</span></span>

<span data-ttu-id="a99df-263">Сводка может быть альтернативным способом отобразить сообщения проверки вместо использования метода `Html.ValidationMessage` для вывода каждой ошибки, относящейся к определенному полю.</span><span class="sxs-lookup"><span data-stu-id="a99df-263">The summary can be an alternative way to display validation messages instead of by using the `Html.ValidationMessage` method to display each field-specific error.</span></span> <span data-ttu-id="a99df-264">Также можно использовать как сводку, так и сведения.</span><span class="sxs-lookup"><span data-stu-id="a99df-264">Or you can use both a summary and the details.</span></span> <span data-ttu-id="a99df-265">Или можно использовать метод `Html.ValidationSummary` для вывода общей ошибки, а затем использовать отдельные вызовы `Html.ValidationMessage` для вывода сведений.</span><span class="sxs-lookup"><span data-stu-id="a99df-265">Or you can use the `Html.ValidationSummary` method to display a generic error and then use individual `Html.ValidationMessage` calls to display details.</span></span>

<span data-ttu-id="a99df-266">Теперь полная страница выглядит как в следующем примере:</span><span class="sxs-lookup"><span data-stu-id="a99df-266">The complete page now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample12.cshtml)]

<span data-ttu-id="a99df-267">Вот и все.</span><span class="sxs-lookup"><span data-stu-id="a99df-267">That's it.</span></span> <span data-ttu-id="a99df-268">Теперь можно протестировать страницу, добавив фильм, но выполнив одно или несколько полей.</span><span class="sxs-lookup"><span data-stu-id="a99df-268">You can now test the page by adding a movie but leaving out one or more of the fields.</span></span> <span data-ttu-id="a99df-269">При этом отображается следующее сообщение об ошибке:</span><span class="sxs-lookup"><span data-stu-id="a99df-269">When you do, you see the following error display:</span></span>

![Страница добавления фильма с сообщениями об ошибках проверки](entering-data/_static/image5.png)

## <a name="styling-the-validation-error-messages"></a><span data-ttu-id="a99df-271">Определение стиля сообщений об ошибках проверки</span><span class="sxs-lookup"><span data-stu-id="a99df-271">Styling the Validation Error Messages</span></span>

<span data-ttu-id="a99df-272">Видно, что есть сообщения об ошибках, но они не слишком хорошо выделены.</span><span class="sxs-lookup"><span data-stu-id="a99df-272">You can see that there are error messages, but they don't really stand out very well.</span></span> <span data-ttu-id="a99df-273">Однако существует простой способ стиля сообщений об ошибках.</span><span class="sxs-lookup"><span data-stu-id="a99df-273">There's an easy way to style the error messages, though.</span></span>

<span data-ttu-id="a99df-274">Чтобы задать стиль отдельных сообщений об ошибках, отображаемых `Html.ValidationMessage`, создайте класс стиля CSS с именем `field-validation-error`.</span><span class="sxs-lookup"><span data-stu-id="a99df-274">To style the individual error messages that are displayed by `Html.ValidationMessage`, create a CSS style class named `field-validation-error`.</span></span> <span data-ttu-id="a99df-275">Чтобы определить вид сводки проверки, создайте класс стиля CSS с именем `validation-summary-errors`.</span><span class="sxs-lookup"><span data-stu-id="a99df-275">To define the look for the validation summary, create a CSS style class named `validation-summary-errors`.</span></span>

<span data-ttu-id="a99df-276">Чтобы увидеть, как работает этот метод, добавьте элемент `<style>` в раздел `<head>` страницы.</span><span class="sxs-lookup"><span data-stu-id="a99df-276">To see how this technique works, add a `<style>` element inside the `<head>` section of the page.</span></span> <span data-ttu-id="a99df-277">Затем определите классы стилей с именами `field-validation-error` и `validation-summary-errors`, которые содержат следующие правила.</span><span class="sxs-lookup"><span data-stu-id="a99df-277">Then define style classes named `field-validation-error` and `validation-summary-errors` that contain the following rules:</span></span>

[!code-cshtml[Main](entering-data/samples/sample13.cshtml?highlight=4-17)]

<span data-ttu-id="a99df-278">Обычно можно разместить сведения о стилях в отдельном *CSS* -файле, но для простоты их можно разместить на странице сейчас.</span><span class="sxs-lookup"><span data-stu-id="a99df-278">Normally you'd probably put style information into a separate *.css* file, but for simplicity you can put them in the page for now.</span></span> <span data-ttu-id="a99df-279">(Далее в этом руководстве вы перенесите правила CSS в отдельный *CSS* -файл.)</span><span class="sxs-lookup"><span data-stu-id="a99df-279">(Later in this tutorial set, you'll move the CSS rules to a separate *.css* file.)</span></span>

<span data-ttu-id="a99df-280">При возникновении ошибки проверки метод `Html.ValidationMessage` визуализирует элемент `<span>`, который содержит `class="field-validation-error"`.</span><span class="sxs-lookup"><span data-stu-id="a99df-280">If there's a validation error, the `Html.ValidationMessage` method renders a `<span>` element that includes `class="field-validation-error"`.</span></span> <span data-ttu-id="a99df-281">Добавив определение стиля для этого класса, можно настроить то, как выглядит сообщение.</span><span class="sxs-lookup"><span data-stu-id="a99df-281">By adding a style definition for that class, you can configure what the message looks like.</span></span> <span data-ttu-id="a99df-282">При наличии ошибок метод `ValidationSummary` динамически визуализирует `class="validation-summary-errors"`атрибута.</span><span class="sxs-lookup"><span data-stu-id="a99df-282">If there are errors, the `ValidationSummary` method likewise dynamically renders the attribute `class="validation-summary-errors"`.</span></span>

<span data-ttu-id="a99df-283">Снова запустите страницу и намеренно оставьте несколько полей.</span><span class="sxs-lookup"><span data-stu-id="a99df-283">Run the page again and deliberately leave out a couple of the fields.</span></span> <span data-ttu-id="a99df-284">Ошибки теперь более заметны.</span><span class="sxs-lookup"><span data-stu-id="a99df-284">The errors are now more noticeable.</span></span> <span data-ttu-id="a99df-285">(На самом деле они переработаны, но это просто покажет, что можно сделать.)</span><span class="sxs-lookup"><span data-stu-id="a99df-285">(In fact, they're overdone, but that's just to show what you can do.)</span></span>

![Страница добавления фильма с показанными ошибками проверки](entering-data/_static/image6.png)

## <a name="adding-a-link-to-the-movies-page"></a><span data-ttu-id="a99df-287">Добавление ссылки на страницу фильмов</span><span class="sxs-lookup"><span data-stu-id="a99df-287">Adding a Link to the Movies Page</span></span>

<span data-ttu-id="a99df-288">Одним из заключительных этапов является упрощение перехода на страницу *аддмовие* из исходного списка фильмов.</span><span class="sxs-lookup"><span data-stu-id="a99df-288">One final step is to make it convenient to get to the *AddMovie* page from the original movie listing.</span></span>

<span data-ttu-id="a99df-289">Снова откройте страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="a99df-289">Open the *Movies* page again.</span></span> <span data-ttu-id="a99df-290">После закрывающего тега `</div>`, который следует за вспомогательным модулем `WebGrid`, добавьте следующую разметку:</span><span class="sxs-lookup"><span data-stu-id="a99df-290">After the closing `</div>` tag that follows the `WebGrid` helper, add the following markup:</span></span>

[!code-cshtml[Main](entering-data/samples/sample14.cshtml)]

<span data-ttu-id="a99df-291">Как было показано ранее, ASP.NET интерпретирует оператор `~` как корень веб-сайта.</span><span class="sxs-lookup"><span data-stu-id="a99df-291">As you saw before, ASP.NET interprets the `~` operator as the root of the website.</span></span> <span data-ttu-id="a99df-292">Не нужно использовать оператор `~`; можно использовать разметку `<a href="./AddMovie">Add a movie</a>` или какой-либо другой способ определения пути, который понимается в HTML.</span><span class="sxs-lookup"><span data-stu-id="a99df-292">You don't have to use the `~` operator; you could use the markup `<a href="./AddMovie">Add a movie</a>` or some other way to define the path that HTML understands.</span></span> <span data-ttu-id="a99df-293">Но оператор `~` является хорошим общим подходом при создании ссылок на страницы Razor, так как делает сайт более гибким — при перемещении текущей страницы во вложенную папку ссылка по-прежнему будет переходить на страницу *аддмовие* .</span><span class="sxs-lookup"><span data-stu-id="a99df-293">But the `~` operator is a good general approach when you create links for Razor pages, because it makes the site more flexible — if you move the current page to a subfolder, the link will still go to the *AddMovie* page.</span></span> <span data-ttu-id="a99df-294">(Помните, что оператор `~` работает только в *CSHTML* -страницах.</span><span class="sxs-lookup"><span data-stu-id="a99df-294">(Remember that the `~` operator only works in *.cshtml* pages.</span></span> <span data-ttu-id="a99df-295">ASP.NET понимает его, но не является стандартным HTML.)</span><span class="sxs-lookup"><span data-stu-id="a99df-295">ASP.NET understands it, but it's not standard HTML.)</span></span>

<span data-ttu-id="a99df-296">Когда все будет готово, запустите страницу *фильмов* .</span><span class="sxs-lookup"><span data-stu-id="a99df-296">When you're done, run the *Movies* page.</span></span> <span data-ttu-id="a99df-297">Она будет выглядеть, как на следующей странице:</span><span class="sxs-lookup"><span data-stu-id="a99df-297">It will look like this page:</span></span>

![Страница фильмов со ссылкой на страницу "Добавление фильмов"](entering-data/_static/image7.png)

<span data-ttu-id="a99df-299">Щелкните ссылку **Добавить фильм** , чтобы убедиться, что он перейдет на страницу *аддмовие* .</span><span class="sxs-lookup"><span data-stu-id="a99df-299">Click the **Add a movie** link to make sure that it goes to the *AddMovie* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="a99df-300">Далее</span><span class="sxs-lookup"><span data-stu-id="a99df-300">Coming Up Next</span></span>

<span data-ttu-id="a99df-301">В следующем руководстве вы узнаете, как разрешить пользователям изменять данные, которые уже находятся в базе данных.</span><span class="sxs-lookup"><span data-stu-id="a99df-301">In the next tutorial, you'll learn how to let users edit data that's already in the database.</span></span>

## <a name="complete-listing-for-addmovie-page"></a><span data-ttu-id="a99df-302">Полный список для страницы Аддмовие</span><span class="sxs-lookup"><span data-stu-id="a99df-302">Complete Listing for AddMovie Page</span></span>

[!code-cshtml[Main](entering-data/samples/sample15.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="a99df-303">Дополнительные ресурсы</span><span class="sxs-lookup"><span data-stu-id="a99df-303">Additional Resources</span></span>

- [<span data-ttu-id="a99df-304">Введение в веб-программирование ASP.NET с использованием синтаксиса Razor</span><span class="sxs-lookup"><span data-stu-id="a99df-304">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="a99df-305">[Инструкция SQL INSERT INTO](http://www.w3schools.com/sql/sql_insert.asp) на сайте W3Schools</span><span class="sxs-lookup"><span data-stu-id="a99df-305">[SQL INSERT INTO Statement](http://www.w3schools.com/sql/sql_insert.asp) on the W3Schools site</span></span>
- <span data-ttu-id="a99df-306">[Проверка вводимых пользователем данных на веб-страницы ASP.NET сайтах](https://go.microsoft.com/fwlink/?LinkId=253002).</span><span class="sxs-lookup"><span data-stu-id="a99df-306">[Validating User Input in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=253002).</span></span> <span data-ttu-id="a99df-307">Дополнительные сведения о работе с вспомогательным приложением `Validation`.</span><span class="sxs-lookup"><span data-stu-id="a99df-307">More information about working with the `Validation` helper.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="a99df-308">[Назад](form-basics.md)
> [Вперед](updating-data.md)</span><span class="sxs-lookup"><span data-stu-id="a99df-308">[Previous](form-basics.md)
[Next](updating-data.md)</span></span>
