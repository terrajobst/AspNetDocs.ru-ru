---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: Знакомство с веб-страниц ASP.NET — отображение данных | Документация Майкрософт
author: Rick-Anderson
description: Этом руководстве показано, как создать базу данных в WebMatrix и способ отображения данных базы данных на странице, при использовании веб-страниц ASP.NET (Razor). Предполагается y...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 5415913626eb063a4cb1013ba03857c130487f42
ms.sourcegitcommit: 0f1119340e4464720cfd16d0ff15764746ea1fea
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/09/2019
ms.locfileid: "59412181"
---
# <a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="9fdba-104">Знакомство с веб-страниц ASP.NET — отображение данных</span><span class="sxs-lookup"><span data-stu-id="9fdba-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>

<span data-ttu-id="9fdba-105">по [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="9fdba-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="9fdba-106">Этом руководстве показано, как создать базу данных в WebMatrix и способ отображения данных базы данных на странице, при использовании веб-страниц ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="9fdba-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="9fdba-107">Предполагается, вы выполнили рядов через [введение в программирование веб-страниц ASP.NET](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="9fdba-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="9fdba-108">Вы узнаете, как:</span><span class="sxs-lookup"><span data-stu-id="9fdba-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="9fdba-109">Как использовать средства WebMatrix для создания базы данных и таблицы базы данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="9fdba-110">Как использовать средства WebMatrix для добавления данных в базу данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="9fdba-111">Как отобразить данные из базы данных на странице.</span><span class="sxs-lookup"><span data-stu-id="9fdba-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="9fdba-112">Сведения о выполнении команды SQL в ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="9fdba-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="9fdba-113">Как настроить `WebGrid` вспомогательный для изменения способа отображения данных и добавления разбиение по страницам и сортировка.</span><span class="sxs-lookup"><span data-stu-id="9fdba-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="9fdba-114">Функции и технологии:</span><span class="sxs-lookup"><span data-stu-id="9fdba-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="9fdba-115">WebMatrix инструменты для баз данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-115">WebMatrix database tools.</span></span>
> - `WebGrid` <span data-ttu-id="9fdba-116">Вспомогательный метод.</span><span class="sxs-lookup"><span data-stu-id="9fdba-116">helper.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="9fdba-117">Что вы создадите</span><span class="sxs-lookup"><span data-stu-id="9fdba-117">What You'll Build</span></span>

<span data-ttu-id="9fdba-118">В предыдущем руководстве, были введены для веб-страниц ASP.NET (*.cshtml* файлы), основы синтаксиса Razor и вспомогательные функции.</span><span class="sxs-lookup"><span data-stu-id="9fdba-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="9fdba-119">В этом руководстве вы начнете создание собственно веб-приложения, которое будет использоваться для остальной части серии.</span><span class="sxs-lookup"><span data-stu-id="9fdba-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="9fdba-120">Приложение является приложением простой фильма, позволяет просматривать, добавлять, изменять и удалять сведения о фильмах.</span><span class="sxs-lookup"><span data-stu-id="9fdba-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="9fdba-121">Когда все будет готово к этому учебнику, вы сможете просматривать список фильмов, который выглядит, как эта страница:</span><span class="sxs-lookup"><span data-stu-id="9fdba-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![Отображение WebGrid, что включает в себя параметры, задайте имена класс CSS](displaying-data/_static/image1.png)

<span data-ttu-id="9fdba-123">Но сначала необходимо создать базу данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="9fdba-124">Очень краткое введение в базы данных</span><span class="sxs-lookup"><span data-stu-id="9fdba-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="9fdba-125">В этом учебнике приводятся только briefest Знакомство с базами данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="9fdba-126">Если у вас есть опыт базы данных, этот короткий раздел можно пропустить.</span><span class="sxs-lookup"><span data-stu-id="9fdba-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="9fdba-127">База данных содержит одну или несколько таблиц, содержащих сведения &mdash; например, таблицы, для клиентов, заказов и поставщиков или для учащихся, преподавателей, классы и оценок.</span><span class="sxs-lookup"><span data-stu-id="9fdba-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="9fdba-128">Структурно таблицу базы данных — электронной таблицы.</span><span class="sxs-lookup"><span data-stu-id="9fdba-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="9fdba-129">Представьте себе типичный адресной книги.</span><span class="sxs-lookup"><span data-stu-id="9fdba-129">Imagine a typical address book.</span></span> <span data-ttu-id="9fdba-130">Для каждой записи в адресной книге (то есть для каждого пользователя) у вас есть несколько блоков информации, такие как имя, Фамилия, адрес, адрес электронной почты и номер телефона.</span><span class="sxs-lookup"><span data-stu-id="9fdba-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Образец таблицы базы данных в виде простой таблицы](displaying-data/_static/image2.png)

<span data-ttu-id="9fdba-132">(Строки, иногда называются *записей*, и столбцы, иногда называются *поля*.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="9fdba-133">Для большинства таблиц базы данных она должна содержать столбец, содержащий уникальное значение, например номер заказчика, номер счета и т. д.</span><span class="sxs-lookup"><span data-stu-id="9fdba-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="9fdba-134">Это значение называется таблицы *первичный ключ*, и использовать его для идентификации каждой строки в таблице.</span><span class="sxs-lookup"><span data-stu-id="9fdba-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="9fdba-135">В примере идентификатор столбца является первичным ключом для адресной книги, показанный в предыдущем примере.</span><span class="sxs-lookup"><span data-stu-id="9fdba-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="9fdba-136">Большую часть работы, как на веб-приложений состоит из считывания информации из базы данных и его отображения на странице.</span><span class="sxs-lookup"><span data-stu-id="9fdba-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="9fdba-137">Вы также часто получать информацию от пользователей и добавить его к базе данных, или вы измените записей, которые уже находятся в базе данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="9fdba-138">(Мы обсудим все эти операции в этот набор.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="9fdba-139">Работа базы данных может быть невероятно сложной и может потребовать специальных знаний.</span><span class="sxs-lookup"><span data-stu-id="9fdba-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="9fdba-140">Для данного руководства Однако вам нужно изучить только основные понятия, которые все объясняются при переходе.</span><span class="sxs-lookup"><span data-stu-id="9fdba-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="9fdba-141">Создание базы данных</span><span class="sxs-lookup"><span data-stu-id="9fdba-141">Creating a Database</span></span>

<span data-ttu-id="9fdba-142">В WebMatrix имеются средства, которые упрощают создание базы данных и для создания таблиц в базе данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="9fdba-143">(Структура базы данных называется базы данных *схемы*.) Для данного руководства, вы создадите базу данных, имеет только одну таблицу, в &mdash; фильмов.</span><span class="sxs-lookup"><span data-stu-id="9fdba-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="9fdba-144">Откройте WebMatrix, если вы еще этого не сделали и откройте WebPagesMovies сайт, созданный в предыдущем учебном курсе.</span><span class="sxs-lookup"><span data-stu-id="9fdba-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="9fdba-145">В левой области щелкните **базы данных** рабочей области.</span><span class="sxs-lookup"><span data-stu-id="9fdba-145">In the left pane, click the **Database** workspace.</span></span>

![Вкладка WebMatrix базы данных рабочей области](displaying-data/_static/image3.png)

<span data-ttu-id="9fdba-147">Изменения ленты для отображения задач, относящиеся к базе данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="9fdba-148">На ленте щелкните **новую базу данных**.</span><span class="sxs-lookup"><span data-stu-id="9fdba-148">In the ribbon, click **New Database**.</span></span>

![«Новая база данных» кнопку на ленте WebMatrix](displaying-data/_static/image4.png)

<span data-ttu-id="9fdba-150">WebMatrix создает базу данных SQL Server CE ( *.sdf* файл), имеет то же имя в качестве сайта &mdash; *WebPagesMovies.sdf*.</span><span class="sxs-lookup"><span data-stu-id="9fdba-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="9fdba-151">(Это не здесь, но можно переименовать файл в любое название, до тех пор, пока он имеет *.sdf* расширение.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="9fdba-152">Создание таблицы</span><span class="sxs-lookup"><span data-stu-id="9fdba-152">Creating a Table</span></span>

<span data-ttu-id="9fdba-153">На ленте щелкните **новую таблицу**.</span><span class="sxs-lookup"><span data-stu-id="9fdba-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="9fdba-154">WebMatrix откроется в конструкторе таблиц в новой вкладке. (Новая таблица недоступен, если убедитесь, что новая база данных выбрана в представлении в виде дерева в левой части.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

![Кнопка «Новую таблицу» в ленте WebMatrix](displaying-data/_static/image5.png)

<span data-ttu-id="9fdba-156">В текстовом поле вверху (где водяной знак говорит: «Введите имя таблицы») введите «Movies».</span><span class="sxs-lookup"><span data-stu-id="9fdba-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Ввод имени таблицы в конструкторе базы данных WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="9fdba-158">Области под имя таблицы является определение отдельных столбцов.</span><span class="sxs-lookup"><span data-stu-id="9fdba-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="9fdba-159">Для *фильмы* таблицы в этом руководстве вы создадите только несколько столбцов: *Идентификатор*, *Title*, *жанр*, и *год*.</span><span class="sxs-lookup"><span data-stu-id="9fdba-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="9fdba-160">В **имя** введите «ID».</span><span class="sxs-lookup"><span data-stu-id="9fdba-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="9fdba-161">Введенное здесь активирует все элементы управления для нового столбца.</span><span class="sxs-lookup"><span data-stu-id="9fdba-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="9fdba-162">Вкладки **тип данных** а выберите **int**. Это значение указывает, что столбец Идентификаторов будет содержать целочисленные данные (числа).</span><span class="sxs-lookup"><span data-stu-id="9fdba-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="9fdba-163">Мы не задаю ее любой см. Здесь (гораздо), но можно использовать стандартные жесты Windows клавиатуры для перехода в этой сетке.</span><span class="sxs-lookup"><span data-stu-id="9fdba-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="9fdba-164">Например может переместиться между полями, вы можно просто начните ввод для выбора элемент в списке и так далее.</span><span class="sxs-lookup"><span data-stu-id="9fdba-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>


<span data-ttu-id="9fdba-165">Клавиши табуляции **значение по умолчанию** поле (то есть, оставьте пустым).</span><span class="sxs-lookup"><span data-stu-id="9fdba-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="9fdba-166">Вкладки **— первичный ключ** флажок и выберите его.</span><span class="sxs-lookup"><span data-stu-id="9fdba-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="9fdba-167">Этот параметр предписывает базе данных, *идентификатор* столбец будет содержать данные, которые определяют отдельные строки.</span><span class="sxs-lookup"><span data-stu-id="9fdba-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="9fdba-168">(То есть каждая строка будет иметь уникальное значение в столбца идентификатора, который можно использовать для поиска этой строки.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="9fdba-169">Выберите **Is Identity** параметр.</span><span class="sxs-lookup"><span data-stu-id="9fdba-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="9fdba-170">Этот параметр предписывает базе данных, он должен автоматически сгенерировать следующий последовательный номер для каждой новой строки.</span><span class="sxs-lookup"><span data-stu-id="9fdba-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="9fdba-171">( **Is Identity** работает параметр только в том случае, если вы выбрали также **— первичный ключ** параметр.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="9fdba-172">В следующей строке сетки щелкните или нажмите клавишу Tab дважды, чтобы завершить текущую строку.</span><span class="sxs-lookup"><span data-stu-id="9fdba-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="9fdba-173">Либо жестов сохраняет текущую строку и начинается следующий.</span><span class="sxs-lookup"><span data-stu-id="9fdba-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="9fdba-174">Обратите внимание, что **значение по умолчанию** столбец теперь говорит **Null**.</span><span class="sxs-lookup"><span data-stu-id="9fdba-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="9fdba-175">(Null значение по умолчанию для значения по умолчанию, так сказать.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="9fdba-176">Завершив определение нового **идентификатор** столбец, конструктор будет выглядеть на этом рисунке:</span><span class="sxs-lookup"><span data-stu-id="9fdba-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![Конструктор баз данных WebMatrix, определив идентификатор столбца для таблицы фильмы](displaying-data/_static/image7.png)

<span data-ttu-id="9fdba-178">Чтобы создать в следующий столбец, щелкните поле в **имя** столбца.</span><span class="sxs-lookup"><span data-stu-id="9fdba-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="9fdba-179">Введите «Title» для столбца, а затем выберите **nvarchar** для **тип данных** значение.</span><span class="sxs-lookup"><span data-stu-id="9fdba-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="9fdba-180">«Var» часть **nvarchar** сообщает базы данных, что данные для этого столбца будут строку, размер которых может меняться от записи к записи.</span><span class="sxs-lookup"><span data-stu-id="9fdba-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="9fdba-181">(Префикс «n» представляет «национальные», которое указывает, что поле может содержать символьные данные для любого алфавит или системы письма — то есть поле содержит данные в Юникоде.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="9fdba-182">При выборе **nvarchar**, откроется другое окно для ввода максимальную длину для поля.</span><span class="sxs-lookup"><span data-stu-id="9fdba-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="9fdba-183">Введите 50 на предположении, что не название фильма, которым вы будете работать в этом руководстве будут длиннее 50 символов.</span><span class="sxs-lookup"><span data-stu-id="9fdba-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="9fdba-184">Пропустить **значение по умолчанию** и очистить **Разрешить значения NULL** параметр.</span><span class="sxs-lookup"><span data-stu-id="9fdba-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="9fdba-185">Вы не хотите базы данных, фильмы для ввода в базу данных, на которых не установлена.</span><span class="sxs-lookup"><span data-stu-id="9fdba-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="9fdba-186">Когда все готово и перемещение на следующую строку, конструкторе выглядит как на этом рисунке:</span><span class="sxs-lookup"><span data-stu-id="9fdba-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![Конструктор баз данных WebMatrix после определения заголовка столбца для таблицы фильмы](displaying-data/_static/image8.png)

<span data-ttu-id="9fdba-188">Повторите эти действия, чтобы создать столбец с именем «Genre», за исключением длина, задайте для него значение просто 30.</span><span class="sxs-lookup"><span data-stu-id="9fdba-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="9fdba-189">Создание другого столбца с именем «Год».</span><span class="sxs-lookup"><span data-stu-id="9fdba-189">Create another column named "Year."</span></span> <span data-ttu-id="9fdba-190">Тип данных, выберите **nchar** (не **nvarchar**) и задающий длину равной 4.</span><span class="sxs-lookup"><span data-stu-id="9fdba-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="9fdba-191">Для года вы собираетесь использовать 4-значное число, например «1995» или «2010», поэтому не требуется столбец переменного размера.</span><span class="sxs-lookup"><span data-stu-id="9fdba-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="9fdba-192">Вот, как выглядит по завершении разработки:</span><span class="sxs-lookup"><span data-stu-id="9fdba-192">Here's what the finished design looks like:</span></span>

![Конструктор баз данных WebMatrix, определив все поля таблицы фильмы](displaying-data/_static/image9.png)

<span data-ttu-id="9fdba-194">Нажмите клавиши Ctrl + S, или нажмите кнопку **Сохранить** кнопку на панели инструментов быстрого доступа.</span><span class="sxs-lookup"><span data-stu-id="9fdba-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="9fdba-195">Закрытие вкладки, закройте конструктор баз данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="9fdba-196">Добавив некоторые данные примера</span><span class="sxs-lookup"><span data-stu-id="9fdba-196">Adding Some Example Data</span></span>

<span data-ttu-id="9fdba-197">Далее в этой серии руководств вы создадите страницу, где можно ввести в форме новых фильмов.</span><span class="sxs-lookup"><span data-stu-id="9fdba-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="9fdba-198">Теперь можно добавить некоторые примеры данных, которые затем можно отобразить на странице.</span><span class="sxs-lookup"><span data-stu-id="9fdba-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="9fdba-199">В **базы данных** рабочей области в WebMatrix, обратите внимание, что дерево, которое показывает, *.sdf* файл, созданный ранее.</span><span class="sxs-lookup"><span data-stu-id="9fdba-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="9fdba-200">Откройте узел нового *.sdf* , а затем откройте **таблиц** узла.</span><span class="sxs-lookup"><span data-stu-id="9fdba-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![WebMatrix базы данных рабочей области с деревом откройте таблицу фильмов](displaying-data/_static/image10.png)

<span data-ttu-id="9fdba-202">Щелкните правой кнопкой мыши **фильмы** узел и выберите **данных**.</span><span class="sxs-lookup"><span data-stu-id="9fdba-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="9fdba-203">WebMatrix открывает сетку, в котором можно ввести данные для *фильмы* таблицы:</span><span class="sxs-lookup"><span data-stu-id="9fdba-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Сетка запись базы данных в WebMatrix (пусто)](displaying-data/_static/image11.png)

<span data-ttu-id="9fdba-205">Нажмите кнопку **Title** столбца и введите «Когда Гарри выполнены Светлана».</span><span class="sxs-lookup"><span data-stu-id="9fdba-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="9fdba-206">Переместить **жанр** столбца (можно использовать клавишу Tab) и введите «Romantic комедия».</span><span class="sxs-lookup"><span data-stu-id="9fdba-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="9fdba-207">Переместить **год** столбца и введите «1989»:</span><span class="sxs-lookup"><span data-stu-id="9fdba-207">Move to the **Year** column and enter "1989":</span></span>

![Сетка запись базы данных в WebMatrix с одной записью](displaying-data/_static/image12.png)

<span data-ttu-id="9fdba-209">Нажмите клавишу ВВОД и WebMatrix сохраняет новый фильм.</span><span class="sxs-lookup"><span data-stu-id="9fdba-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="9fdba-210">Обратите внимание, что **идентификатор** столбца должна быть заполнена.</span><span class="sxs-lookup"><span data-stu-id="9fdba-210">Notice that the **ID** column has been filled in.</span></span>

![Сетка запись базы данных в WebMatrix с одной записи и автоматически созданный идентификатор](displaying-data/_static/image13.png)

<span data-ttu-id="9fdba-212">Введите другой фильма (например, «прошли с ветер», «Драма», «1939»).</span><span class="sxs-lookup"><span data-stu-id="9fdba-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="9fdba-213">Столбец идентификатора заполняется снова:</span><span class="sxs-lookup"><span data-stu-id="9fdba-213">The ID column is filled in again:</span></span>

![Сетка запись базы данных в WebMatrix с двух записей и автоматически сформированных идентификаторов](displaying-data/_static/image14.png)

<span data-ttu-id="9fdba-215">Введите третий фильма (например, «Ghostbusters», «Комедия»).</span><span class="sxs-lookup"><span data-stu-id="9fdba-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="9fdba-216">В качестве эксперимента, оставьте **год** столбца пустым и нажмите клавишу ВВОД.</span><span class="sxs-lookup"><span data-stu-id="9fdba-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="9fdba-217">Так как вы невыделенного **Разрешить значения NULL** параметра отображает ошибку в базе данных:</span><span class="sxs-lookup"><span data-stu-id="9fdba-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

![Отображается ошибка «Недопустимые данные», если не указано значение требуемого столбца](displaying-data/_static/image15.png)

<span data-ttu-id="9fdba-219">Нажмите кнопку **ОК** вернуться и исправить записи (года для «Ghostbusters» — 1984 г.) и нажмите клавишу ВВОД.</span><span class="sxs-lookup"><span data-stu-id="9fdba-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="9fdba-220">Заполните несколько фильмов, пока не получится 8, или чтобы.</span><span class="sxs-lookup"><span data-stu-id="9fdba-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="9fdba-221">(Ввод 8 упрощает для работы с разбиение по страницам в более поздней версии.</span><span class="sxs-lookup"><span data-stu-id="9fdba-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="9fdba-222">Но если это слишком много, введите несколько сейчас). Фактические данные не имеет значения.</span><span class="sxs-lookup"><span data-stu-id="9fdba-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Сетка запись базы данных в WebMatrix с либо записей](displaying-data/_static/image16.png)

<span data-ttu-id="9fdba-224">Если вы ввели все фильмы без ошибок, значения Идентификаторов расположены последовательно.</span><span class="sxs-lookup"><span data-stu-id="9fdba-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="9fdba-225">При попытке сохранить запись неполные фильма, числовые Идентификаторы могут не быть последовательными.</span><span class="sxs-lookup"><span data-stu-id="9fdba-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="9fdba-226">Если Да, это нормально.</span><span class="sxs-lookup"><span data-stu-id="9fdba-226">If so, that's okay.</span></span> <span data-ttu-id="9fdba-227">Числа не несут любой определенное контекстуальное значение и — это единственное, что важно, что они являются уникальными в *фильмы* таблицы.</span><span class="sxs-lookup"><span data-stu-id="9fdba-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="9fdba-228">Закройте вкладку, которая содержит конструктор баз данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="9fdba-229">Теперь вы можете включить для вывода этих данных на веб-странице.</span><span class="sxs-lookup"><span data-stu-id="9fdba-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="9fdba-230">Отображение данных на странице с помощью вспомогательного элемента WebGrid</span><span class="sxs-lookup"><span data-stu-id="9fdba-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="9fdba-231">Для отображения данных на странице, вы собираетесь использовать `WebGrid` вспомогательный.</span><span class="sxs-lookup"><span data-stu-id="9fdba-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="9fdba-232">Этот вспомогательный выдаются в виде сетки или таблицы (строки и столбцы).</span><span class="sxs-lookup"><span data-stu-id="9fdba-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="9fdba-233">Как вы увидите, вы будете может выделить сетки с помощью форматирования и других функций.</span><span class="sxs-lookup"><span data-stu-id="9fdba-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="9fdba-234">Для запуска в сетке, вам придется написать несколько строчек кода.</span><span class="sxs-lookup"><span data-stu-id="9fdba-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="9fdba-235">Эти несколько строк будет использоваться в качестве своего рода шаблон для почти всех доступа к данным, как и в этом руководстве.</span><span class="sxs-lookup"><span data-stu-id="9fdba-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="9fdba-236">Фактически существует несколько вариантов для отображения данных на странице; `WebGrid` вспомогательный — один.</span><span class="sxs-lookup"><span data-stu-id="9fdba-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="9fdba-237">Мы выбрали его в этом руководстве, поскольку это самый простой способ отображения данных и является достаточно гибким.</span><span class="sxs-lookup"><span data-stu-id="9fdba-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="9fdba-238">В рамках следующего учебного курса вы увидите, как использовать дополнительные «ручной» способ работы с данными в странице, которая обеспечивает более прямой контроль над тем, как для отображения данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>


<span data-ttu-id="9fdba-239">В левой области в WebMatrix, щелкните **файлы** рабочей области.</span><span class="sxs-lookup"><span data-stu-id="9fdba-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="9fdba-240">Является новой базы данных, созданную в *приложения\_данных* папки.</span><span class="sxs-lookup"><span data-stu-id="9fdba-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="9fdba-241">Если папка не существует, WebMatrix он создан для новой базы данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="9fdba-242">(Папка может существовать Если вспомогательные функции были установлены ранее.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="9fdba-243">В представлении в виде дерева выберите корень веб-сайта.</span><span class="sxs-lookup"><span data-stu-id="9fdba-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="9fdba-244">Необходимо выбрать корневой веб-сайта; в противном случае новый файл могут быть добавлены в приложение\_папку данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="9fdba-245">На ленте щелкните **New**.</span><span class="sxs-lookup"><span data-stu-id="9fdba-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="9fdba-246">В **выберите тип файла** выберите **CSHTML**.</span><span class="sxs-lookup"><span data-stu-id="9fdba-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="9fdba-247">В **имя** окне имя новой страницы «Movies.cshtml»:</span><span class="sxs-lookup"><span data-stu-id="9fdba-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

![«Выберите тип файла» диалоговое окно страницы «Фильмы»](displaying-data/_static/image17.png)

<span data-ttu-id="9fdba-249">Нажмите кнопку **ОК** кнопки.</span><span class="sxs-lookup"><span data-stu-id="9fdba-249">Click the **OK** button.</span></span> <span data-ttu-id="9fdba-250">WebMatrix открывает новый файл с одними элементами схемы в нем.</span><span class="sxs-lookup"><span data-stu-id="9fdba-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="9fdba-251">Сначала вы будете писать код, нужно получить данные из базы данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="9fdba-252">Затем вы добавите разметки страницы, чтобы собственно, отображения данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="9fdba-253">Написание кода запроса данных</span><span class="sxs-lookup"><span data-stu-id="9fdba-253">Writing the Data Query Code</span></span>

<span data-ttu-id="9fdba-254">В верхней части страницы между `@{` и `}` символы, введите следующий код.</span><span class="sxs-lookup"><span data-stu-id="9fdba-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="9fdba-255">(Убедитесь, что вы введете этот код между открывающей и закрывающей фигурными скобками.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="9fdba-256">Первая строка открывает базы данных, который был создан ранее, который всегда является первым шагом перед действия с базой данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="9fdba-257">Вы указываете `Database.Open` метод имя базы данных, чтобы открыть.</span><span class="sxs-lookup"><span data-stu-id="9fdba-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="9fdba-258">Обратите внимание, что вы не включаете *.sdf* в имени.</span><span class="sxs-lookup"><span data-stu-id="9fdba-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="9fdba-259">`Open` Метод предполагает, что он ищет *.sdf* файла (то есть *WebPagesMovies.sdf*) и что *.sdf* файл находится в *приложения\_ Данные* папки.</span><span class="sxs-lookup"><span data-stu-id="9fdba-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="9fdba-260">(Ранее было отмечено, *приложения\_данных* папка является зарезервированным; этот сценарий является одно из мест, где ASP.NET делает предположения о существовании.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="9fdba-261">При открытии базы данных, ссылку на него помещается в переменную с именем `db`.</span><span class="sxs-lookup"><span data-stu-id="9fdba-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="9fdba-262">(Который может быть присвоено любое имя.) `db` Переменная доступна как в результате получается взаимодействия с базой данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="9fdba-263">Вторая строка фактически выполняет выборку данных базы данных с помощью `Query` метод.</span><span class="sxs-lookup"><span data-stu-id="9fdba-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="9fdba-264">Обратите внимание на то, как работает этот код: `db` переменная имеет ссылку на открытую базу данных, и вы вызываете `Query` метод с помощью `db` переменной (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="9fdba-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="9fdba-265">Сам запрос — это SQL `Select` инструкции.</span><span class="sxs-lookup"><span data-stu-id="9fdba-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="9fdba-266">(Немного Общие сведения о SQL, ознакомьтесь с описанием в более поздней версии.) В операторе `Movies` определяет таблицу для запроса.</span><span class="sxs-lookup"><span data-stu-id="9fdba-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="9fdba-267">`*` Знак означает, что запрос должен возвращать все столбцы из таблицы.</span><span class="sxs-lookup"><span data-stu-id="9fdba-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="9fdba-268">(Можно также указать столбцы по отдельности, разделенных запятыми.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="9fdba-269">Результаты запроса, если таковые имеются, возвращаются и становятся доступными в `selectedData` переменной.</span><span class="sxs-lookup"><span data-stu-id="9fdba-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="9fdba-270">Опять же переменная может быть присвоено любое имя.</span><span class="sxs-lookup"><span data-stu-id="9fdba-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="9fdba-271">Наконец, третья строка сообщает ASP.NET, что вы хотите использовать экземпляр `WebGrid` вспомогательный.</span><span class="sxs-lookup"><span data-stu-id="9fdba-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="9fdba-272">Создании (*создать экземпляр*) вспомогательный объект, с помощью `new` ключевое слово и передайте его в результатах запроса с помощью `selectedData` переменной.</span><span class="sxs-lookup"><span data-stu-id="9fdba-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="9fdba-273">Новый `WebGrid` объекта вместе с результатами запроса к базе данных, становятся доступными в `grid` переменной.</span><span class="sxs-lookup"><span data-stu-id="9fdba-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="9fdba-274">Полученный результат вам потребуется чуть фактически выводит данные на странице.</span><span class="sxs-lookup"><span data-stu-id="9fdba-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="9fdba-275">На этом этапе база данных открыта, вы азы данных требуется, и после подготовки `WebGrid` вспомогательное приложение с помощью этих данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="9fdba-276">Следующий шаг — создание разметки на странице.</span><span class="sxs-lookup"><span data-stu-id="9fdba-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> **<span data-ttu-id="9fdba-277">Язык SQL (Structured Query Language)</span><span class="sxs-lookup"><span data-stu-id="9fdba-277">Structured Query Language (SQL)</span></span>**
> 
> <span data-ttu-id="9fdba-278">SQL — это язык, который используется в большинство реляционных баз данных для управления данными в базе данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="9fdba-279">Она включает команды, которые позволяют получать данные и обновить его, и, которые позволяют создавать, изменять и управлять данными в таблицах базы данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="9fdba-280">SQL отличается от языка программирования, (например C#).</span><span class="sxs-lookup"><span data-stu-id="9fdba-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="9fdba-281">С помощью SQL указать базу данных, то, что нужно, и это задание базы данных, чтобы выяснить, как получить данные или выполнять задачи.</span><span class="sxs-lookup"><span data-stu-id="9fdba-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="9fdba-282">Ниже приведены примеры некоторых команд SQL и что они делают.</span><span class="sxs-lookup"><span data-stu-id="9fdba-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="9fdba-283">Первый `Select` инструкция возвращает все столбцы (определяется `*`) из *фильмы* таблицы.</span><span class="sxs-lookup"><span data-stu-id="9fdba-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="9fdba-284">Второй `Select` инструкция извлекает идентификатор, имя и цена столбцы из записей в *продукта* таблицы, значение столбцов которых цена — более чем 10.</span><span class="sxs-lookup"><span data-stu-id="9fdba-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="9fdba-285">Эта команда возвращает результаты в алфавитном порядке на основе значений имени столбца.</span><span class="sxs-lookup"><span data-stu-id="9fdba-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="9fdba-286">Если нет записей, соответствующих критериям цена, команда возвращает пустой набор.</span><span class="sxs-lookup"><span data-stu-id="9fdba-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="9fdba-287">Эта команда вставит новую запись в *продукта* таблице, задание для 1,99 столбце имен, чтобы «Круассан», столбца описания «Ненадежных удовлетворят A», а также цену.</span><span class="sxs-lookup"><span data-stu-id="9fdba-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="9fdba-288">Обратите внимание на то, что при указании нечисловое значение, значение заключено в одинарные кавычки (не двойные кавычки, как в C#).</span><span class="sxs-lookup"><span data-stu-id="9fdba-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="9fdba-289">Можно использовать эти кавычки вокруг значений текст или даты, но не вокруг чисел.</span><span class="sxs-lookup"><span data-stu-id="9fdba-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="9fdba-290">Эта команда удаляет записи в *продукта* таблицы, в которой столбец даты окончания срока действия более ранняя, чем 1 января 2008 г.</span><span class="sxs-lookup"><span data-stu-id="9fdba-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="9fdba-291">(Команда предполагает, что *продукта* таблица имеет такой столбец, конечно.) Здесь вводится дата в формате мм/дд/гггг, но должно вводиться в формате, который используется для используемого языкового стандарта.</span><span class="sxs-lookup"><span data-stu-id="9fdba-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="9fdba-292">`Insert` И `Delete` команды не возвращают результирующие наборы.</span><span class="sxs-lookup"><span data-stu-id="9fdba-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="9fdba-293">Вместо этого они возвращают число, которое указывает, сколько записей, затронутых командой.</span><span class="sxs-lookup"><span data-stu-id="9fdba-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="9fdba-294">Для некоторых из этих операций (например, вставка и удаление записей) процесс, который запрашивает операцию должен иметь соответствующие разрешения в базе данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="9fdba-295">Именно для рабочих баз данных, обычно требуется ввести имя пользователя и пароль при подключении к базе данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="9fdba-296">Существует множество команд SQL, но все они соответствуют шаблону, как и команды, которые вы видите здесь.</span><span class="sxs-lookup"><span data-stu-id="9fdba-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="9fdba-297">Команды SQL можно использовать для создания таблиц баз данных, подсчитать число записей в таблице, расчета цен и выполнять многие другие операции.</span><span class="sxs-lookup"><span data-stu-id="9fdba-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>


### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="9fdba-298">Добавив разметку для отображения данных</span><span class="sxs-lookup"><span data-stu-id="9fdba-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="9fdba-299">Внутри `<head>` элемент, содержимое набора `<title>` элемент «Кино»:</span><span class="sxs-lookup"><span data-stu-id="9fdba-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="9fdba-300">Внутри `<body>` элемент страницы, добавьте следующий код:</span><span class="sxs-lookup"><span data-stu-id="9fdba-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="9fdba-301">Вот и все.</span><span class="sxs-lookup"><span data-stu-id="9fdba-301">That's it.</span></span> <span data-ttu-id="9fdba-302">`grid` Переменной является значение, созданное при создании `WebGrid` объект в коде ранее.</span><span class="sxs-lookup"><span data-stu-id="9fdba-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="9fdba-303">В представлении в виде дерева WebMatrix, щелкните страницу правой кнопкой мыши и выберите **запустить в браузере**.</span><span class="sxs-lookup"><span data-stu-id="9fdba-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="9fdba-304">Вы увидите страницу вроде этой:</span><span class="sxs-lookup"><span data-stu-id="9fdba-304">You'll see something like this page:</span></span>

![По умолчанию выходные данные вспомогательной функции WebGrid из таблицы фильмы](displaying-data/_static/image18.png)

<span data-ttu-id="9fdba-306">Щелкните ссылку заголовка столбца для сортировки по этому столбцу.</span><span class="sxs-lookup"><span data-stu-id="9fdba-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="9fdba-307">Возможность сортировать, щелкая заголовок — это функция, которая встроена в **WebGrid** вспомогательный.</span><span class="sxs-lookup"><span data-stu-id="9fdba-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="9fdba-308">`GetHtml` Метод, как предполагает имя, создает разметку, которая отображает данные.</span><span class="sxs-lookup"><span data-stu-id="9fdba-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="9fdba-309">По умолчанию `GetHtml` метод формирует HTML `<table>` элемент.</span><span class="sxs-lookup"><span data-stu-id="9fdba-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="9fdba-310">(Если требуется, вы можете проверить отрисовку, просмотрев исходный код страницы в браузере.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="9fdba-311">Изменение внешнего вида сетки</span><span class="sxs-lookup"><span data-stu-id="9fdba-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="9fdba-312">С помощью `WebGrid` вспомогательный, как только прост, но дисплее обычный.</span><span class="sxs-lookup"><span data-stu-id="9fdba-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="9fdba-313">`WebGrid` Вспомогательный содержит все параметры, позволяющие управлять отображением данных.</span><span class="sxs-lookup"><span data-stu-id="9fdba-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="9fdba-314">Существует слишком много для изучения этого руководства, но этот раздел дает вам представление о том, некоторые из этих параметров.</span><span class="sxs-lookup"><span data-stu-id="9fdba-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="9fdba-315">В последующих руководствах этой серии рассматриваются несколько дополнительных параметров.</span><span class="sxs-lookup"><span data-stu-id="9fdba-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="9fdba-316">Указав отдельные столбцы для отображения</span><span class="sxs-lookup"><span data-stu-id="9fdba-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="9fdba-317">Чтобы начать, можно указать, что будет отображаться только определенные столбцы.</span><span class="sxs-lookup"><span data-stu-id="9fdba-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="9fdba-318">По умолчанию, как вы убедились, этой сетке перечислены все столбцы из всех четырех *фильмы* таблицы.</span><span class="sxs-lookup"><span data-stu-id="9fdba-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="9fdba-319">В *Movies.cshtml* файл, замените `@grid.GetHtml()` разметка, которую вы только что добавили на следующий:</span><span class="sxs-lookup"><span data-stu-id="9fdba-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="9fdba-320">Чтобы сообщить вспомогательное приложение столбцы для отображения, включите `columns` параметр для `GetHtml` метод и передать в коллекцию столбцов.</span><span class="sxs-lookup"><span data-stu-id="9fdba-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="9fdba-321">В коллекции укажите каждый столбец для включения.</span><span class="sxs-lookup"><span data-stu-id="9fdba-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="9fdba-322">Укажите отдельный столбец для отображения, включая `grid.Column` и имя столбца данных, необходимо передать.</span><span class="sxs-lookup"><span data-stu-id="9fdba-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="9fdba-323">(Эти столбцы должны быть включены в результаты запроса SQL — `WebGrid` вспомогательный невозможно отобразить столбцы, которые не были возвращены запросом.)</span><span class="sxs-lookup"><span data-stu-id="9fdba-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="9fdba-324">Запустите *Movies.cshtml* страницу в браузере еще раз и на этот раз появится экран, аналогичный приведенному ниже (Обратите внимание, что столбец не идентификатор):</span><span class="sxs-lookup"><span data-stu-id="9fdba-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![WebGrid окна, содержащего только выбранные столбцы](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="9fdba-326">Изменение внешнего вида сетки</span><span class="sxs-lookup"><span data-stu-id="9fdba-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="9fdba-327">Существует довольно много Дополнительные параметры для отображения столбцов, некоторые из которых мы подробно рассмотрим в последующих руководствах в этом наборе.</span><span class="sxs-lookup"><span data-stu-id="9fdba-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="9fdba-328">Сейчас в этом разделе вы узнаете о способами, в котором можно стилистически сетки в целом.</span><span class="sxs-lookup"><span data-stu-id="9fdba-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="9fdba-329">Внутри `<head>` раздел страницы, непосредственно перед закрывающим `</head>` , добавьте следующие `<style>` элемент:</span><span class="sxs-lookup"><span data-stu-id="9fdba-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="9fdba-330">Эта разметка CSS определяет классы называются `grid`, `head`, и т. д.</span><span class="sxs-lookup"><span data-stu-id="9fdba-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="9fdba-331">Можно также поместить эти определения стиля в отдельном *.css* файл и связать его на страницу.</span><span class="sxs-lookup"><span data-stu-id="9fdba-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="9fdba-332">(На самом деле, можно сделать позже в рамках этого учебного курса.) Но чтобы упростить вещи в этом руководстве, они внутри той же странице, которая отображает данные.</span><span class="sxs-lookup"><span data-stu-id="9fdba-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="9fdba-333">Теперь вы можете настроить `WebGrid` вспомогательный метод для использования этих классов стиля.</span><span class="sxs-lookup"><span data-stu-id="9fdba-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="9fdba-334">Вспомогательное приложение имеет несколько свойств (например, `tableStyle`) для этого — назначить имя класс стиля CSS к ним, а имя этого класса отображается как часть разметки, отображаемой в вспомогательной функцией.</span><span class="sxs-lookup"><span data-stu-id="9fdba-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="9fdba-335">Изменение `grid.GetHtml` разметки, так что он теперь выглядит следующим образом:</span><span class="sxs-lookup"><span data-stu-id="9fdba-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="9fdba-336">Новые возможности здесь —, которые были добавлены `tableStyle`, `headerStyle`, и `alternatingRowStyle` параметров `GetHtml` метод.</span><span class="sxs-lookup"><span data-stu-id="9fdba-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="9fdba-337">Эти параметры заданы имена CSS-стилей, которые были добавлены только что перечислили.</span><span class="sxs-lookup"><span data-stu-id="9fdba-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="9fdba-338">Выполните страницы, и это время вы увидите сетку, которая выглядит гораздо менее plain, чем до:</span><span class="sxs-lookup"><span data-stu-id="9fdba-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![Отображение WebGrid, что включает в себя параметры, задайте имена класс CSS](displaying-data/_static/image20.png)

<span data-ttu-id="9fdba-340">Чтобы увидеть, что `GetHtml` метод, созданный, вы можете просмотреть исходный код страницы в браузере.</span><span class="sxs-lookup"><span data-stu-id="9fdba-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="9fdba-341">Мы не будем останавливаться здесь подробно, но важно ли это, указав параметры, например `tableStyle`, вызвавшего сетки для создания HTML-теги, следующим образом:</span><span class="sxs-lookup"><span data-stu-id="9fdba-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="9fdba-342">`<table>` Имел тега `class` атрибут, добавленный к нему, который ссылается на одно из правил CSS, добавленным ранее.</span><span class="sxs-lookup"><span data-stu-id="9fdba-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="9fdba-343">Этот код показан базовый шаблон &mdash; различные параметры для `GetHtml` позволяют вам ссылки на метод CSS классов, затем метод создает вместе с ней.</span><span class="sxs-lookup"><span data-stu-id="9fdba-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="9fdba-344">Что делать с классы CSS возлагается.</span><span class="sxs-lookup"><span data-stu-id="9fdba-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="9fdba-345">Добавление разбиения на страницы</span><span class="sxs-lookup"><span data-stu-id="9fdba-345">Adding Paging</span></span>

<span data-ttu-id="9fdba-346">В качестве последней задачи в этом руководстве вы добавите разбиения на страницы в сетку.</span><span class="sxs-lookup"><span data-stu-id="9fdba-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="9fdba-347">Сейчас это не проблема для отображения всех фильмов за один раз.</span><span class="sxs-lookup"><span data-stu-id="9fdba-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="9fdba-348">Но если вы добавили сотни фильмы, отображением страницы получит long.</span><span class="sxs-lookup"><span data-stu-id="9fdba-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="9fdba-349">В коде страницы, измените строку, которая создает `WebGrid` объект на следующий код:</span><span class="sxs-lookup"><span data-stu-id="9fdba-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="9fdba-350">Единственное отличие от перед —, которые были добавлены `rowsPerPage` параметр, который имеет значение 3.</span><span class="sxs-lookup"><span data-stu-id="9fdba-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="9fdba-351">Откройте страницу.</span><span class="sxs-lookup"><span data-stu-id="9fdba-351">Run the page.</span></span> <span data-ttu-id="9fdba-352">В сетке отображаются 3 строки во времени, а также ссылки навигации, которые позволяют постраничный фильмами в базе данных:</span><span class="sxs-lookup"><span data-stu-id="9fdba-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![Отображение WebGrid с разбиением на страницы](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="9fdba-354">В ближайшее время</span><span class="sxs-lookup"><span data-stu-id="9fdba-354">Coming Up Next</span></span>

<span data-ttu-id="9fdba-355">В следующем руководстве вы узнаете, как использовать код Razor и C# для получения ввода пользователя в форме.</span><span class="sxs-lookup"><span data-stu-id="9fdba-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="9fdba-356">Вы добавите поле поиска на страницу Movies могли найти фильмов по заголовку или жанра.</span><span class="sxs-lookup"><span data-stu-id="9fdba-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="9fdba-357">Полный список фильмов страницы</span><span class="sxs-lookup"><span data-stu-id="9fdba-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="9fdba-358">Дополнительные ресурсы</span><span class="sxs-lookup"><span data-stu-id="9fdba-358">Additional Resources</span></span>

- [<span data-ttu-id="9fdba-359">Введение в веб-программирование для ASP.NET с использованием синтаксиса Razor</span><span class="sxs-lookup"><span data-stu-id="9fdba-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> <span data-ttu-id="9fdba-360">[Назад](intro-to-web-pages-programming.md)
> [Вперед](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="9fdba-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
